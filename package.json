{
  "name": "kookie-ui-v2",
  "private": true,
  "description": "KookieUI v2 workspace. The @kushagradhawan/kookie-ui-react package, the documentation site and the governance documents.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/KushagraDhawan1997/kookie-ui-v2.git"
  },
  "type": "module",
  "packageManager": "pnpm@12.4.1",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "measure": "turbo run measure",
    "lint:root": "eslint .",
    "ci": "turbo run build test lint measure && pnpm run lint:root",
    "changeset": "changeset"
  },
  "devDependencies": {
    "@changesets/cli": "^3.0.2",
    "@types/node": "^26.5.1",
    "eslint": "^10.10.0",
    "turbo": "^2.10.12",
    "typescript-eslint": "^8.70.0"
  }
}
