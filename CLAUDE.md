# KookieUI v2

Ground-up rebuild of KookieUI. Base UI primitives behind a Kookie-owned API, generated OKLCH color, token-only styling, semantic axes, hard CSS budget.

## State

Blockers resolved 2026-07-31; code phase open, following §14's build order strictly.

Done: scaffold (1), token pipeline including the colour generator (2), Box and the responsive mechanism (3), Theme + Flex/Stack/Grid (4), and the pointer axis tokens (4b) — coarse cells, Theme `pointer` prop, preview matrix, laws. No runtime touch reserve: the designed geometry carries both the locked 24 floor (WCAG 2.2 AA) and the 44 target on the default path, so every control is one element. Theme shipped with dark-mode SSR still unresolved (REVIEW.md); that debt came due at apps/docs and was paid there 2026-08-05.

**Button shipped 2026-08-03 (step 5)** on Base UI, the first runtime dependency: `size × tone × emphasis × bordered × loading`, plus Spinner. Exit gate met — +1,206 bytes gzipped for the whole control layer, of which ~480 is Button's own; additivity is law-tested, not just measured. Confirmed on a real iPhone. Every transition shipped zero for six days (Kushagra's call, and the finding behind it survived the motion system intact — see the motion paragraph below: a press's PAINT is still instant, it is only its geometry that moves). **`material` returned 2026-08-04, and became a fill MODIFIER the same day** (§10 "Material is a fill modifier"): the veil is the component's *own* fill mixed toward transparent at the thickness alpha (`--kui-fill-src` indirection in both layers) — tone and loudness survive the glass, quiet is bare blur, the rung keeps its own label pairing, interaction steps the mix alphas, the filter never moves with state. Card is the special case (seal @ alpha = page glass, visually unchanged); derived fills carry `@property inherits:false` guards. v0 alphas, judged in the preview.

**Card shipped 2026-08-03 (step 6), then stripped to a shell 2026-08-04**: `size × material` (solid | thin | regular | thick) and children, one fixed treatment (opaque --color-surface seal + border), no tone/emphasis/bordered props, no anatomy slots, zero CSS of its own (a law asserts card.css does not exist). The governing criterion (LOG 2026-08-04): anatomy is system-owned only where something non-visual forces it (Dialog a11y, Callout status); layouts are blocks (kookie-blocks). Card-as-button is `render={<button/>}` — the surface layer notices element semantics.

**The elevation axis is deleted; depth is an app identity.** No call site chooses a shadow. Theme `depth="flat"|"elevated"` (renamed from `surfaces` 2026-08-10; DEFAULT `elevated` since 2026-08-17, and the chrome roles took the lab's values — see the 2026-08-17 paragraph below) is the one sanctioned consumer: --surface-chrome composed var(--shadow-3), --control-chrome var(--shadow-2) (2026-08-07: the palette is FIVE rows, one contact+ambient anatomy per height, row 2 the control drop — and the elevated world's light reaches Button: loud casts row 2 and catches a gradient, medium casts only — a wash over pastel is fog — reversing the button-stays-flat law under the four-worlds frame, §19). The border stays --tone-border (sharp, and contrast="high" reaches it); dark adds only the inset rim-lights. --shadow-1..5 is otherwise a §13 resource for escapes/blocks via `style`.

**Layout space shipped 2026-08-04 (§3, §12):** `--layout-space-N`, the density-aware semantic layer every layout distance consumes — gap on Flex/Stack/Grid, Box `p`/`m`, surface padding (fixed picks, re-baked per density scope). Default is the 1:1 identity map onto the untouched space palette; compact/comfortable shift picks for steps 1-8 while the gutter band 9-12 holds. Raw space = palette + control-family picks (innards answer density via the designed sets, never twice); pointer never touches the layer. Preview has a page-wide density select.

**Surface radius is size-indexed (2026-08-04, §6):** `--radius-surface-1..4` picks from a widened surface band (palette 0-10: control 1-5, surface 6-9, overlay 10); size 3 anchored each level's old flat value UNTIL the lab port re-authored the band (2026-08-17 paragraph below); density never touches a surface corner; the semantics re-declare in every `[data-radius]` block (substitution-at-declaration). The preview's demos now space exclusively through kuiBox (flex + gap via the real resolver) — demos modeling margins was called out as a law violation and fixed.

**Audited 2026-08-03 (multi-agent, adversarially verified), and the findings are fixed.** Twenty-two defects, clustered in two places. The escape scopes: `contrast="high"` resolved **no rule at all** in light (the block was `:root`-scoped; Theme renders a div), `prefers-contrast: more` could never fire in dark (Theme stamped the `normal` the guard excludes), and `appearance="light"` had no block, so a light section inside a dark app stayed dark. The claimed-versus-actual law surface: four guarantees the docs named as CI-asserted could not fail, and writing them exposed a WCAG 2.4.11 failure — the dark focus ring sat at |Lc| 22 against the page and is now step 11 per mode. Also fixed: `inset` and `overflow` shipped as dead no-ops (shorthand before longhand, the §2 defect LOG 2026-08-02 already records fixing once for padding); `render={<a/>}` emitted `type="button"` and a focusable unannounced dead link; the `render` escape overwrote the target's ref, style and children; the control size join leaked the whole control family onto every Card; the chrome widths were raw px and the only geometry ignoring `--scale`; dark's seal had no hover step; `surfaces="flat"` (now `depth`) could not escape an elevated ancestor; and `build` regenerated the very files the drift laws read, so ENGINEERING's "a hand edit fails CI for everyone" was false.

**The lesson, and the standing rule it earned:** every broken axis was one where no law read a *computed value*. The tests asserted attribute strings, token *names*, and in-memory objects — each one indirection short of the thing that could be wrong, and in each case that thing was wrong. An axis is now proven by a law that reads a computed token through a mounted `<Theme>` in both appearances, or the emitted declarations where the question is what the generator wrote. Every fix in that sweep was falsified against the pre-fix code before it was accepted. 299 tests (was 265); budget 9358 → 9584 gzipped, re-recorded per commit.

**Text + Heading shipped 2026-08-04 (§15)**, the first of the post-slice repetition: one type system through two family slots — `size` 1–9 joining the paired scales, `weight` as token names, no tone (they read the surface's foreground context, §11) — resolved by the shared type layer `system/type.css`; neither ships CSS of its own, law-tested, and type's density/pointer invariance is a mounted law. **`emphasis` reached type the same day** (Kushagra's call), the third resolution of the one axis: fills for controls, dressing for surfaces, foreground roles here — loud `--color-text`, medium muted, quiet the faint role (v0 neutral-10 then; a solved target since 2026-08-10, below). Text rests loud (full contrast is the accessible resting state for reading); on a loud surface the ladder collapses to `--tone-contrast`. **`tone` followed the same day** (Kushagra: full ladder per family, semantic only): the ink trio `--{tone}-ink/-ink-muted/-ink-faint` joins the tone indirection — neutral's inks were steps 12/11/10 (≡ the tone-less roles) and a chroma family faded its one designed text colour (11) by v0 percentages — BOTH replaced 2026-08-10 by one solved rule, below. Stamped only when chosen; an explicit tone survives the loud-surface collapse. **The tone set widened to six the same day** (Kushagra: "a few basic families", the middle ground vs Radix's 30): blue/green/orange join as colour-as-data vocabulary, ~1.15KB gzipped each, `Tone` derives from config. The widening caught a generator drift — the state-direction code didn't implement its own comment's rule — fixed as a flip GATED on the APCA label law. **Amber joined 2026-08-05 at vividness 0.9** — the refusal was real, but the input space was not exhausted: at full vividness the resting solid parks ON the sRGB cusp, where the mud-guard halts state travel at .032 against a .035 floor (refused by .003, which is why the preview looked fine while the law said no); ten percent off the cusp buys the visible press state, and no generator exception was ever needed. LOG records the declined brighter routes, including Radix's own carry-the-light-pin-into-dark. **The semantic core completed the same day** (§2, §7, Kushagra closing the standing open question): success/warning/info join for **ten tones**, each its OWN generated family resolving to an already-judged pigment — success on green's hue, warning on amber's (the cusp finding is what made warning possible), info on blue's — never an alias, so a meaning stays independently correctable without dragging the data colour it currently shares a hue with. **Accent became hue-authored the same day**: `{ hue: 250, vividness: 1 }`, blue's own recipe rather than a pinned copy of its rendered hex, so the two resolve identically by construction and cannot drift (−539 bytes: accent ≡ blue compresses). **Accent was GREY from 2026-08-10 to 2026-08-16** (Kushagra) — neutral's recipe verbatim, so that identity pointed one family over; the generator needed no exception because `lowChromaThreshold` already routes a low-chroma solid to step 12 and is keyed on chroma rather than on the name "neutral", which put the system's primary button at near-black in light and near-white in dark. It cost +58 bytes and it broke four laws that proved an axis by contrasting `accent` against `neutral`. **It is BLUE again since 2026-08-16, and the material work is what ended it**: a fill under glass is the component's own colour mixed toward transparent, so saturation is what survives the mix — a brand with no chroma arrives as smoke on every translucent surface while the categorical families beside it still read. `{ hue: 250, vividness: 1 }`, blue's recipe verbatim. **The brand is VIOLET since 2026-08-26** — `{ hue: 295, vividness: 1 }`, solid `#8410ff`, judged live against a day of alternatives (blue is one config line away), landing with the generator's two bright-brand mechanisms: the focus ring SOLVES down the hue when its picked step misses any of its three beds, and high contrast walks its state excursion down to the largest travel the label can absorb, so yellow and neon green are brandable too. **`accent ≡ blue` no longer holds**, for two independent reasons — the hue moved, and `undilutedTones` made accent a ROLE (its washed rungs point at neutral, so an accent is never faded), which cost the 539 bytes that equality was worth and bought back 40 in the neutral block. Neutral's hue now DERIVES from the accent, so the greys lean toward the brand by construction. The low-chroma branch is untouched and still keyed on chroma, so a desaturated brand is as supported as it ever was.

**TextField shipped 2026-08-04 — repetition's first entry, and the additivity claim's first real test.** `size × material` + `leading`/`trailing` slots + the native input's props, for **+247 bytes gzipped** (against Button's +1,206 for the layer it created). No emphasis and no tone: loudness ranks actions, and a form where one field is louder than the next names nothing. The visible control is a WRAPPER around the input — which is what makes slots legitimate anatomy here after Card refused them (the border cannot stay on the `<input>` once an icon sits inside it, and the wrapper then owes caret-on-click, slot-aware layout, and a trailing control that keeps its own press). Validity is state (`aria-invalid` / Base UI's `data-invalid`), and its tone remap went into the SHARED layer beside disabled. Focus rings when the caret is in it — a field's focus is a mode, not a keyboard affordance. `ref` goes to the input; `className`/`style` dress the wrapper. **The value wears content dress (2026-08-05, §15):** regular weight, not the skeleton's medium — a button-label decision that reached the input through `font: inherit`, inherited and never decided; slots follow, a hosted control re-declares its own medium. The placeholder was the faint role, whose definition then named a placeholder as its case; it moved to MUTED on 2026-08-10 when that definition changed (below). Muted is also the slot colour.

**The audit after it found three "exactly one" claims already stale** (one box-shadow, one focus ring, three cursors), each guarded by a law that counted occurrences in a single file. Both laws now walk every shipped stylesheet and assert the value rather than the count — mutation-checked. Open, in the Open-questions list: TextField's absent `render` escape, and iOS zoom-on-focus at size 1. The adornment spelling closed 2026-08-04 — Button renamed to `leading`/`trailing`.

**A control hosted in a control is now a system rule (§4, 2026-08-04).** Judged in the preview: a trailing button read as a second field wedged inside the field, and it was — measured 1px of slack, 100% of the container's height under coarse at size 1, with the field GROWN 2px past its own size token in 16/16 cells. The system had no rule for nesting, so the call site had to pick a size index, and the mapping it was asked to infer was non-uniform and undefined at size 1. One designed `slotInset` per size now drives all four sides and the hosted height is derived from it; the touch hit area matches the CONTAINER's box rather than 44, so a hosted control never becomes a bigger target than the thing containing it. `iconOnly` lands with it — one Button, not a second IconButton component (v1's, which was opt-in by memory and simply forgotten), with the accessible name required by the type.

**Type bands: `handheld` and `narrow` (§17, split 2026-08-05 — supersedes the single conjunction-gated band).** The one band was doing two unrelated jobs: reading steps rose because a held screen is close to the eye, display steps fell because a narrow screen is seven characters wide. Kushagra caught it from Apple's own page — "iOS, iPadOS Dynamic Type sizes" is ONE table, Body 17pt on both — so the reading question does not distinguish phone from tablet, and our width gate excluded every iPad from a rise it should have had (at 768px it separated an iPad mini in portrait from every other iPad). Now `handheld` on `(pointer: coarse)` owns steps 1-4 and `narrow` on `(max-width: 48rem)` owns steps 8-9; each emits ONLY the steps it moves, so the two cannot fight on a phone where both apply. The four cells are law-tested, including the two that were wrong: a tablet in landscape rises and keeps its 56px display, and a squeezed desktop window cuts display without touching reading sizes. **The `device` prop is DROPPED (2026-08-05, Kushagra)** — its only surviving cases were the kiosk and the POS terminal, which we are not designing for; the band rides the pointer axis's own `[data-pointer]` scopes, so pinning `pointer` forces the whole coarse world, type included, and the preview's pointer select judges phone type (LOG records how to bring it back). **There is no tablet band**: sort what Figma-on-iPad changes and it is features (product), navigation (width), targets (pointer) and type (where Apple says tablet ≡ phone). The real gap it surfaced is **window-level size classes for app-shell layout** — tiers are container-keyed on purpose and nothing answers "which interface should this app show"; recorded open. `any-pointer` closed the same day, for geometry too: capability is not use. Preview says drag the window; the browser suite's viewport is pinned wide, because it had been silently sitting inside the narrow band.

**Control padding left the space palette 2026-08-05 (§3, §12)** — the radius bug one family over. The palette is a layout rhythm whose control band grows ~1.44x per step against a height ladder growing ~1.20x, so an index could not hold a fraction: default ran 0.286 -> 0.500, and coarse/comfortable ran out of palette and repeated a step (size 4 padded no wider than size 3). `px` joins `height` as a designed raw number per set; all 24 cells sit in 0.24-0.38 of their box, held by three laws that each fail against the shipped config. Size 4 down 33%. **`pxPill` joined the designed sets 2026-08-05 (§4, §6):** at `radius="full"` the corner swings inward at the text's cap line and the label read crammed, so full's cells carry their own raw padding (~0.40–0.44 of the box, just under the half-height capsule rule), per density and per pointer world — a raw length has no palette step to ride, so the pointer cells must exist. Applied PER SIDE, read off the DOM: a side whose content starts with a `[data-slot]` wrapper keeps the plain padding, because the slot already stands between the text and the curve. Button's slots got the wrapper they had been missing, which also extended §4's hosted-control geometry to Button. Same day, four TextField audit defects: the ring narrowed from `:focus-within` to the input holding focus (a hosted control was lighting two rings and the field was claiming a focus it did not have); a glass field's fill was still moving because material's mix ramp, not its sources, was what moved; `Field.Root disabled` never reached the wrapper (the shared layer now reads it with the same `:has()` arm `invalid` has); and `children` was typed on the props and spread onto the `<input>`. Plus the invalid remap's DIRECT arm, which had no law at all.

**The remaining audit debts closed 2026-08-05.** The `--kui-h` stem collision: the control layer shared three private names with the layout mechanism (`h`, `px`, `py` against Box's `height`/`px`/`py`), and because layout registers its stems `inherits: false` by NAME, `--kui-h` was silently absent wherever it was not declared — which is why the hosted-control height needed two hops. The control layer now wears `--kui-ct-` (matching surfaces' `--kui-sf-`), the second hop is deleted, and a law forbids any layer from so much as *mentioning* a name layout declares (`--kui-border-color` stays shared on purpose; two laws learned to strip comments so they stop firing on their own documentation). iOS zoom-on-focus: the hole was bigger than size 1 — an iPad in landscape reads as `desktop`, takes the 14px size-2 ladder, and Safari still zooms — so `--input-font-floor` rides the POINTER axis (0 fine / 16 coarse) and floors the input's font alone, leaving the box and label at their designed steps. `readOnly` shipped resolving to nothing and now drops the seal and only the seal (it is not disabled: the value is live, selectable, submitted). `type` closed to a union — unconstrained, `type="hidden"` rendered a visible empty box. Slot content reaches AT through `aria-describedby`, appended to the caller's. And the absent `render` escape is now a recorded refusal with a type law: there are two elements here and neither can move.

**Window size classes shipped 2026-08-05 (§18).** The one viewport question — "which interface should this app show" (container tiers answer a different question: how much room a component has). Three classes, `narrow | regular | wide`, named for room and never hardware (Kushagra's own argument: users run apps in windowed mode, so device names lie; `phone|tablet|desktop` and `narrow|medium|wide|full` both rejected — LOG). Three not four: a class picks the navigation shape and there are three shapes; Material's fourth/fifth compensate for lacking container queries; the set widens by config when a real shell forces it (`expanded` waits, the tone-set rule). The narrow boundary IS the narrow type band's 48rem BY DERIVATION — `narrowMedia` builds from `windowClass.narrowMax`, law-pinned, closing §17's threshold question; boundaries land downward, law-tested at exactly 768/1200. Wide is 75rem (64 opened, moved same day: at 1024 the content behind a sidebar is narrower than a narrow window; 80rem rejected — it puts the boundary exactly on 1280, where half of every 27" display lives; re-judged by eye when a shell exists). Mechanism: `useWindowClass()` over matchMedia — a hook because a shell is components; NO token moves with the class (law: generated CSS never mentions one). SSR returns `null` honestly — and stays null through first paint BY DESIGN: the "one script, both answers" bundling with dark SSR was retracted the same day (LOG) — CSS never needed a window stamp (media queries are native and pre-paint) and the hook cannot read one (hydration must match the server's null branch; next-themes has the same shape). A first-paint shell is CSS-shaped via the exported boundary queries; the hook is for post-mount decisions. Dark mode keeps its inline script alone. `data-window` exists the day pinning does, or never.

**TextArea shipped 2026-08-05 — repetition's second entry, +91 bytes gzipped** (1,206 → 247 → 91: the additivity curve's third point). ONE element UNTIL 2026-08-25 (glass reversed it — see the wrapper paragraph below), no wrapper, no slots — the anatomy criterion answered twice: TextField's wrapper exists because a slot forces the border off the input; a textarea has no slots, so the border stays on the `<textarea>` and every wrapper debt never exists. The first non-fixed-height control: per §4, padding is the dimension — and the dimension is ONE inset, all four sides (REVERSED same day, LOG "the frame is the inset": the first cut derived block padding from the height so one row matched a TextField, and the residue read as an accident with a second line — centering leftover is not an inset; every real textarea is a paragraph, a composer is its own component). Block padding = the plain side padding at every radius level — the pill bump stays horizontal-only, no exception for roundness, flagged for the eye pass. A rows={1} textarea sits taller than a field, accepted; control height survives as min-height; growth is `rows`. `resize` is vertical-only in the stylesheet, NOT a prop (renames raw CSS; `style` is the escape). Shared layer gained the disabled remap's third arm (`.kui-control:disabled` — the control IS the native form element, no stamp). Refused by the type: `children`, `cols`, `render`, `emphasis`/`tone`. Its facts repeat text-field.css self-keyed on purpose; the third field-shaped control promotes the family into the shared layer, not the second (LOG). **It immediately forced a §6 fix (same day, Kushagra caught it in the preview): full's control band now STATES the capsule** — `--radius-control-N: calc(var(--control-height-N)/2)` per (density × pointer) cell — instead of 9999px riding CSS clamping, which asks the RENDERED box and turned a three-row textarea into a stadium. Fixed-height controls render byte-identically; a grown box keeps the one-row curvature; the surface band's per-band precedent applied to the control band; the old law pinning the literal 9999px was the bug's spelling and is rewritten to radius ≡ h/2 (LOG).

**The material grew its own edge and rim 2026-08-05 (§10).** Kushagra, on the preview's glass cards: blur plus the ordinary card border read cheap, and it was — the pane owned only its body (the alpha mix and the filter) and borrowed everything else from systems designed for opaque, in-flow surfaces. Three parts landed, each per thickness and mode, all v0. The **edge** is the material's own translucent hairline, consumed with a `var(--tone-border)` fallback that resolves at the element — closing §10's long-open "does the material border stay opaque" with the platform answer it had already sketched. The **rim** is a top inner light catch painted as a `background-image` gradient, deliberately not a shadow: `none` is illegal inside a shadow list and flat worlds declare `none`, so a shadow rim would have meant rewriting every one-box-shadow law. And **depth stays the app's** — separation was welded to the glass for an hour and reversed on sight: rim and edge are what the material IS, lift is what the app SAYS, so glass in a flat world gets edge and glint and no shadow. `contrast="high"` leans on the glass rather than unmaking it — a designed `alphaHigh` triple per thickness (more opaque, never fully), with edge and rim emptied so the border falls back to the tone system, the one place that setting can reach an edge. Fading blur was built, judged and reverted the same session (recorded in LOG: cheap and real if scroll-edge chrome ever wants it). +136 bytes; baseline now 17,706 gzipped.

**Checkbox shipped 2026-08-05 — repetition's third entry, and the first control to leave the height ladder while keeping the size index (+311 bytes gzipped).** `size` and the native checkbox props; `tone`, `emphasis`, `material`, `render` and `children` are all refused by the type (§11 assigns the family its one tone — neutral off, accent on — which is an identity, not an axis; the LABEL is a sibling, because a mark sits beside its label and the row owns the space between them). What it introduces is **the mark family (§4): one ladder for every control that IS its own mark** — checkbox, radio, switch track, slider thumb — because four separately designed ladders in one visual weight class drift, and a checkbox beside a switch has to read as the same size of thing. The derivations are identities, never ratios (Kushagra: "I don't like fraction"): checkbox = radio = slider thumb = `mark(n)`, switch track = `mark(n+1)`, the one-index shift Radix arrives at by hand. **The ladder is the line box** — `--mark-N` IS `--line-height-N`, so a mark occupies exactly one line of its label, aligns with it by construction, and grows on a phone with nothing designed twice (§17's handheld band raises the type and the mark rides it; Spectrum reaches the same place by maintaining two scales by hand). 16/20/24/26 fine, 20/24/26/28 coarse. The space palette was tried first and cannot hold it — two rungs (16 and 24) across the whole range, so a four-step ladder repeats or overshoots: the control-padding failure's third instance, and Radix papered over the same wall with `calc(var(--space-5) * 5/6)`.

**Its target reopened a §16 rejection and narrowed it (§4).** Pseudo-element hit expansion was refused outright because "its safe extent depends on neighbour gaps, which CSS cannot read" — legitimate here because the extent is not guessed: it is `min(--control-height-N, 44)`, the box a control of that size already occupies (Kushagra's call — the hosted-control rule inverted: there the hit area shrinks to its container, here it grows to the container it never had). Both pointer worlds, not coarse-only: a fine cell's mark is under WCAG 2.5.8's minimum too. The cap bounds the reach at 11px; what makes a STACK safe is the spacing rule (2026-08-06, audit D1): marks in a list need 12 real pixels — below the reach, the later sibling's target overlaps the earlier one's painted box and wins the hit-test, so a click on one checkbox toggled the next, and the shipped claim of "clear at any layout-space gap" was false at the scale's bottom rungs. Law-mounted at exactly 12px in all 24 cells with a 4px negative control that must keep stealing. `--touch-target-min` gets its first stylesheet consumer. **Its corner is the family's own band** (§6, corrected the same day — Kushagra by eye: "size 4 looks much more rounded than size 1"). It shipped riding `--radius-control-N` with a ceiling at `full`, which was the fraction bug's third instance: those radii hold ~0.2 of the HEIGHT ladder, and a mark is not on it, so the corner held a fraction of a box the control does not have — 0.250 → 0.385 across the index, and **0.462 at comfortable size 4**, a circle in all but name. The ceiling could not catch that half, because it guarded the radius LEVELS and the offender was DENSITY, an axis that never touches the mark's box. `--radius-mark-N` picks into the palette on the family's own terms: a per-level band (small 0.08-0.13, medium 0.17-0.25, large/full 0.25-0.38 — the audit caught the first spelling claiming the middle band for all of them), density-invariant, levels still reach it (`none` squares it), and `full` holds at `large`'s values — the surface band's own sentence one band over. Two defects the laws caught while writing them, both the same shape — a component reaching past the shared layer: the resting box declared `--kui-border-color` directly, so the `invalid` remap (which rewrites tone ROLES) could not reach it; and the checked rule used `:is()`, tying with that remap and winning on source order, so a checked invalid checkbox looked fine. `:where()` states the rule instead: being ticked is dress, being invalid is a state, and a state outranks dress. `--color-border` is minted with it — "neutral off" needs a hairline with no family, and §11 already promised Separator one.

**Found while building it (2026-08-05), CLOSED 2026-08-08: the Box shrink-wrap collapse — containment is now opt-in.** Every `.kui-box` was a `container-type: inline-size` query container (§2), so its contents never contributed to its own width and a Box as a flex-row item computed to 0px — `<Flex><Box/><Box/></Flex>`, the most ordinary composition the library has, rendered invisible children. The playground's Layout section shipping broken was the real break the decision waited for. Closed as the `container` prop (Kushagra's call): a plain Box hugs its content exactly like a div (law-asserted against one), only a marked Box is measurable, tiers fall back through opted-in ancestors to the Theme root (the ROOT Theme only, and a container since 2026-08-16 narrowed it from every Theme — see the containment paragraph below; it behaves like the window), and the shrink-wrap price is paid only where the prop opted in — behind a dev-build warning that fires when a container Box renders 0px wide, JSDoc on the prop, and a registry refusal. No peer defaults containment (Tailwind's `@container` class is the same opt-in shape); automatic marking was rejected because the mark serves the box's children, so its own props are no signal. Laws pin both directions, falsified against the re-added blanket mark.

**Audited 2026-08-05 (ultracode, 40 agents, adversarially verified) and the findings closed 2026-08-06.** Thirty-one raised, fourteen confirmed, one refuted worth keeping (Base UI's label-association works across five shapes — do not re-raise). The headline pair: the invisible target was 2 x --border-width short of its own rule in all 24 cells (an absolutely positioned box resolves insets against the PADDING box; 22px at fine/compact/1 against the 24 floor), and below the target's reach a stacked mark's LATER sibling won the hit-test over the earlier one's painted pixels — the claim "clear at any layout-space gap" shipped false in three places. Closed as: the border term in the inset, and **the 12px stacking rule** (Kushagra's call — keep the expansion, state the spacing it obliges, check it automatically: two marks at exactly 12px in all 24 cells, every point inside the first's paint belongs to the first, plus a 4px negative control that must KEEP stealing). Also closed: **`--mark-edge`** (D2; superseded 2026-08-07 by the SOLVED `--control-edge`, worn by marks AND outlined fields — an unchecked checkbox IS its hairline, and --color-border sits at |Lc| 22.8 against a floor of 45 the system already enforced for the focus ring; now a GENERATED value — binary-searched between ladder rungs, the `--accent-label` precedent — and widened to the field family as `--field-edge` one tier down; see the 2026-08-07 paragraph below); **`readOnly` refused** (D5 — researched, not designed: the platform has no read-only checkbox, Material and Ant never shipped one, so the prop joins the closed edges rather than getting an appearance); **a hosted mark stays square** (D4 — the hosted rule's `height` beat `block-size`, 20 x 24 in a field's slot, and the mark kept its own expander inside the container; both axes now one expression, the expander scoped out of slots); **disabled reaches the resting fill** (D6 — the seal is not a tone role, so the arm could not grey an unchecked box); nativeButton refused, the ref honestly a span's, the label guidance made real and mounted (D10-12); the corner-spread laws cover coarse (D7 — the fine-pinned denominator hid a 1.3846 spread on the phone's default path; ceiling restated at 1.4 with its reasoning, eye-pass flagged); the per-level corner bands replace the one-band claim (D13); and the stylesheet laws walk the directory (D14 — checkbox.css had shipped outside a three-file literal claiming "all of them").

**The audit's lesson is the 2026-08-03 lesson, reproduced on the first component shipped after it was written — by the same author, in the same session that quoted it.** Nine of the fourteen sat on a law reading a DECLARED value, a token name, or a reconstruction of the author's own arithmetic: targetBox() re-derived the intended inset from the same inputs the buggy formula used; the focus law asserted `!== "auto"`, which `none` satisfies; the disabled law read opacity and cursor, neither of which could be wrong. And one law of the fix batch was itself caught measuring nothing before it was trusted: the overlap scan ran off-viewport, elementFromPoint answered null, and 24 cells passed — a row nobody claims is now an error, not a zero. The bar stands: read the browser's resolved output, then try to make the law fail.

**An external rule-set audit ran 2026-08-06 (Vercel's react-best-practices, all 70 rules over the repo), and the runtime came back nearly clean.** Five findings, fixed same day: Theme's `resolved` memo depended on the parent context's *identity* rather than the six fields it reads, so a nested Theme re-rendered its subtree on ancestor changes it overrides; `useWindowClass` allocated fresh MediaQueryLists on every snapshot read and subscribe (now one shared trio per document, lazy so SSR never allocates); the spinner's spoke elements and the resolver's digit regex hoisted to module level; and **the Spinner grew a `<span>` wrapper — two elements now (§8, LOG)**, because an SVG root's transform is not reliably composited and a busy indicator that freezes when the main thread blocks fails its one job — the wrapper owns the icon box, animation, `fill` and ref; the ref retyped to `HTMLSpanElement` at zero cost while nothing is published. Found on the way: the preview's standalone spinners still set the pre-rename `--kui-icon` and had been rendering at the size-2 fallback since the stem rename. Everything else passed by mechanism, not luck — no JS at interaction time means most re-render rules have nothing to bite on, and the barrel is tree-shakeable by construction (unbundled output, `sideEffects`, deep Base UI imports, zero module-level mutable state). Baseline 18,137 gzipped (+15, the svg-fill rule).

**The architecture sweep landed 2026-08-06 (seven fixes, an external deep-module review run against LOG's own record).** The pattern the review found was consistent — the right mechanism existing in one file and never promoted — and the fixes are promotions, not inventions. `system/axes.ts` owns the axis vocabulary (Size/Tone/Emphasis/Material, now with value lists, plus SlotName; six components, index.ts and the shared layer's laws had all been importing it from button.tsx, against §12's direction). `test/stylesheets.ts` owns the node-law mechanisms — the directory walk, the comment strip, and tokens.test.ts's LOUD parser as the only parser: ~20 `slice(indexOf)` sites in recipes/surfaces laws could go green on a renamed selector (`indexOf` → −1 → one-char slice → every negative assertion passes); `block()`/`from()` throw instead, mutation-checked. The control contract is WRITTEN (ENGINEERING §2.1) and its missing second-value laws walk the directory: fill-triple per rule, no painted-name mentions, entry-imports both directions, the data-slot union, Base UI pre-bundle entries — so Radio and Switch inherit the checkbox audit's lessons the day their stylesheets exist. `tokens/emit.ts` owns the declaration format (byte-identical artifact, drift check untouched) and unlocked the dangling-var law — tokens.css is closed over its own vocabulary, which no law had checked (a typo'd ROLES entry shipped a var() resolving to nothing). The judged numbers crossed the config seam (chroma mud-guard, APCA floors — now pinned by law so the single home is not a silent lever — ink mixes, ring/invalid-edge step picks), so the eye pass edits config lines, never emitters. The preview derives its lists (ROLES-exhaustive accent swap and role table, axes sizes, props.ts tier thresholds) — its ten-name swap copy had already drifted, keeping the theme's inks under a swapped brand. And the browser harness deepened (mounted/probeIn/forEachCell/teardown in test/browser.tsx): seven law files shed 290 net lines of re-grown scaffolding, four probe-placement comments collapsed into one, with the load-bearing divergences kept and stated.

**The sweep's remainder closed the same night (Kushagra's calls collected, then executed).** The alpha ramp composites over the SEAL (decided: an alpha fill's home is a sealed surface) — `alphaBackdrop` reads config's `surfaceColor` in both modes, dark re-solves against the real neutral-2 (the faintly pink grey-overlay hexes were the symptom), and the recomposition law derives the seal independently from config, ending its tautology; budget re-recorded 18,137 → 18,129. The config boundary is declared in both headers (color-config = what the OKLCH generator consumes), the stale `touchTargetMin` comment stops denying its consumer, and one `Size` union across three ladders is recorded as deliberate. Three new laws: material-ladder monotonicity in every lever (was config prose), the test-file walk — a stylesheet-bearing component must ship a browser test file, and Spinner's mounted laws land with it (it had CSS and zero laws) — and the total `:is()` ban in component sheets (defect (b) made structural; ENGINEERING §2.1 has no marked-unenforced clause left). The suite's last hand-kept copies collapsed (one viewport home, one scope-reader, `vitest/browser` imports silencing the deprecation), and the preview's ramp annotation now reads the rendered text, so a band cannot make it lie. 639 laws; **left open on purpose**: the Box shrink-wrap collapse (Kushagra: not now — decide against a real break, not on principle; **the break arrived and it closed 2026-08-08**, see above) and the preview's hand-stamped scope toggles (fenced by the no-JSX-runner constraint).

**The look axis shipped 2026-08-06 (§19) — one Theme prop then, TWO since 2026-08-10 (see below), and fields left the elevated world the same day (for one day; see 2026-08-07).** `outlined | filled`: the resting dress of the one-look families, the border-question's app-identity half. The criterion that settled two days of scrutiny: a border's JOB decides its owner — on a control it is RANK (Button's `bordered` is the emphasis half-step, quiet < quiet+border < medium, and survives as a prop; the preview matrix is the proof) and on a one-look family it is DRESS. Resolution is per family (the emphasis sentence reused): surface fill+edge, field fill only (its states are border and ring), mark fill trio+edge; membership IS role consumption (`--look-<family>-<slot>`, tokens.css the one home, law-pinned), Button byte-identical across looks per rung by mounted negative law. **The mark family's wiring landed in the shared layer, one rule for all three members** — it had been written against `.kui-checkbox` the day before the promotion, and rebasing it onto `.kui-mark` is the promotion paying its way: Radio and the slider thumb answer the axis without a line of their own, and a law pins all three to the same computed pair. The slider TRACK is deliberately outside the axis: it is already an edgeless well (`--color-track`), so there is no border-versus-fill trade to make. `outlined` is the identity end-to-end (sheet reads role → outlined maps role to old value → mounted ≡ bare render); `filled` darkens by hierarchy — surface neutral-2, field 3, mark 4, steps walking upward, v0 for the eye pass; accent tint deferred by the tone-set rule as a future VALUE. Two scars: outlined borders are `initial` + element-side fallback (the material edge's pattern) because `var(--tone-border)` baked at the Theme scope — §6's substitution-at-declaration, violated by its own author, caught by the mounted laws pre-browser; and the painted-variable law pushed the field-family edge rule into the shared layer (`.kui-field, .kui-textarea`, named members until the third promotes). State outranks dress (invalid/disabled arms stand the field role down; marks win on specificity); material outranks look on glass. **Elevation's membership got its criterion the same day** (§5: elevation dresses boxes that establish a plane of their own — a field is a WELL, content of a plane, not a plane above one; no platform shades an input): fields left `depth="elevated"` — **reversed 2026-08-07 at CONTROL scale** (see the elevation paragraph below): what this criterion rejected was fields lifting like CARDS, and control-scale depth did not exist yet. Only Checkbox's negative law survives; the chrome consumer count lives in DECISIONS §5 and is deliberately not restated here — it was written as four in this sentence and as five one paragraph down on the day Switch shipped the fifth (audit 2026-08-08), which is the second time this number drifted between two homes.

**Radio and Slider shipped 2026-08-06 (repetition's third and fourth entries), and the mark family promoted into the shared layer on its third member** — the slider thumb, landing with Radio, is what fired TextArea's promotion rule: the box, the invisible target, the hosted floor, the seal-and-edge resting identity, the disabled fill arm, the binary ON state and the glyph rule now live in `system/recipes.css` as `.kui-mark` rules (checkbox.css keeps the tri-state's glyph picks; the mounted checkbox laws needed not one edit, which is the promotion proving it changed nothing). A structural law forbids any component stylesheet from sizing a mark's box or re-pointing the mark edge. **The circle is role semantics and breaches the kill switch on purpose (§6):** a square radio reads as a checkbox — the checkbox-at-`full` sentence mirrored — so a radio (circle, `calc(mark / 2)`) and a slider thumb (also a circle — the 2026-08-07 horizontal capsule and its `--slider-thumb-w-N` ladder were reverted 2026-08-08, too wide by Kushagra's eye) hold their shapes at every radius level, `none` included, and those two corners were the ONLY two the radius axis never reached until Switch widened the set to FOUR (see the Switch paragraph below; the count is stated there and not restated here) — the rule held at two since 2026-08-07, when the slider RAIL turned out to be a silent third and was fixed rather than argued for (audit R8: its cap was `calc(track / 2)`, raw px with no palette token, so `none` left it a capsule; now `min()` against the control radius, which squares at `none` and is byte-identical everywhere else). This sentence said "ONLY two" for a day after the set became four, in the same file that said four one paragraph later — audit 2026-08-08, the same shape as the box-shadow count and fixed the same way: one home, and a pointer from everywhere else. **Radio** brought almost nothing else of its own: the group is Base UI's `RadioGroup` wrapped with zero CSS (`render` open so the group can BE a Stack; `readOnly` refused on both, the checkbox's refusal inherited). **Slider** is the first value control, and its target needed NO mechanism: the ROOT is the control — it rides the height ladder, the whole strip is pressable, so the 44 coarse default and 24 floor arrive free — and the thumb's own expander is scoped out, which generalised the slot exclusion into the real rule (a mark inside another control never grows its own target, `:not(.kui-control *)`). Two things were minted: **`--color-track`** (the low neutral well — §11's "track low" needs neutral through a role because the element stamps accent; switch off-track and progress/meter inherit it; deliberately subtler than the mark edge) and **`--slider-track-N`** (4/5/6/7 raw designed px, ~0.25 of the fine mark — the palette has nothing between 4 and 8, the third family to hit that wall; density- and pointer-invariant, iOS's own posture). Fixed identities: `thumbAlignment="edge"`, horizontal only (`orientation` refused — vertical ships as its own designed set when something forces it). Range sliders are the same component (array value, one thumb per entry). 71 mounted laws; the interaction laws learned the settled-frame lesson (Base UI's edge alignment hides the input until first layout — a law that focused it early would have skipped itself, the scan-law vacuity one component over). Budget 18,137 → 18,314 gzipped: **+177 for BOTH controls plus the minted role, the track ladder and the promotion** — the additivity curve's strongest point yet (1,206 → 247 → 91 → 311 → 177÷2), because the promotion meant Radio's own stylesheet is one declaration.

**apps/docs stood up 2026-08-05 — bare Next.js, every visible pixel `@kookie-ui/react` (no docs framework: Mintlify/Fumadocs refused on the stance that a design system whose docs run on someone else's UI argues against itself — Kushagra, LOG), and dark SSR closed where REVIEW said it came due.** A pre-paint inline script stamps `data-appearance` + `data-contrast` on `<html>` (both, because the generated high-contrast selectors need them co-located on one element) and the root Theme runs `appearance="inherit"` — one source of truth, no flash, hydration-safe via `useSyncExternalStore` server snapshots. `/matrix` is the AXIS-grid judging surface: size × density per pointer world with radius/pointer/depth/contrast switchable in place, the tone × emphasis board, the type ramp. **`/preview` — "the playground" — joined it 2026-08-08 and is the surface the eye pass actually runs on**: one long page, every shipped component alphabetical, dense specimen tables (rungs × families, sizes × states, the ten-tone sweep) with materials judged over a real backdrop, and an environment panel holding every Theme axis that sits INSIDE the Theme it configures — flip density and its own chips tighten. The division is that `/matrix` answers "is this cell right" and `/preview` answers "does this component look right beside the others". It was named in shipped CSS comments, in LOG and in this file as the place two Switch reversals were judged, one commit BEFORE it existed and with no doc describing it (audit 2026-08-08 — doc-code drift in the direction the rule does not usually catch: the code cited a surface the docs had never introduced). Being the first real consumer earned two findings in the first hour: a link-as-button wore the UA underline (fixed in the control skeleton, computed-value law), and a Box as a row-flex item collapses to width 0 — the SAME defect Checkbox's preview found the same day, independently, which is why DECISIONS carries one entry with both discoveries and both sets of escapes folded in.

**Audited 2026-08-06 (multi-agent, adversarially verified) — the docs slice; 27 confirmed, 5 refuted, and the fixes shipped.** Two criticals, both a half-guarded mechanism rather than a missing one. **`pnpm dev` was broken on every route, and it was a PACKAGE defect**: an element created in a Server Component crosses the RSC boundary as a *lazy node* (props undefined), and `composeRender` dereferenced it blind — dev-only (facebook/react#32392), so `next build` was clean and the command a human types was the one nobody typed; fixed via the public `React.Children.toArray`, as Base UI has done all along. And **a storage-denied browser got a blank site**: the store's readers are `useSyncExternalStore`'s snapshot, so they run during render — the pre-paint script had guarded the identical reads, so the half that fails safely was guarded and the half that kills the page was not. Guarding was still not enough (`apply()` re-derives from storage, so an unpersisted write was invisible to the next read and the toggle stayed dead); the session's truth is now memory, storage is persistence. Fixed with them: the missing `not-found.tsx` (Next's fallback injects a `prefers-color-scheme` body style AND is third-party UI on a route we serve — stance and bug, one fix), the type ramp sitting outside every pointer Theme, `next dev` writing an unauthored `apps/docs/CLAUDE.md`, and ENGINEERING.md still calling dark SSR open. **The rule grew two clauses (LOG): a mechanism with two implementations owes a law that they AGREE, and a new app owes a place for its laws before it owes anything else** — apps/docs had no harness, which is why both criticals shipped green; writing the law caught the storage fix being incomplete. Five confirmed findings are deliberately unfixed in DECISIONS' open list, each a system decision (Button-as-link's role, TextField's min-width, the card-as-link box, the surface margin, how selection state is spelled).

**The look axis's value layer settled 2026-08-07, and it ended in a standing rule about who governs a resting colour.** The day's chain: an ultracode audit of the look changes (seventeen raised; fixes shipped, five recorded as blind-spot laws); filled re-centred on its fill with per-appearance `--dress-*` roles ("an index is not a colour", the mode-blind-step bug's third instance); Button's RSC lazy-node unwrap (`pnpm dev` crashed on every route — dev-only, facebook/react#32392); the slider leaving the look axis WHOLE (§19's membership test grew its second half: the axis dresses things whose resting state is a surface with a boundary — a slider is an instrument, and Progress will follow); the rail squaring at `radius="none"` (audit R8); forced-colors declined as premature; the control edge SOLVED per tier (`--control-edge` / `--field-edge`, marks fine-detail and fields one tier down by Kushagra's eye, high-contrast a designed tier up); and filled's wells staying soft with `contrast="high"` as the measured accommodation. **The standing rule (Kushagra, verbatim: "APCA rule checks for high contrast mode, taste over APCA rules in standard" — scope: borders and fills):** a resting border or fill is DRESS, stated in config and held to no floor — the two rulers had split on one hex (the solved light ring clears APCA 46 at 2.44:1 WCAG) and filled's picked steps sat beside outlined's solved ones with no principle to arbitrate; `contrast="high"` is the CONFORMANCE surface, its edge targets the ones law-anchored to `apcaFloors`, with the look-border stand-down routing filled through the same solved edges. Text, the focus ring and the invalid edge stay floor-checked in standard mode — signals, not dress. The second clause, same day: taste stays contrast-informed — the suite prints a per-run report of every resting border and fill against its advisory tier on BOTH meters (APCA and WCAG 2, which disagree exactly where the palette works), asserting only that the measurement happened. Still open from the day: the slider thumb ignores `invalid`/`disabled` (the mark's own `--tone-border` declaration beats the inherited state colour), and slider HC-deafness under outlined at rest. Budget 18,870 gzipped; 802 tests.

**The elevation work was audited 2026-08-07 (ultracode, adversarially verified) and the five defects closed the same night.** Twenty-six raised, nineteen survived refutation, deduping to five — and three of them were one rule twice over: **a fallback chain is only a mechanism if every arm of it is reachable.** The pane's transmitted cast was written onto `--kui-surface-chrome` itself, the name the world declares on the Theme element and every surface reads by INHERITANCE, so a plain opaque card inside a glass card cast a third of its shadow (dark also lost the rim-light); the fix is a second non-inheriting name consulted first (`--kui-sf-cast`) — the shape the control layer had arrived at the day before, never promoted — and NOT registering the world name non-inheriting, which kills elevation outright. The disabled arm stood down the second item in the control chain while glass resolves through the first, so a dead glass button computed a shadow byte-identical to its live self in 12 of 16 cells; the stand-down names the three per-thickness rows because the `:has()` arm (a field whose own input is disabled) ties with the glass rules and loses on source order. And the lifted glint re-declared a GENERATED name — a one-way door, so `flat` could not restore it and a nested flat Theme kept the brighter line wherever appearance was inherited (apps/docs' own root); it rides a `--kui-` pointer now, with the generated name as a second fallback for the un-themed document, which the pointer alone broke within the hour. Plus the disabled slider handle, which read the palette VALUE to cast always and thereby walked out of the shared disabled arm — "always" is about the world, never about state. **The transmission law could not fail**: it rebuilt its expected value with the generator's own regex, character for character, so respelling a row as `rgba(0, 0, 0, 0.1)` makes the fade a silent no-op and the law agrees with it — demonstrated, not argued. It parses alphas independently now and asserts the fade did something, the arithmetic, and the ordering. Ten new mounted laws, every one falsified first; **one was thrown away for being unfalsifiable** (a sealed pane inside a glass pane, which cannot exist — reduced transparency seals both). The doc sweep that followed closed what the shadow renumber had left saying the opposite of the code: the tuning comments pointing at the card row after it became the control row, both field stylesheets still stating as a decision that a field never casts, the consumer count written three different ways in one section while the law asserted four, and the slider handle still called a circle. 834 tests; budget 19,737 gzipped.

**Switch shipped 2026-08-08 — repetition's fifth entry, the mark family's SHIFTED member.** The track is mark(n + 1) stated in the shared size join (`--kui-ct-mark-up` + one `.kui-control.kui-switch` re-point winning the tie on source order — two laws forbid the per-size spelling anywhere else), and `--mark-5` now exists to hold size 4 (the line box of type step 5, same sentence as the other four). **The width closed as ONE designed ladder, not eight numbers**: `switchW` is indexed by the mark the track IS (steps 2-5: 34/40/44/48), and both pointer worlds price their cells through the same band picks that price the marks — coarse widens one entry for the same reason it rises one step, every cell holds 1.67-1.71 of its track (iOS 1.65, Material 1.63, Radix 1.75), and no fraction ever enters the chain. The thumb is the track minus the designed 2px inset (thumb = mark(n) refused: the band's 4/5 collapse drives that inset to zero at coarse size 4), its diameter DERIVING in CSS from the channel (block insets + aspect-ratio), so the hosted floor shrinks it instead of overflowing. §6's corner exceptions widened to FOUR, argued: the switch thumb is the slider's grip one control over (circle), and a circle only nests in a curve (capsule track) — the rail's `min()` escape does not transfer, half a 20-28px track dwarfs the mid-level control radii. **The whole control left the look axis with the slider** (§19: off is the `--color-track` WELL with the edge melted into it — a channel felt for, not a surface read; the well pairing was decided the day Slider minted the role). The thumb inherits the grip's exceptions with the role: `--color-thumb`, casts ALWAYS (fifth box-shadow consumer, count law moved), disabled stand-down stated on its own element because it is not a `.kui-mark` — the slider-handle audit hole closed before shipping this time. The coarse size-4 wrinkle is law-asserted as an EQUALITY (switch stands level with its checkbox there), not skipped. 41 mounted laws, three falsified against sabotaged CSS first; two law-side defects caught and recorded (50% computes as a percentage; a half-width grip's LEFT edge cannot cross the midline — its center does). 882 tests; budget 19,737 → 19,937 gzipped (+200 for the component, the width ladder, `--mark-5` and the join's shifted entries); width, inset and well pairing v0 for the eye pass. Same-day correction, caught in the playground: the disabled arm's first spelling melted the grip into its own channel — the thumb is the tick, so disabled stands down the CAST alone and the grip keeps `--color-thumb` (LOG).

**Separator shipped 2026-08-08 — repetition's sixth entry, +63 bytes gzipped, built ahead of Progress as the cheapest component left.** Base UI's `role="separator"` div wearing `--color-border` at `--border-width` — §11's row was already fully designed. `orientation` is the one prop (Slider's refusal doesn't bind: it was priced on vertical needing a designed geometry set; a vertical hairline swaps two axes at zero designed values); `children` refused; Radix's `decorative` omitted (a rule hiding from AT is a styled Box). Extent is the container's — no length prop, `flex: none` so a squeezed Stack never thins it. The quiet-hairline identity is law-asserted negatively (paint ≠ `--control-edge`, both appearances), the first component whose whole identity is §7's edge-order sentence. Budget 19,937 → 20,000 gzipped; 891 tests.

**Progress shipped 2026-08-08 — repetition's seventh entry, +115 bytes gzipped, and the first component whose headline decision is an ABSENCE.** §19 predicted it twice ("a bar is a rail with no grip") and every prediction held by derivation: well = `--color-track` (the role's second consumer, minted for exactly this pair), fill = `--tone-solid` under a stamped accent identity, outside the look axis by the instrument rule, and NO target mechanism — the one §16 consumer that grows none, because a bar answers no pointer and takes no focus, so the box is the paint. Two elements, not three: Base UI's `Progress.Track` is skipped because the root is already the positioned parent, and `Label`/`Value` fail the anatomy criterion (a label is `aria-label` or a `<Text>`; a formatted value is a `<Text>`). **It ships with no `size`, and the ladder was asked first as the rule requires**: `--slider-track-N` holds ~0.25 of the fine MARK, and a bar has no mark — riding it would size the bar against a box the component does not have, which is §6's checkbox-corner sentence verbatim and **the fraction bug's fifth instance, the first one caught before shipping** (reached by inheritance this time — take the neighbour's ladder — rather than by arithmetic). Separator's shape instead: one designed thickness (`--progress-track` = 6, v0 — one step over the default rail's 5 because a rail has a grip to lend it presence, still inside the ladder's 4-7 range so bar and rail read as one kind of line), extent from the container, no index. Withholding is the ADDITIVE direction, which is what made the call makeable without a taste pass; the counter-argument is recorded open, unrefuted (`--mark-N` *is* `--line-height-N`, so the ladder is equally "a quarter of a line of text" — and granting the index means a RENAME, `--slider-track-N` → `--track-N`, one ladder two consumers, the mark family's own promotion). Not a `.kui-control`: the skeleton is the interactive one, and opting in would mean overriding most of what opting in declares plus a 44px box on the coarse path around a 6px line. **Indeterminate ships and does not touch §8's motion laws** — those govern the easing of a state CHANGE; a sweeping segment is motion that IS the content, the Spinner's category, so it takes the Spinner's answer (slowed, never stopped) and reads neither motion token. It kept that exemption when the control layer started moving. `tone` left closed and recorded open (a failed upload in `destructive` is the six-tone widening's own vocabulary; Slider's refusal was priced on EMPHASIS and does not transfer). 26 mounted laws, every one falsified against sabotaged CSS, including two vacuity guards the Slider round taught. 919 tests; budget 20,000 → 20,115 gzipped.

**Code and Kbd shipped 2026-08-08 — repetition's eighth and ninth entries, +75 bytes gzipped FOR BOTH** (the additivity curve's strongest point: almost everything they look like was already written). Type-family atoms, not controls: the mono slot joins `--font-body`/`--font-heading` as the type layer's third family, and the ramp, weights, emphasis rungs and margin reset all arrive from `system/type.css` with no rule of their own. **§11's row said `emphasis: medium` and implementing it would have been a bug** — `medium` there names a FILL rung from the control resolution, so a chip would have climbed the ladder with its fill while its ink read the same axis as foreground roles (one axis, two resolutions, one element — §9's reason for deleting `variant`). The row is corrected: these take the TYPE resolution, and "subtle fill" becomes a fixed identity, Card's sentence at atom scale. Tone reaches BOTH colours (an atom carrying a fill has a second thing to tint), law-read in both, because "ink went red, box stayed grey" is the half-fix that ships. **`size` is optional with no default** — the only component that does this: Text anchors at 3, and a Code inheriting that anchor would push a size-1 caption's literal to 16px and silently stop being usable in small text; unset means the element states no step and takes the line it sits in. **The first non-token length in the package is here** — `em` padding, on the mark family's own argument (a mark is one line of its label because its box is a property of the type beside it; a chip's breathing room is a property of the glyphs inside it), with both alternatives already scarred in this repo. **Kbd left this identity on 2026-08-08, the day after it shipped** — a mono cell draws ⌘ compact to fit its fixed advance, and the platform sets shortcuts in the UI sans, so the cap wears the BODY family at its own `kbdScale`, in a box exactly one line tall (`block-size: 1lh` — the face, and the reason it never spreads its line; a constant-em face grew a step-9 line by 13px under a bound that only held in the sizes the law looped, audit 2026-08-08). It also CASTS ALWAYS, the grips' role-semantics exception (sixth box-shadow consumer, counted by law) — the day-one no-shadow refusal reversed on sight. What survives of the shared identity: the fill, the tone indirection, the type join and `--tone-border`. Its sheet repeats Code's facts self-keyed because the SECOND member self-keys and the third promotes — Badge and Tag are next. **Found and fixed on the way: `docs:test` was a cache hit that could not fail** — the playground law parses `packages/ui/src/index.ts`, turbo's `test` task had no edge to the package, and a full `pnpm run test` reported 43 docs tests green while two new exports had no section at all; `dependsOn: ["^test"]` closes it, and ENGINEERING gains the clause (**a law that reads across a package boundary owes a build edge across the same boundary** — "did not run" is a way of not failing). 949 tests, 45 docs tests; budget 20,115 → 20,190 gzipped.

**Blockquote shipped 2026-08-08 — repetition's tenth entry, +27 bytes gzipped, the type family's third component and cheapest.** Two declarations; everything about how it READS is the shared layer's (ramp, weights, emphasis rungs, zeroed margin, and it joins the body family slot in type.css rather than declaring one). **Its one open decision was whether a chosen tone tints the RULE, and the answer is no** — §11's rule for the family is that tone re-scopes the ink trio, held rather than quietly widened, so a `destructive` quote is red words beside a neutral rule; §7's edge order puts a quote's rule exactly where a separator's sits (a law asserts the two resolve one colour). **The spelling was the more dangerous half:** `var(--tone-border, var(--color-border))` reads accommodating and is unsafe, because custom properties inherit — an unstamped quote inside any stamped ancestor would silently take that ancestor's family, the `--kui-h` shape again. Flat `--color-border`, plus a mounted law that nests an unstamped quote in a `destructive` ancestor; the sabotage run confirms that law is the one that fails when the fallback form goes back. A quote whose BAR carries meaning is a Callout, already on §11's list. Anchors at size 3 like Text rather than inheriting its line like Code (a quote is a block); indent is `em`, which makes the atoms' argument a family pattern. `<blockquote>` also happens to be the element arriving with the most UA margin in the set, so the outer-spacing non-negotiable finally gets tested where it bites. 965 tests, 46 docs tests; budget 20,190 → 20,217 gzipped.

**Audited 2026-08-08 (self-adversarial, every finding measured in a mounted browser and every fix falsified first) — five findings, two repairs, three recorded.** **The one real defect was in the newest file and it was mine:** Progress declared `flex: none`, borrowed from Separator, which holds the block axis and also freezes the inline one — against `inline-size: 100%` that meant 381px of bar inside a 300px flex row, overflowing by 81 beside an ordinary Button. Fixed as `min-block-size`. **The law that let it through was one-sided** — it asserted the column and never the row, so it tested the axis that was right; now two laws that the sabotage run shows catching opposite mistakes (**a law about one axis of a two-axis mechanism is half a law**). **A superseded law survived its own replacement:** checkbox carried BOTH the D9 ring law and the weak `not.toBe("auto")` spelling D9 was written to replace, on the same element, with a comment claiming a `:focus-visible` limitation the probe disproves (programmatic focus does qualify; the ring computes solid at 2px) — deleted. **The loud-extraction lesson reached the browser project** as `within()`: four law files carried `querySelector(".kui-x") ?? el` whose query arm has NEVER matched (the root carries the class, querySelector searches descendants), documenting an anatomy the components do not have — reported honestly as hygiene, not a hidden bug, since a plain rename fails anyway through the stylesheet. **Recorded not fixed:** `contrast="high"` reaches nothing on a bar (correct by the 2026-08-07 rule — fills are dress — and measured at 3.06 light / 4.13 dark, identical to the shipped Slider and above the 3:1 non-text floor) — **SUPERSEDED, re-measured 2026-08-26: the WELL moves and the fill does not**, because the high-contrast scopes re-declare `--color-track` from `--neutral-a4` to the opaque `--neutral-6` in both appearances (reaching bar, rail and switch off-track together) while `--tone-solid` is not re-declared; the open system question is unchanged, and neither component has a law reading the well across contrasts; a Separator in a wrong-axis container computes 1×0, unfixed on §16's own "CSS cannot read the container" argument; and the switch's width ladder holds its 1.67–1.71 claim (measured 1.667–1.714, coarse size-4 collapse exactly where §4 records it). **Two of my own measurements were wrong before they were right and both failed silently** — canvas `fillStyle` returns black for `color(display-p3 …)`, and `/[\d.]+/` takes the *3 in display-p3* as the red channel: an instrument must be calibrated against a known answer before its output is evidence.

**The component reference shipped 2026-08-08 at apps/docs `/components` + `/components/[slug]`** — ONE renderer over a data file (`registry.tsx`), ENGINEERING §1.1 applied to the docs themselves, so adding a component is adding a row rather than writing a page. **The `refusals` section is why the route exists:** a generated API table can only show what a component HAS, and cannot tell "no `variant` because we deleted it, and the deletion is load-bearing" from "no `variant` because nobody got to it" — which is most of what this system is. A coverage law walks the package's exports against the registry (the playground law's sentence one route over, and a DIFFERENT failure: that one says every export is RENDERED, this says every export is EXPLAINED — eleven were visible and undocumented), checks the reverse direction, and carries two anti-rot clauses because the cheapest way to satisfy a coverage law is an entry that says nothing: a blurb must be a real sentence and a refusal list cannot be empty. All falsified against a sabotaged registry. **`tsc` caught a factual error in the prose** — the Theme entry documented an `accentColor` prop that has never existed (accent is hue-authored in config and baked by the generator, so it is one app-wide identity, not a per-subtree choice); a markdown docs site would have shipped that sentence, this one failed the build, and the absence is now a recorded refusal. Verified in `pnpm dev` and screenshotted, not just built — that command is the one this repo's history says nobody runs.


**Portal groundwork shipped 2026-08-09 (§20), ahead of Menu.** Two rules, both measured: portalled content re-themes (a bare `<Theme>` inside the portal — context crosses, attributes don't; law-pinned identical to an in-flow twin under hostile axes, `contrast="high"`, html-stamped inherit), and the DOM-outermost `.kui-theme` is the stacking frame (`isolation: isolate` via `:not(.kui-theme *)` — app z-indexes stay inside, portals paint above by DOM order, no number ladder). Rejected: MUI/Mantine/shadcn z-index ladders, portal-into-theme (clipping), Radix's relative+z-index spelling (anchor side effect), a React root sentinel. The spelling is triple-pinned: node law (isolation only, no backdrop-root/containing-block breakers on any theme rule), the portal browser laws (all falsified — deleting the rule re-shows the covered portal, un-wrapping re-shows the white card), and a build assertion proving the rule survives Lightning's minifier. One constraint: the root Theme must never render onto `<body>` (portals would land inside it; dev warning + JSDoc). Menu is next and inherits the pattern plus the ENGINEERING clause naming its owed agreement law.


**Menu shipped 2026-08-09 (§21, §22) — the first floating component, on the row family declared the same day.** Twelve flat exports in shadcn/ui's dropdown-menu vocabulary (MIT, credited): `MenuContent` folds Portal → bare Theme (§20's first consumer — its agreement law un-marked the ENGINEERING clause) → Positioner → Popup; the popup wears Card's stamped identity + `kui-floating`, a CONCENTRIC corner — row corner + panel padding, derived per (size × radius level) by the floating size join in surfaces.css through the `--kui-floating-p` hook, with `none` guarded to mean none (the third spelling in one day, all judged by Kushagra's eye: the overlay band was dialog-priced, and the fixed surface-1 that replaced it matched only where the sums coincided; §6's overlay stays unclaimed until Dialog), one designed padding (`--floating-p`, renamed to the family when Select shipped), and `max(--menu-min-w, --anchor-width)`. **The row family (§21)**: `kui-control kui-row` riding the existing control cells — except the height: a row's box is its text line + one designed `rowInset` per (density × pointer) cell (2026-08-09, the sparse-menu judgment; fine default 24/30/34/38 (size 2 softened by eye), coarse 36/40/44/48, each a notch under its button — the shadcn/macOS relationship, no invented floor; per-size same day: constant air read cramped at size 4) — emphasis refused (quiet stamped), lit by `data-highlighted` never `:hover` (a stand-down inside the one guard), checked = the accent SOLID on the indicator glyph (reversed 2026-08-09 from whole-row `--accent-label` ink — a text ink reads as emphasis, a tick owes only the non-text floor; the `:not([data-disabled])` stand-down survives the move), destructive via the tone indirection at zero CSS. **The floating chrome**: third chrome role, declared by BOTH worlds — elevated reads shadow row 5, the palette's top (row 4 read too low by eye, 2026-08-09; row 3 since the 2026-08-16 depth port), flat the same row generator-faded under the overlap amendment ("coverage is information, not expression") — RETIRED 2026-08-19: flat's value is the no-op layer, nothing casts in flat including popups, because the flat hairline now draws a covering pane's boundary (LOG) — re-pointing the surface layer's one cast site so the box-shadow count held at six. The registry learned `parts` (the one law amendment, argued in LOG). Size on the root like Button (Kushagra's reversal); glass opt-in; motion instant on the day it shipped and the motion system's FIRST consumer the day after (the panel unfurls out of a seed and dissolves — see the motion paragraph below). 17 mounted laws including the §20 agreement law, four sabotage passes; two test-instrument bugs caught by calibration (the stale-popup selector, twice). Judged live in light/dark/glass at /preview. 1,032 package tests; budget re-recorded. **Audited 2026-08-09 (ultracode, 47 agents, every finding measured in a mounted browser and adversarially verified) and the findings are fixed — 38 raised, 35 survived, deduping to fourteen repairs.** The pattern was ONE shape three times: a mechanism whose argument was written for a top-level element and then applied to a nested one. The width floor means "never narrower than the trigger you pressed", and a submenu's anchor is its trigger ROW at `inline-size: 100%` of the parent panel, so it compounded 446.59 → 437 → 427 across three levels; the portal wrapper re-stamped the axes a React Theme chose, and on the un-themed path it could not tell "nobody chose" from "someone chose light", opening a white menu in a dark document; and the stacking frame contains the z-indexes that are its DESCENDANTS, so any positioned ancestor at `z-index: 1+` paints app content over every popup — §20's "the ONE placement the frame cannot survive" was two. **RTL was broken three ways and nothing in the repo touched direction**: `dir` is a platform attribute the wrapper had nothing to re-stamp, Base UI positions from a context whose only setter this repo never rendered, and the chevron was a fixed rightward path. One measurement off the trigger — the only in-flow node a menu owns — answers all three. Also closed: the trigger re-shipped Button's 2026-08-03 `type="button"`-on-an-anchor defect (the fix needs a DEEPER check than Button's, because `render={<Button/>}` is a component, not an element); `MenuLabel` outside a group threw on OPEN and wiped the React root, which is shadcn's own canonical shape; the popup's `overflow-y: auto` clipped a focused row's ring at the padding box, invisible at compact — and the "but the highlight fill is a visible indicator" defence that downgraded it measured 1.16:1 light and 1.08:1 dark, with `contrast="high"` moving it by nothing, so the lit row joins the SIGNAL side of the 2026-08-07 split and goes solid under the conformance surface (20.6:1 / 17.5:1); the submenu's seam was two JS constants against a density-dependent padding, aligned in NONE of the six cells; `--available-width` was never consumed; a separator inside a group lost its bleed; and the portal wrapper was a query container, re-targeting every tier at the viewport. **Five laws could not fail** — including the §20 agreement law itself, which reads twelve cascade-delivered properties the wrapper re-stamps by construction, so a dropped stamp was its only reachable failure. Every fix is falsified against the pre-fix code, and one NEW law shipped vacuous and was caught by its own sabotage pass: it selected the subject by index, and the in-flow twin precedes the portal in document order, so it compared the twin with itself. 1,069 package tests (was 1,032); 37 mounted menu laws.

**Select shipped 2026-08-09 (§23) — the floating family's second member, and the proof the family generalises: zero new visual design.** Six flat exports (shadcn vocabulary, credited); the trigger is a FIELD-shaped button — it wears `kui-field`, so the seal, edge, look, material and state arms arrive by membership (the third field-shaped control promoting the family, honored by wearing the class), with the two caret facts stood back down at (0,2,0) and the skeleton's `:focus-visible` ring applying because the field's focus-mode ring is input-keyed and inert. The value machinery: Base UI's hidden input (a Select submits with forms), `items` for the closed trigger (Base UI resolves labels from MOUNTED options — measured showing "b" for Beta without it), placeholder in the faint role (muted since 2026-08-10), chevron muted and unmirrored. Positioning is the system's whole (no props; always anchored, floor = max(--floating-min-w, --anchor-width)); `alignItemWithTrigger` pinned false with the macOS overlap mode recorded open WHOLE — **reversed 2026-08-17, and it took the entry with it** (see the curtain paragraph below). **Two promotions landed on the second member:** the §20 machinery (PortalScope + ambient direction) moved to `system/floating.tsx` — a JS mechanism with laws promotes at two consumers (render.ts precedent), while CSS keeps the second-member-self-keys rule — and the panel tokens renamed to the family (`--floating-p`, `--floating-min-w`). The shared selected rule widened to `:where([data-checked], [data-selected])`. 20 mounted laws including Select's own §20 agreement law and the trigger≡TextField family agreement; sabotage pass catches PortalScope, the caret stand-down and the accent spelling; two instrument lessons recorded (Base UI's press-drag window; closed = hidden, not removed). 1,093 package tests, 120 docs tests.


**A select's panel is placed by what is inside it 2026-08-17 (§22, §23, Kushagra) — so it is placed BEFORE it is posed.** The overlap placement returns (`alignItemWithTrigger` back to Base UI's default, the macOS/Radix geometry): the chosen row lands exactly on the value it replaces and the rest of the list falls above and below it. The scroll ARROWS stay refused — wheel, trackpad and keyboard already scroll the panel, and an arrow is a control nobody has designed. **The ENTRY is unchanged** (Kushagra: *"I would expect same animation as dropdown, but only the position changes… I still expect the animation"*) — a curtain was built first, law-tested and judged, and rejected on sight; the family has one entry for anchored panels and a select is a member of it, so what moves is where the flight LANDS. **The mechanism is an ordering.** Base UI computes the overlap from the panel's real box, and the runner poses on the mount frame (§22's own rule), so the placement was computed against a box the size of the trigger — the chosen row settled 66px low. The plan now carries `placedByContent` and the entry waits for the box to hold still; nothing is lost, because the pose is transparent until aimed. **Two pose rules were re-keyed, and one was a latent defect**: the silhouette's geometry and the body's squish were keyed on `data-seed` — the VISIBILITY gate — rather than on `data-unfurling`, the flight, so both applied during the placement window and shrank the box the placement is measured from (the squish alone was 58px). **And the flight BORROWS an inline height**: an item-aligned panel is `height: 100%` of a positioner Base UI sized, and an inline declaration beats every stylesheet rule, so the block-size channel was dead — seed height written, pose matching, panel at full height for every frame of an unfurl. The runner takes the property for the entry and puts it back exactly as found; `!important` was refused (it wins the cascade and leaves the library's intent unstated). **The width step at release was a stale premise**: the entry publishes the panel's floor because floating-ui's `--anchor-width` does not exist on a first open, and `heldAnchorWidth` predicted the trigger's SCALED box on the premise that floating-ui measures anchors with transforms — Base UI 1.6 reads `getScale()` and normalises the rect instead (measured: rect 83px at scale 0.975 against `--anchor-width: 85px`; a wide trigger's panel widened 351 → 360 on the release frame). It publishes the resting box now, law-read as the AGREEMENT between the two floors, and falsified against the old prediction after the obvious sabotage (divide → multiply) proved worthless at scale 1. The curtain was DELETED rather than left behind a flag — a mechanism with no consumer is the entropy this repo keeps paying for. 45 select laws, every new one falsified.

**Its entry was moving the page before that, and closing one door moved the symptom to the other (2026-08-17).** A select is the only floating member whose open focuses something inside the panel, and the browser answers a focus by scrolling that element into view. With `overflow: hidden` the flying box is a scroll container, so the PANEL took an offset nothing settles at (measured 57px) and unwound it frame by frame as the box grew — the contents visibly sliding. Refuse it with `overflow: clip` and the browser walks one step out and takes the PAGE (65px, and it stays). Both doors are shut, in two layers: `clip` on the flying box, and the page parked for the entry's opening frames inside the scroll event itself. One law reads both per frame, falsified in both arms. An earlier note claiming a 2151px single-frame flash survived the page hold is WITHDRAWN — that came from a probe reading in the wrong order (a rAF callback runs after the frame's scroll events) and from a file mounting three subjects into one page.
**Audited 2026-08-09 the same day (ultracode, 45 agents, every finding re-measured in a mounted browser and adversarially verified) — 34 raised, 24 survived, deduping to eighteen repairs, all shipped.** The dominant shape was ONE and it recurred four times: **a rule written for one element, applied to a different one.** The trigger inherited the field family's PINNED FILL, whose stated reason ("the border and the ring carry its states") is a text input's — the ring here is keyboard-only — so it computed rest = hover = press = OPEN, byte-identical, while Base UI stamped `data-popup-open` on it and nothing read it; the look axis now publishes the field family's two missing steps (derived: outlined reuses the surface roles verbatim, filled walks the siblings' own +1/+2) and select.css stands the pin down as the THIRD caret-assuming fact beside cursor and selection. The read-only rule needs an `<input>` inside it and can never match a button, so `readOnly` shipped fully accepted and drawing nothing at all — refused now, researched: HTML states `readonly` does not apply to `<select>`, the checkbox's own refusal one family over. The menu's SEPARATOR rules were copied into a listbox, where a separator is markup an accessibility scan reports as a violation — refused, with the group as the divider a listbox actually has. And the concentric corner added half a BUTTON's height to rows that left the height ladder: `--radius-row-N` is the fraction wall's eighth instance, missing in 21 of 24 cells by up to 9px.

**Three more that reached beyond Select**: ambient direction was measured ONCE in a ref callback, and every runtime language switch sets `dir` in an effect that runs after the render that would have re-measured — so a page switched to Arabic in place opened its panel on the wrong side and never recovered, with the stale stamp OVERRIDING the direction the portal would otherwise have inherited (Menu had it too); a row was `inline-size: 100%` with nothing bounding its CONTENT, so one unbreakable address gave the panel 90px of sideways scroll and left the highlight short of the words it highlighted; and the panel's stated "never pushed off the viewport" was unreachable, because in CSS a minimum beats a maximum. Also closed: `material` on the trigger (four documents promised glass and nothing could ask for it), the hand-written trigger props that blocked `id` — what `<label for>` needs, and every other component takes it — while TypeScript's hyphenated-name exemption waved `aria-*` through undeclared, and the disabled placeholder the shared remap could not reach because the rule named a colour instead of a role.

**Four of the twenty laws could not fail, and the lesson is the 2026-08-03 lesson yet again — three of them measured the axis that was RIGHT**: the tick law read the chosen row's colour and read visibility only on an UNCHOSEN row (one word in the hiding rule hides every tick, suite green); the padding-floor law ran at the one density where the floor is a no-op; "the floor holds against a wide trigger" wrote the width onto the panel and asserted the panel was that wide. The fourth was the hostile axis set listing six of seven axes under a comment claiming all of them, while ENGINEERING named this file as enforcing `contrast="high"`. Every fix here was falsified against the pre-fix code, and **one NEW law was thrown away by its own sabotage pass**: "an open trigger looks different" opened by CLICK, which leaves the pointer resting on it, so `:hover` alone satisfied it — it opens from the keyboard now and asserts nothing is hovering. The system-wide gap the same thread surfaced is closed too: the `prefers-contrast` guard's EMITTED half had no law, so a respelled selector could kill the platform signal with the suite green.

**`full` became the default radius 2026-08-09 (§6, Kushagra) — and the flip audited the system.** Two homes moved (config `defaultRadiusLevel`, Theme DEFAULTS); the un-themed path promptly rendered the resurrected 9999px clamp because full's answer (capsule + pill) varies per (density × pointer) and had only ever been emitted into `[data-radius]` scopes. Two architectures were measured wrong the same hour — an inheriting binding baked at :root (substitution-at-declaration, §6's oldest lesson re-proven by probe), and a level-flavored family that re-created the density-clobbers-radius race — before settling on: level-blind family blocks, answers co-located per (level × density × pointer) cell + every level block, :root carrying the default's baked answer (law-pinned and falsified). The docs were the last casualty and earned an export: the preview panel and /matrix each copied the six axis defaults, so the playground kept opening at `medium` — the package now exports `themeDefaults` and both derive, with a docs law that fails on a restated axis. Eleven default-implicit laws now pin `radius="medium"` explicitly; the menu 24-cell law was comparing row corners against `--radius-control-N`, equal to `--radius-row-N` at every level but full — wrong-token-passed, geometry edition. Same day's eye-pass batch: glyph strokes unified with soft-apex carets, `--color-text-caption` minted one step under faint for panel group labels, size-2 fine rows softened to 30. **The stroke is 1.5** — it went to 1.25 in that batch and back to 1.5 one commit later (6d04dd1); this sentence said 1.25 for a day while the code said 1.5, which is the doc-code drift rule catching its own author. **It is `iconStroke` = 1.75 since 2026-08-23, and the literal is gone** — see the stroke paragraph below.

**The playground became a judging surface for COMPOSITION, and taste got a written brief (2026-08-09/10, §15).** Kushagra on the shipped confirm card: *"its not 'extensive' enough… but also, its not tastefully done… I'm not seeing real examples, what a real consumer will use."* A second `/examples` route was proposed and refused on the spot — *"preview without examples doesnt make sense"* — so `/preview` now opens with **Real screens**: a workspace shell, sign-in, a confirm card, notification settings, a deploy in flight, a plan picker, a project form and a composer, all ordinary call sites picking no colour and inventing no length. Two cross-family sections lead the specimen list out of alphabetical order (**Sizes**, every control at one index in one row; **Tones**, ten families across every consumer), because sweeping an axis ACROSS components is the permutation no single component's table can hold. The playground law grew its anti-hollow clause: the showcase must render several fragments reaching components from different families, and no fragment may paint a colour of its own — falsified twice against sabotaged sources.

**Three passes on the same fault, and it was always a ratio.** Section headings and card titles shipped at the same size (no structure above the card); then card titles sat 1.14× their body, which is a rounding error rather than a hierarchy. **`bold` is refused system-wide** (2026-08-09, Kushagra: *"We don't use bold, we shouldn't"*) — refused in the TYPE and the token deleted with it, because a value left in the union is one every call site can re-introduce; semibold tops the ladder and hierarchy is size plus the ink roles. The removal caught a law that would have failed on the fix rather than the defect (`type.test.ts` looped a literal four-weight list; it derives from config now, and gained the assertion a removal needs). **DECISIONS §15 gained "Composition: the house style"** — the standing brief for every surface this project composes, written because *"I can control spacing using density, but the type is on the consumer"*: every distance answers to an axis, type has none, so `size` is a free index and a written rule is the only thing between the system and nine opinions per screen. The ladder is five steps and a floor — page 8, section 7, card title 6, body 3, label and meta 2 — and **size 1 is retired from composed surfaces** (12px is for genuinely marginal text; "matters less" is what the muted and faint ink roles say at 14 or 16). Two rules were written straight off the reference set and reversed the same day for coming off a reference sheet rather than out of this system: the **eyebrow** (two elements doing one element's job) and the sentence-with-a-period headline — Kookie titles are succinct and self-explanatory. The playground's page also stopped painting `--neutral-1` and painted the seal, so a flat card had nothing but its hairline to be a card with — **reversed 2026-08-10 (Kushagra), back to `--neutral-1`**: the surfaces held the harder bed, but a page that IS the seal makes every surface argue with its bed before it argues with the eye, and glass over a backdrop identical to the seal is glass judged over nothing. The library paints no page background in either direction; this is only ever the app's call. **apps/docs installs Hugeicons (2026-08-10)** at stroke 1.5 (1.75 since 2026-08-23), closing a gap between §8 and one of its consumers rather than deciding anything — §8 has always named the set; the app had been drawing its own.

**The motion system landed 2026-08-09 and the fields closed it out 2026-08-10 (§8) — the §14 slice's last standing debt, and the 2026-08-03 finding survived it by being SPLIT rather than traded.** An eased press never reaches its colour inside a ~60ms tap, which is why every transition shipped zero for six days; the resolution is two clocks. Paint is a *signal* — it eases, it is short, and a press sets `--kui-ct-paint` to zero, so a tap lands its colour on the first frame. Geometry is *physics* — it rides a damped spring **baked into a `linear()` easing** (config states ζ and ω, the generator samples the model, a law re-derives the emitted curve), so it costs exactly what a cubic-bezier costs and there is still no JS at interaction time. Geometry has two clocks of its own: a press is short and stiff, everything else is the object RECOVERING and is long and lively. **Each family says how far it moves and nobody says how long** — a button sinks and rises 1px to the pointer, a mark squashes, a field's box does not move at all; a checkbox's tick DRAWS along its own stroke (`pathLength` normalises it) and a radio's dot arrives, both IN and never OUT. Damping is sacred: the shipped curves cross their target at most once, and a movement that needs to be more visible gets more TRAVEL. Three structural forcings, each caught by a law: the switch thumb had to be drawn by BOTH its edges (`inset-inline-end: auto` cannot be animated to, and `aspect-ratio` silently outranked the lean), a mark must state NO resting transform (any non-`none` value makes a stacking context, which broke the 12px stacking rule in five laws at once), and the harness had to learn stillness by default (`inMotion()` opts a law back in). Menu is the floating half — the panel unfurls out of a seed and dissolves, with `--floating-w/-h` measured on mount because a `max()` floor beats an animating width. **2026-08-10, the fields:** their answer is mostly a refusal (a field's box does not travel or scale, measured under a real pointer) plus two findings. The field family and the mark family had **no hover state whatsoever** — measured byte-identical at rest and hovered, because the shared rule steps the FILL and those are exactly the two families whose fill is pinned — so the BOUNDARY now steps, mixed toward the family's own ink (the solved edges sit between ladder rungs by construction, so "+1 step" is undefined on them; a mix also carries the tone and needs no second number for dark). And a field's ring is INSTANT by measurement, not by preference: Chrome resolves `outline-offset` to whole CSS pixels, so a ring gets one frame per pixel of travel — the field's 2px of room is three values and reads as a stutter, the button's 4px is five and reads as motion, which bounds ring travel below at ~4px. **The day's real find was that `prefers-reduced-motion` had never worked**: `:not()` takes the specificity of its most specific ARGUMENT, so the ring rule was (0,3,0) against a (0,2,0) stand-down and landed for every user who asked their OS for stillness — invisible because the suppression law walked `transition` (this is an `animation`) and, even there, only asked whether a selector was PRESENT in the guarded block rather than whether it WON. Reduced motion now stands down a HOOK, so a recipe and its suppression share a selector; and the suite can enter a media query for the first time (`asksForStillness()` over CDP), which is what turns that whole story from stylesheet text into a measurement. Same day, one harness fix: a law that hovers left the pointer parked for the next file — three radio laws read a hovered border as a resting one, passing alone and failing together. Budget 20,921 → 23,594 gzipped.

**The look axis split in two 2026-08-10 (§19, Kushagra), and `surfaces` became `depth` to make room — then the axis was DELETED WHOLE: the control half 2026-08-19, and `surfaceLook` 2026-08-20 (its `filled` value was never judged or used — see the 2026-08-20 paragraph below; this paragraph is history).** `surfaceLook` and `controlLook`, each `outlined | filled`: cards and the panels wearing a card's identity on one, fields and marks on the other (they move together — a filled input beside an outlined checkbox reads as an accident). **A screenshot settled it, not an argument**: a white card holding grey filled inputs, the most ordinary form on the web, was unreachable, because one answer moved the card to `--neutral-2` and the field one step past it and one step is mush. **No colour value moved** — the split changes which values a single prop can reach, nothing about what they are; both-filled is still mush and stays on the eye-pass list, which is where a claim of mine was withdrawn (I argued the split forces a 2×2 pigment table; it does not — the plain-card cell works with today's numbers, and the coupling exists only in the cell that was already broken). Refused with their strongest forms answered: taking the surface family OUT instead (delivers the same screenshot, but a card is the most resting surface in the library, so it cannot use the exit the slider/switch/progress bar used, and it forecloses the tinted surfaces `filled` should grow into) and one OBJECT-valued prop (partial overrides need merge semantics no other axis has; an inline literal is a fresh identity every render — Theme's own 2026-08-06 memo bug as an API). Mechanism: `lookAxes` in config owns the grouping, two independent selectors over DISJOINT families rather than a compound (a law asserts the partition — two blocks writing one role would make the losing prop silently inert), and a mounted law measures the cell the split exists for. The components outside the axis are now pinned against BOTH props, the playground law derives its axis list from `themeDefaults` instead of naming five by hand, and `/preview` and `/matrix` grew the second picker. +68 bytes gzipped, all of it longer selector names.

**The ink ladder became solved targets 2026-08-10 (§15, Kushagra: "there's not a lot of contrast difference between the three").** Measured, the rungs ran 103/78/65 light and 94/67/36 dark, and the two halves of the system reached them by different means — neutral picked steps 12/11/10 while every chroma family faded a fixed 74%/52% — so nothing knew what contrast any rung landed on. `inkLc` now states two numbers and the generator SOLVES the fade per family per mode against the harder of the seal and the page (the `--accent-label`/`--control-edge` mechanism's fourth consumer, no new machinery); every family lands within a point of target on both beds, law-tested through the emitted percentage and falsified against the old picked values. **Loud is deliberately not solved** (Kushagra) — it stays the family's designed text colour, with its own law, because the accessible resting state for reading should not be at the mercy of a target. **Medium is `apcaFloors.body` exactly (60)** — real information said quietly — and **quiet is 30, below the reading floor on purpose**: the EXCEPTION rung, for something stood down, a written carve-out from the 2026-08-07 "text stays floor-checked" rule rather than a quiet erosion of it. Two consumers moved with the definitions: placeholders left faint for muted (faint had named the placeholder as its case while it merely meant "the quiet one"), and `--color-text-caption` was deleted — minted a day earlier for group labels, which is what muted now means. Open and recorded: a chroma family's loud measures ~66 in dark, so a coloured ladder there is 66/60/30 and its top two rungs read as one; the fix is a per-family loud, the exception this rewrite removed, so it waits for the eye pass. +145 bytes gzipped.

**The icon box joined the pointer axis 2026-08-10 (§4, Kushagra, from the playground: the icons "don't seem to scale" on coarse).** It was the ONE thing inside a control the coarse world never re-priced — measured at size 2 default, the button grows 32 → 44, its label 14 → 16, its line box 20 → 24 and its checkbox sibling 16 → 20, while the glyph sat at 16 in both worlds. The guessed cause (the stroke) was one layer off: a stroke lives in the glyph's viewBox and scales with the box, so nothing scaled because the box did not. **The real cause was one sentence covering two axes** — config read "NOT part of the density or pointer sets — the icon grid is a perception floor, not a breathing-room choice", which is a correct DENSITY argument extended to pointer with no argument of its own; coarse is not breathing room, it is a screen held close, which is why it re-prices type and the whole mark family. §12's "label cluster" (type + icon + the gap) had already implied it, with two of three members answering the axis. Now its own designed ladder per world — 16/16/20/24 fine, 20/20/24/24 coarse — NOT a ride on the line box like the marks, because an icon must land on the 16/20/24 drawing grid while a type step lands wherever the ramp does; density still declares no icon box anywhere. Two laws, both falsified against the pre-fix ladder: the emitted ladder (both worlds in full, coarse never smaller, plus the vacuity guard that some size must actually rise) and the PAINTED width of an icon in a mounted Button per size — the second being the one a token law could never have made. +32 bytes gzipped; v0 for the eye pass.

**Dialog shipped 2026-08-10 (§24) — the floating family's third member, the first anchored to nothing, and a Card that COVERS.** Six flat exports (shadcn vocabulary, credited); Base UI's Dialog carries the focus trap, the scroll lock and the a11y wiring, and the panel wears the surface identity whole (seal, edge, look, material, and its padding off the ordinary surface size join), so what the component designs is only what a card never needed. It carries a second family class, `kui-overlay`, the way an anchored panel carries `kui-floating` — but deliberately NOT `kui-floating` itself, which would import the concentric corner (written for a pane hugging rows) and the floating cast. **`size` prices the BOX alone** (Kushagra's call, reversing §6's "a dialog has one size"): width, padding and corner, never the type inside it, so `DialogTitle`/`DialogDescription` state §15's composition steps and a dialog matches the playground's confirm card by construction. **That reversal took the overlay radius band with it** — `--radius-overlay-1..4`, picks 7-10, so an overlay wears the corner of the card ONE SIZE UP at every level, the 2026-08-04 Card amendment arriving one band over; the old flat value survives as what size 4 wears. Width is a designed raw-px ladder (`--overlay-w-N`, family-named because the shared layer spells it) and a MAXIMUM: the viewport's gutter wins on a narrow window. **The SCRIM is minted** — §10's table has carried the recipe unemitted since 2026-08-04 — as its own per-mode pair rather than a fourth material (a material makes a component's own fill translucent to defend a foreground; a scrim pushes the app back), black in both modes because a page-coloured scrim vanishes in dark, dark leaning harder, blur below the thinnest material's, with `contrast="high"` and reduced transparency sharing one answer: more pigment, no defocus. **And the scrim IS the separation, so the panel casts nothing of its own** (Kushagra: "no cast, but needs to respect material") — §22's coverage clause is satisfied by the whole viewport going dark, and what survives is the world's ordinary surface chrome, law-asserted equal to a Card's in both worlds with a flat-world menu as the negative control. Anatomy is §10's one sanctioned case (Title/Description forced by `aria-labelledby`/`aria-describedby`); Header/Footer and the corner ✕ are refused as Card's cut re-argued, and `DialogClose` places a real Button. Two mechanisms fell out of being unanchored: **ambient direction falls back to the document** (a dialog need not have a trigger, and a stamped `ltr` OVERRIDES the `rtl` a portal would inherit — the stale-stamp finding by a different road), and the panel centres by AUTO MARGINS inside Base UI's scrollable viewport, because a centred flex item taller than its scroll container puts its own title out of reach. **Its first stylesheet was refused by a shipped law** — `data-size` in a component sheet is what the no-axis walk forbids — so the per-size spellings live in the shared layer's overlay size join, which answers the corner and PUBLISHES the width for the pane to consume (a Sheet that wants the window's width will simply not read it). **Its entry locked 2026-08-16 as DEPTH, NOT DISTANCE** (§24; the materialization it replaced went to AlertDialog, §25): 3% in z on the new `poised` spring (ζ0.8 — one crossing at ~1.5%, because mass forbids overshoot), zero travel in x/y, presence as paint, and the content sharing exactly one channel — blur → sharp on the box's own clock, because depth of field is a property of the mass and blur is the only thing the system may honestly animate on content it does not own. The scrim pushing the app back IS the arrival, which is what buys the panel the right to move almost not at all. No measurement, no runner, no JS: Base UI's stamps carry it, and the ABSENCE of every size channel is law-asserted because the absence is the design. 18 mounted laws, four sabotage passes; +295 bytes gzipped; the width ladder, the gutter, the scrim's alphas and the band's picks are all v0.

**AlertDialog shipped 2026-08-16 (§25) — the overlay family's second member, split from Dialog because the motion proved they are different things, and the first component whose CONTENT is system-owned.** Seven flat exports (shadcn vocabulary, credited); `role="alertdialog"`, outside-press refused (Escape stays — a keyboard "not now" is Cancel by another route). The fixed anatomy is licensed by three forcers stacking (§10's criterion): the role wires name+description, the behavior demands actions exist with focus on the safest, and the entry animates content — legal only on content the system owns. The cascade: role → closed content → closed box → owned motion; open the slots and the print leaves with them. **`size` prices EVERYTHING** (box, corner, padding, title/description type steps, buttons) where Dialog's stops at the box — ownership is the difference; size 3 IS the confirm card, law-asserted against a mounted Heading. **Width is FIXED per index** (`alertWidth` 280/320/360/400 v0, no prop, strictly narrower than the dialog's at every step), published by the shared overlay join re-pointing `--kui-overlay-w` (the switch join's shape). **The component owns its layout** — a two-column grid on the entry's body: non-buttons span, actions split 50/50 and fill, a lone Action spans whole; Cancel-first-in-DOM is position, reading order and initial focus at once. Cancel and Action are priced Buttons with NO render escape; Action defaults LOUD — legal exactly here, §11's one-focal-point rule held by anatomy. **The materialization moved home with it** (the motion agent's sequencing): recipe re-keyed to the alert's popup, OverlayBody promoted to system/floating.tsx on its second consumer, three motion laws moved and re-targeted; Dialog is entry-less for one commit until its large-mass entry lands. Lab2's fake alerts replaced with the real component under the lab's own classes. The boundary sentence: any control beyond the two buttons makes it a Dialog. Three instrument findings recorded in LOG (the viewport eats backdrop clicks; defaultOpen focuses the popup; the pointer-park guard was blind to portals — widened). 17 mounted laws, six sabotage passes; budget re-recorded.

**An interactive surface MOVES 2026-08-17 (§8, §10, Kushagra: “should work like button, but because of larger area, perhaps a little different physics”).** Asked whether any component still lacked the motion principles, and the answer took a measurement: a `<Card render={<button/>}>` had `transition-duration: 0s` and `translate: none` while its fill stepped white-to-grey under the pointer. A dating artifact — card-as-button shipped 2026-08-03, the motion system landed six days later written against `.kui-control`, and §8's roster (button, mark, field, select trigger, slider grip) is controls all the way down. §10 had already said an interactive surface REUSES the control state machine, and that is now as true of how it moves as of what colour it turns: same two clocks, same lively recovery and stiff press, same `kui-ring-land` arrival, same shared `--hover-travel` rise. Only the DISTANCES are its own, which is §8's rule stated exactly — and the SCALE is the one that could not be shared, because scale is relative: a ~64px button at 0.975 moves each edge 0.8px and a 400px card at the same factor moves 5px, which reads as the page flexing. 0.995 matches the button's EDGE movement (1.0px at 400px); the sink halves to 1px. A second SPRING was proposed on §24's “mass forbids overshoot” and **refuted by arithmetic before it was built** — lively overshoots 10.7% and 10.7% of one pixel is a tenth of a pixel. Everything else came back covered or refused on purpose (fields by measured decision, Spinner/Progress motion-as-content, the type family stateless). An existing law caught the work and was right to: `resolveHooks` knew only `--kui-ct-`, so the surface's channels read as unsprung and hand-typed — widened to both private stems. Two instrument findings: the first draft opted into motion and read a correct hover rise as `0px` (the first sample of a 550ms spring), and `el.focus()` does not make a BUTTON `:focus-visible` in Chrome, so the ring law asserted nothing. +74 bytes gzipped, measured against a real build.

**A panel that lands BESIDE its trigger flies from the SEAM 2026-08-17 (§22, Kushagra: the submenu “travels a lot, especially if dropdown menu is wide”).** Measured on a 365px menu before anything moved: the seed was the sub-trigger ROW whole — 353 x 30 at x=10 — flying into a 92 x 73 panel at x=376, so 366px of travel while SHRINKING to a quarter of its width, both numbers the parent panel's own. The silhouette is honest only where the panel LANDS on the thing it came out of; a submenu lands beside the panel its row sits in, so photographing the row starts it somewhere it will never be and the unfurl runs backwards. Keyed on the PLACEMENT and never on the component — the positioner already publishes `data-side`, so the runner asks the question the placement answered. A side-opening panel keeps the row's height and corner (that edge is real), drops the width photograph to the family's designed seed, and takes an x offset of ZERO, which also makes it direction-blind for free. Decided in `aim()`, the first moment the placement is certain and still before any frame can paint. The 2026-08-10 refusal and the 2026-08-15 re-opening had both reasoned about this and neither measured the travel at a realistic width; surfaces.css had shipped the opposite claim (“grows the short, true distance out”), true only for a narrow menu. Its law opens by HOVER and reads the LAST seeded frame — `defaultOpen` on the sub measured 5.8px of pre-fix travel and the first aimed frame 10px, against the real 366 — both caught by sabotaging the fix and finding the failure message too small to be the defect.

**The entry work of 2026-08-14→16, compressed (LOG carries each in full).** The floating entry LOCKED as the trigger's own silhouette — three forming recipes (condensation, deep scale, clip-morph), the restored circle and a covering placement all judged out; the panel's first frame is the trigger's box sitting exactly ON it, position measured never derived, the seed a static pose, invisible until aimed. The overlay materialization FORMED against it — a circle rising from below into a MEASURED box — and the family unified on the elastic spring (ζ 0.62, one ~8.4% crossing): box clocks speed-matched to their travel (overlay ~1.7× the menu's), content clocks time-matched to the family (print 380, echo 8px, shared verbatim); it then moved whole to AlertDialog at the split. Two mechanism findings shipped with it: **an ending rule must RESTATE the entry's geometry channels** (dropping a property from `transition-property` cancels its running transition — Escape mid-flight snapped the box 125→560 in one frame before), and **a quick reopen is an open with NO BIRTH** (Base UI hands back the still-mounted popup with no starting stamp — the floating layer's observers read `data-open` returning as the announcement, a new flight retires the old one's release clock, and the menu's cancel listener arms at departure, never at begin) — **the first half of which was REVERSED 2026-08-20 (Kushagra: "on second quick click it does show wrong animation"): a reopen mid-dissolve is CAUGHT, not replayed.** The panel is on screen and already placed, so posing it back onto the trigger's silhouette teleported it — measured 355 x 98 at 58% opacity to 239 x 32 at full opacity in ONE frame, 3/3, and 358px → 64px off a short trigger — before unfurling it again. An entry is how a panel ARRIVES and this one never left; the ending stamp leaving simply takes the exit's targets off and the paint clock carries it back from wherever the dissolve reached — **and since 2026-08-25 that same edge rests the popup's OWN ScrollArea viewport at its top: the catch keeps the box's flight, never the list's browsing** (a fresh mount rests at the top, so timing must not decide what the second press shows; measured, a constrained menu reopened at `scrollTop` 196 with its group label clipped above the panel's edge). The reset rides the REVOCATION because a reset written at the arrival is undone — the exit's posed body contributes its scaled size to the scrollable overflow and the browser takes the offset back as content grows out of it; its reach is the direct-child chain (`:scope > .kui-scroll-area > .kui-scroll-viewport`), so a caller-composed scroller inside a popover keeps its position and a select — whose offset IS the placement — is untouched by construction, both law-pinned and falsified. A reopen is told from an OPEN by what the panel was doing when `data-open` ARRIVED — mid-dissolve the ending stamp is still on, on a real open it is long gone — because deleting the branch outright took a kept-mounted panel's ordinary second open with it (Select's own law: *the entry ran once per lifetime*). There is no second flight, so the menu law that timed two overlapping clocks is replaced by the hazard it really guarded (dismiss WHILE AIRBORNE, take it back, and the interrupted flight's own pending clock must still land the panel). Both "REPLAYS the entry" laws now assert the opposite, falsified against the restored branch. **The adoption pass ran 2026-08-16 behind a six-dimension audit (22 confirmed findings, each adversarially verified), and its headline was ENTROPY: the two entry runners had drifted four ways** — a laid-out guard dead in one and working in the other (`parseFloat` of an unresolved `calc()` token), a flight registered too late to retire, a body measured after the writes that invalidate its layout, a listener freed on one path of four. **They are ONE runner now**, the single real difference (does this panel fly from its trigger?) asked three times and nowhere else, with the two names for the measured box collapsed to `--kui-fly-*`. The critical find was one channel missing from the exit's restatement (`box-shadow`), which cancelled and snapped a dismissed menu 169 → 303px — closed by restating it, by teaching the dismissal listener that only geometry cancellations end a flight, and by a law that DERIVES the exit's channel list from the entry's so the class cannot return. Three mechanisms had no law at all and now do: the springs (emitted curve re-derived from config's physics, plus each curve's claimed overshoot and crossing count read off its own samples), the flight retirement, and the floating family's reduced-motion suppression — which also changed shape: the guard no longer maintains an inverse of every pose (that inverse is what produced two of the defects), it owes only "nothing moves, nothing is measured", both mounted-law-asserted.

**The material lab was audited 2026-08-16 ahead of any port (ultracode, six lenses each adversarially refuted, every load-bearing claim re-measured in a mounted browser), and the headline finding is that the lab has never rendered the numbers it is written with.** `--l2-noref` sat behind a guard reading an attribute nothing writes to the root — `data-l2-refract` exists, but on individual menu elements with a filter id as its value, and the html stamp was renamed mid-session and lost — so it matched permanently. Measured: a `.l2-regular` glass button drew blur(5.6px) against a `.l2-card`'s blur(2.4px) while declaring LESS blur than the card. The cause is one fact in two homes: the filter chain is written in CSS and again as a JS string for refracting panes, and only the CSS copy carries the term. Fixed by deleting the guard (the file's foot already states it correctly, keyed on the attribute the switch really writes). **It invalidates the control material cells** — "every family re-prices the material at its own scale", stated three times in the CSS as a system rule, was tuning against this defect in the wrong direction — **every perf number from the parked audit**, and every eye judgment on glass buttons, menus and dialogs. The other confirmed findings, none yet fixed: the veil is built from `--color-surface` rather than the component's own fill, so in the LAB tone and the look axis never reach a pane — reported as the port's biggest blocker and **corrected the same day by measurement: the package was already right** (it mixes `--kui-sf-fill-src`; outlined vs filled glass measures different colour at identical alpha, and a destructive glass button tints), the real defect being that the guarantee had NO LAW, so a working package and a broken one read alike — now pinned by a law asserting both halves at once and falsified by transplanting the lab's defect into surfaces.css; the pointer-tracked rim is an unconditional rAF doing per-frame `getBoundingClientRect` per pane (against "no JS at interaction time") AND a second light model contradicting the one the shadow palette, the material rim and the elevated world all derive from; the Lit rung writes literal shadow anatomy that varies per rung and on `:active`, three things §9 forbids; ink on glass is hardcoded alphas, off the solved ladder and its floor; `layered={false}` bakes `--neutral-2`, a page colour the library twice declined to own, and is not identical by construction; the dark thick CARD renders light's spectral rim (that recipe written twice, only the dialog's copy corrected); menu and dialog dark saturation is flat across all three thicknesses, which the shipped monotonicity law would fail on. The merge order and the three axis-shape questions that gate it (does `material` become a Theme property, does `depth` grow a third rung, does `layered` exist) are in `docs/handovers/2026-08-16-material-port-audit.md`. **Nothing ports until those are answered and the lab is re-judged at its real blur.**

**Material became a THEME property 2026-08-16 (§5, §10, Kushagra), and `layered` is dropped.** The proposal was theme-value + per-family offset (dialog one rung up, control one down — the mark family's own shape); Kushagra killed it with one question: **`thick` has no rung above it.** There was never a ladder — material means what the app is BUILT of, a table and a chair of the same oak are the same oak, so ONE value covers the scope and no component can hit a ceiling. What makes a dialog read heavier than a menu is coverage and its scrim, not a second thickness; the evidence for per-family re-pricing had just evaporated anyway (the control cells were tuned against the blur defect, in the wrong direction). Refused: a `glass` boolean (it would still owe a thickness — two props for one fact — and `solid` IS a material, the rung where light stops); a per-component override (my own recommendation, refuted: it leaves glass card → glass field → glass button as the default composition, the thing the one-glass-per-stack call forbids). Nine props deleted. **Two mechanisms, both React:** the value travels by CONTEXT and each component stamps its own `data-material`, so all 28 selectors stay element-keyed (a descendant-keyed rule re-creates the veil-inheritance defect the `@property inherits:false` guards fixed four times); and **glass does not stack** — a member painting a veil scopes its subtree, everything below resolves `solid`, enforced structurally rather than asked of a call site. A `<Theme>` RESETS that scope, which is what keeps portals correct (a menu opened from a glass card is glass again; a field composed inside that card is opaque) and is why Menu/Select/Dialog/AlertDialog each split their popup into an inner component — React context follows the tree, so reading outside the portal would resolve the card's scope. `useMaterial()` is public: a consumer's pane is `<div className="kui-surface" data-material={useMaterial()} />`. `layered` dropped with it — the audit measured that its sealed composite cannot be identical (it baked `--neutral-2`, lost a specificity fight in dark, could not answer thickness), so there is no free glass and opting out of the cost is `material="solid"`. **Cost: zero CSS** — the stylesheets did not move, which is the evidence the move was made at the right layer. 1295 tests; six new laws, all falsified, including the negative control that catches a scope applied unconditionally.

**The judged glass ported 2026-08-16 (§10, Kushagra: "whatever these values are look good") — and it is a DIFFERENT material, not a tuning.** The ladder went 5/16/32 → **2.4/4/5.6** blur, with saturation 172.5/207/241.5 light and 175.5/195/208 dark, alphas 34/49/65 and 52/67/80. Four to six times sharper than what shipped, because the lab's direction was near-clear stone: refraction needs detail left to bend and heavy blur erases what it bends. **The values were EXTRACTED from mounted panes, never read off the stylesheet** — two multipliers sit between the lab's written numbers and the screen (a global Frost dial at 20%, plus per-mode knob sets), so nobody has ever seen the written numbers and porting them would have shipped glass five times frostier than the thing approved. Two laws changed because they were WRONG, not inconvenient: the monotonicity law parsed radii with `\d+`, so a fractional value CRASHED it rather than failing it; and the scrim law asserted the scrim blurs less than the THINNEST material, a premise that dies on contact with the judged ladder and was wrong anyway (a scrim defocuses the whole app to push it back, thin glass stays clear so structure ghosts through — the ceiling against `thick` is what actually binds). Three component laws restated the radii as literals and now derive them. **Recorded and unresolved — the first thing to check in the playground:** the sharp ladder is NOT self-sufficient. In the lab the lens does the legibility work; refraction has not ported (Chromium-only, runtime, keyed on pixel geometry the token pipeline cannot supply for content-sized boxes), so the package defends with blur and saturation alone, §10's stated 12px defense floor is now unmet at every rung by judgment, and quiet text on thick glass over a photo was already marginal where the lens WAS helping. Still lab-only THEN: the shadow stack, the pointer-tracked rim, the Lit rung's literal shadows — the first and third shipped 2026-08-17 (the depth rows and `controlChrome`, the state paragraph below); the pointer-tracked rim stays refused (JS at interaction time, and a second light model). Budget 25327 → 25334.

**The lab's DEPTH and its LENS ported 2026-08-16 (§10, §13), after the colour shipped without either.** Kushagra: *"Why is there no refraction on preview? The depth is different totally… why do you refuse to do things I ask"* — accurate on both counts, same cause: the material's colour ported and its structure was written down as blocked instead of built. **Depth** was redefined at the tokens, which is where he said to do it: the drop rows went from contact + one ambient (topping out at `0 16px 32px -8px`) to the lab's contact + drop + BLAST, whose 32px/80px reach under a card is most of what the eye reads as depth and more than double what the palette could express. Rows carry the lab's own geometry (2 button, 3 menu, 4 solid card, 5 card) and the CHROME ROLES re-point — surfaceChrome 3→5, floatingChrome 5→3 — because the lab prices a cast by the size of the box throwing it ("a smaller caster owes a smaller shadow, or it reads swollen"), which reverses the 2026-08-09 call that a floating pane must out-cast everything: the palette is ordered by REACH, not rank. No component gained a shadow; the fenced resource stays fenced. Found doing it: `fadeShadow` read a literal `shadow[mode][2]`, so moving the role left every glass pane transmitting a row nothing else used — `floatingRow()` had solved that exact problem for its own role and was never generalised. **The lens** is `system/refraction.tsx`: a displacement map computed from Snell's law over a rounded-rect SDF (method from kube.io's "Liquid Glass in the Browser", credited; mathematics re-implemented), the bezel bending the pane's own backdrop with R/G/B split at the lip. It is what makes the near-clear ladder legible — blur HIDES a backdrop, a lens RE-STATES it — so it closes §10's 12px floor, which is RETIRED rather than lowered (the mush ceiling with it). It does not breach "no JS at interaction time": built on mount and resize, never on hover, press, focus or scroll, the seam the floating layer already uses — **and never while a pane is FLYING (2026-08-22)**, the one gesture that resizes a pane continuously and the one that sentence was silent about (27 maps minted during a single glass menu open; the measurement is deferred to the flight's own seam, not skipped). Bounded by memoisation on (w,h,radius,params), a 320px generation cap, and never editing a live filter (Chromium resolves `url(#id)` once). **Additive by construction**: prepended through `var(--kui-lens, )`, which substitutes to nothing on every engine that cannot render it and every surface that is not glass — the empty fallback is load-bearing (bare `var()` is invalid at computed-value time and would take the blur with it), the ORDER is law-pinned (bend then soften), and the property is non-inheriting for the fourth time that guard has been needed, here because a map encodes ONE box. Rejected: walking the blur back up (it would quietly undo the judged ladder), and a separate overlay carrying the lens (two backdrop roots filter each other — it buys the fallback by changing the material). Two mistakes kept: `"chrome" in window` is FALSE in headless Chromium, so the first support gate disabled the lens in the only engine that renders it; and "a solid card carries no lens" could not fail, because a solid card has no `backdrop-filter` at all. WebKit is an open question, stated not guessed — it may parse and paint nothing, and the var() seam means that would cost the blur too.

**Only the OUTERMOST Theme is a query container since 2026-08-16 (§2), and making material a Theme property is what exposed why.** `.kui-theme` carried `container-type: inline-size` unconditionally with the collapse recorded beside it as an accepted caveat — defensible while a nested Theme only re-scoped an axis on a region layout had already sized. `<Theme material="thin">` became the ordinary way to put one pane behind glass, so nested Themes went into flow everywhere, and every glass specimen in the playground rendered ZERO pixels wide from the commit that shipped it — three panes stacked on each other over the hostile bed, found when Kushagra asked how to see the new glass properly. 1,300 laws passed on both sides of the fix: none read a nested Theme's width or its containment. The fix is Box's own 2026-08-08 rule one element over (containment serves a box's CHILDREN, so its own props are no signal — and an axis prop says nothing about whether anything inside wants measuring); tiers in a nested Theme fall back through opted-in ancestors to the root, §2's floor untouched, and `<Box container>` is the escape for a region that genuinely wants measuring. The portal exclusion moved INTO the selector because a following override would now tie it on specificity and survive on source order alone. Three laws, each falsified against the blanket rule — and the load-bearing one measures the WIDTH (`expected 0 to be greater than 40`), not `container-type`, which would have been one indirection short of the thing that was wrong. Rejected: fixing the call sites (leaves the trap loaded on the path the material API now pushes people down), and a Box-style dev warning (nobody asks a Theme for containment; they ask it for glass). Found with it: `/preview`'s panel held `material` in its state and derived its default — exactly what the existing law checks — while no chip could flip it and the canvas Theme was never handed it, so the axis law now asserts a chip AND a prop for every axis.

**Every Theme axis has one home since 2026-08-16 (§5, §12): `themeAxes`.** It holds each axis and every value it takes, the unions derive from it, and a law asserts `themeDefaults` lives inside it key for key. Exported, which is what let `/preview` and `/matrix` stop restating all seven lists — they had no choice before, the lists were not exported, so that entropy was forced rather than chosen. Deliberately NOT merged with the harness's `POINTERS`/`APPEARANCES`, a different fact wearing similar names (the RESOLVED worlds a law walks, excluding `auto`/`inherit`). **Two laws for it could not fail and their own sabotage passes caught both:** asserting a stamped attribute comes back equal cannot fail, because an attribute is written verbatim — it passed a third-depth-rung sabotage, so "is this value IMPLEMENTED" moved to a node law reading the emitted CSS, leaving the browser only what a mount can settle; and that node law then passed a sabotage DELETING a value, since a shorter list is just a shorter walk — it now checks both directions against the stylesheet as an independent second source. Also fixed: the component-reference coverage law parsed the package index with a single-line-anchored regex, so a multi-line export block would have silently dropped every component in it from "every export is EXPLAINED".

**Axis value lists have one home each since 2026-08-16 (§12).** Eight law files carried their own two-value depth array and six their own three-value thickness array — fourteen private copies of a claim about how many values an axis has, every one of which would keep passing after the axis widened, which is what a third depth rung would have walked into. `DEPTHS` now sits beside `Depth` in theme.tsx with the union deriving from the list; `GLASS_MATERIALS` had lived in axes.ts all along and was simply never reached for (surfaces.test.ts imported it at line 13 and restated the literal twice below). `test/browser.tsx` re-exports both beside `POINTERS`/`APPEARANCES`/`DENSITIES`. The law reads the SOURCE rather than the values — a copy that agrees today is the copy that silently disagrees tomorrow — checks both orders (one file had the pair backwards), and was falsified in both arms; it caught its own author on the first run, because the explaining comment spelled the literals out and the walk reads its own file. Left open on purpose: the two docs files restate the same lists, and closing them means exporting the axis lists publicly, which is an API decision.

**Shell shipped 2026-08-16 (§27) — repetition's eleventh entry, spec'd and built the same day from v1's autopsy, +556 bytes gzipped for the whole app frame.** Eight flat exports (Shell, ShellHeader, ShellRail, ShellSidebar, ShellContent, ShellInspector, ShellBottom, ShellTrigger); every pane is a `.kui-surface` claiming its own grid area by name (no child scanning — v1's `displayName` inspection deleted with the thin mode, the exclusivity rule and the close-cascade), so the sheet is almost pure geometry and the seal, edge, material and depth all arrive from surfaces.css. State is pane-owned on Dialog's pattern; an untouched pane is `data-state="auto"`, resolved per §18 window class BY CSS (first paint scriptless and hydration-proof; mount-time callbacks structurally impossible), with the JS mirror serving only aria-expanded and the toggle's starting point — agreement law-pinned. `panes="flush"|"floating"`: flush seams are single hairlines by inner-edge ownership; floating IS the gap (half on the frame's padding, half on every pane's margin — even at every pane combination by construction, where both grid `gap` and v1's margins double somewhere). Overlay presentation is the same element leaving flow (an abs grid item resolves insets against its own AREA — under the header by derivation), with scrim, Escape, `inert` siblings and focus return shipped whole; widths are the system's first sanctioned raw-number props, writing the one custom property a future resize will write. Zero transitions on Menu's own precedent — the spring entry is the recorded follow-up, and a node law asserts the absence. 33 laws, the load-bearing five falsified first; 1,335 package tests; budget 25,327 → 25,883.

**Audited the same day (ultracode, 17 agents; 44 raised, 33 survived, 7 repairs) and the headline is that the shell could permanently BRICK an app.** Containment was written per pane, so with two overlays live each inerted its sibling — including a live one — and each pane's snapshot restored values another had written, leaving `<header>` and `<main>` inert forever after a scrim press. Reachable by pointer alone; all 33 laws green throughout, because every overlay law mounted exactly ONE overlay (the fixture's other panes resolve auto→closed at narrow — "a law about one axis" where the axis is HOW MANY). Repair is structural: containment, focus capture and Escape are the ROOT's, one pass, ONE lazily-filled snapshot, exempting the scrim and the child CONTAINING each live overlay (identity-matching inerted a consumer's wrapper div — the shell inerted its own open drawer), re-running on every Shell render (a plain child mounted behind the scrim notifies nothing), with Escape moved off `document` (layer-blind: a Dialog inside a pane dismissed the pane under it). Six more: overlay panes cap at `calc(100% - var(--touch-target-min))` (uncapped, a 320px window had a 0px scrim and no route back), `box-sizing: border-box` on the frame (floating overflowed its own container by the gap), the header bounds its inline axis, `--kui-shell-w/-h` registered `inherits: false` (the `--kui-h` trap). Five new laws + the mirror's first two, all falsified against the pre-fix code — and two of MY OWN new laws were caught unfalsifiable by their own sabotage pass (a window spanning into the next registration; a law that re-parented its subject). Open, honestly: containment stops at the shell root, and is post-mount. 38 mounted + 8 node laws; budget 25,883 → 25,937 gzipped. **Merged into main 2026-08-20** (LOG): renumbered §26 → §27 (main took 26 for Tabs), the look-axis citations deleted with the axis, and the material law rewritten for SELECTIVITY — it asserted that a glass theme's pane stamps the material, which is false by design since 2026-08-17, so it is now stated relatively against a Card under identical placement. The eight exports are excluded from the builder catalog with reasons (the canvas composes inside a frame; the Shell is the frame). 42 mounted + 8 node laws green against main's rewritten surface layer; budget 29,756 → 30,361.
**2026-08-17 — the lab became the shipped default, and the day ended 1345 laws green.** The whole batch in one paragraph, each fact's full record in DECISIONS and LOG. **The lab port completed**: the surface radius band re-authored to the lab's arcs (top steps 40/45/50 — §6's size-3 anchor retired; EVENED 2026-08-19 to 24/32/40/48/56 when the Card audit read the ladder as a ladder — LOG) with `corner-shape: squircle` on where the engine has it (`--kui-corner-k: 1.613`, floating panes 1.75 by eye); the chrome roles took the lab's literals — surfaceChrome row 4-as-was-5, controlChrome the LIT rung (contact + 8/20 drop + light's inset under-shade, the white rim retired), with `-medium`/`-active` variants and the glass POOL (matter, both worlds) beside them; the material's edge became the masked conic RING (border transparent per thickness; the only edge, which ended the double-line defects), the rim went three layers (grain/bloom/sheen) with the LIFTED variant deleted, solid panes took their matte `--material-solid-rim`; loud glass runs HOT (`c×1.6` @ 0.8α), glass ink/row-wash/keycap remaps landed, dark floating panes LIGHTEN toward white, and the scrim re-priced (0.18/0.32 @ blur 8 saturate .8 — it out-frosts the near-clear ladder on purpose). **Depth defaults `elevated`**; `flat` spells no-op layers, not `none`. **Material became SELECTIVE** (Kushagra: "no point of having a glass button unless there is something behind to refract"): expression is placement — `<Box backdrop>` marks a region, Card (renamed from `overContent`)/Button/TextField/TextArea/SelectTrigger take the prop as a one-off, popups pass it by construction, un-marked in-flow controls resolve solid and pay nothing; a solid pane scopes its subtree solid, a glass pane scopes ON-GLASS — REVISED 2026-08-19: the pane scopes the REGION, and an explicit statement inside it wins (the paragraph below). **The fill-first flip** (the lens "fill is identity, light replaces line"): fields and marks rest on the dress WELL with a supplementing dress edge — the seal-plus-solved-hairline identity, the field fill PIN, and the 2026-08-10 boundary-hover rule all died together (`edgeHoverMix` deleted with its rule); hover is ONE step in ONE currency, law-read in both halves; `controlLook` went INERT (both values emit identical declarations for its two families — held by an identity law until Kushagra deleted the prop outright 2026-08-19) — and the flip broke `contrast="high"` silently (the look value at the Theme scope outranked the `<html>` stand-down by proximity; caught by the ultracode audit measuring NO boundary moving, fixed with look-scoped arms derived from `lookAxes`). **Elevation went selective — the fourth flip**: the field family and the medium rung stopped casting (a well is carved in; a medium fill is a wash ON the plane), the press-tighten re-keyed to loud, glass keeps pool alone. **The alpha-ramp moves**: `--tone-soft` = a3/a4/a5 (dark one step up), `--color-track`, the disabled remap's fills (a3) and the field/mark dress all read `--neutral-aN` so fills composite against their LOCAL ground (a dark card's pooled bottom was swallowing opaque steps); surface dress stays opaque — a pane SEALS; non-tone roles dim by `--disabled-dim` 70% (both grips, in lockstep — and writing the rule caught the slider's never-dimming dead rail). **The grips and the cap left `--control-chrome`** for cap-scale `--grip-cast`/`--kbd-relief` (the lit chrome read swollen under a 16-28px cap); the slider thumb's hairline is gone; Kbd's edge went achromatic (`--kbd-edge` — tone moves its fill and ink only). **ScrollArea shipped** (one export over Base UI's anatomy; overlay alpha thumb, no track, fades on scroll/hover; 13 mounted laws) and **Menu adopted it** — the panel never scrolls, its viewport does (padding and ring clearance moved with the clipping; `panelSeam` reads the viewport; the viewport is border-box — both regressions caught by the existing laws and falsified) — Select deferred deliberately (the overlap placement and curtain care who scrolls; noted in select.tsx). **Overlay padding steps one size up for the whole family** (promoted from the alert's arms). **The docs app**: /preview recomposed under the frontend-design skill pass (Dialog re-drawn as a task modal — the alert owns the two-button question; size-proportionate dialog innards; size-1 swept from composed surfaces; one accent focal point per pane; rhythm 5/3; definitional captions deleted — "helper text must be decision-relevant" is now a standing memory), a Materials section with a live `backdrop` toggle, a ScrollArea section, and `.claude/skills/composition` encoding the Gestalt/classical pass. Two instrument lessons recorded: a synthetic `el.click()` makes Base UI stamp `data-instant` (the flight looked dead; it was suppressed correctly THEN — since 2026-08-19 a `"click"` instant flies, see the next paragraph), and real input is slow enough that a whole entry finishes between statements — the flight laws arm a watcher before the press. Budget 28712 gzipped, re-recorded green.

**Tabs and the segmented control shipped 2026-08-18 (§26) — two components that look alike and share no CSS, built together because reaching for the wrong one is the actual mistake: a tab bar switches what is under it, a segmented control sets a value in place.** **The segmented control is a RADIO GROUP**, and that decision is the load-bearing one: Base UI's `ToggleGroup` announces `role="group"` holding `aria-pressed` buttons while `RadioGroup` announces `role="radiogroup"`, and picking one of several IS a radio group — which also buys arrow-keys-select, every platform's segmented behaviour, and closes the question the 2026-08-06 audit deferred (how selection state is spelled on a control the system did not have: as a real role from the primitive, never an aria attribute bolted onto a Button). Multi-select formatting is toggle buttons and a DIFFERENT component (§11's own row). Geometry is §4's hosted-control rule with N hosts: the TRACK rides the height ladder so the control stands level with the Button beside it (law-asserted against a mounted Button, not a token), each segment is the channel minus a designed inset — the switch's sentence, thumb = track − 2 × inset — and the segments read `--kui-ct-h` by inheritance and stamp no index, so the two boxes cannot disagree. The corner is concentric, spelled "never subtract more corner than there is" rather than clamped at zero (no zero literal survives the tokens-only law, and §6's 9999px capsule is why this system states a corner rather than asking CSS to work it out). `segmentInset` is numerically `switchInset` and deliberately NOT shared: both mean a grip in a channel, but the second member self-keys and the third promotes. The track is a WELL so the whole control leaves the look axis with the switch and the bar; the chosen segment is the GRIP (`--color-thumb`, one value across all three states, casting ALWAYS) — **the seventh box-shadow consumer**, the grips' exception a third time, recorded in the count law where such a decision is supposed to fail first. **It ships NO indicator element and the absence is the design**: Base UI's Tabs measures the active tab for you and RadioGroup does not, so a gliding thumb here means writing the measurement for a motion designed in another room — the curtain's own lesson, one day old. **Tabs' indicator DOES ship because Base UI hands it over free**, rendered by `TabsList` and deliberately not exported (structure, not API), drawn by BOTH inline edges rather than an offset and a width (two edges can travel at two speeds; it costs the same two declarations now), in PHYSICAL `left`/`right` against this package's usual preference because Base UI's measurement is physical and a logical pairing puts the rule on the wrong side of an RTL bar. That indicator re-measures on selection change, which is **JS at interaction time, named as the third bounded exception** beside the flight measurement and the lens. The active tab is marked by INK and the rule, never a louder fill (reads as a button among links) or a heavier label (semibold is wider than medium, so the bar reflows on every switch); the ink pair is the TONE-LESS foreground roles, because the ink trio only exists under a stamped `[data-tone]` and tabs refuse tone. The bar's hairline is `--color-border`, law-asserted to resolve the same colour a Separator does. **The part vocabulary diverges from shadcn for the first time**: `TabsTrigger` cannot come across, because a *trigger* in this package opens a floating layer and a tab opens nothing. Glass: the segmented track is a pane and scopes its subtree (one glass per stack, structurally); a tab bar paints nothing, so material never reaches it. **Both grew the travelling highlight 2026-08-23** (§8, Kushagra, judged in the "Clip vs Physics" bench): one object drawn by its two inline edges, the edge FACING the destination on the shorter clock (320 against 480, both on `calm`), so it stretches toward where it is going and gathers itself on arrival — measured, a tab rule resting at 56.9px peaks at 125.7 mid-flight and settles at 94. Direction is the one fact CSS cannot see, so it is stamped: **Tabs gets it free** (Base UI already publishes `data-activation-direction`, `none` included, which is exactly the first paint — so Tabs gained NO JavaScript), and the segmented control writes its own. **The tab rule is drawn by two edges again with the second one DERIVED** — `calc(100% - left - width)`, from the pair Base UI computes in ONE space — which closes the 2026-08-19 revert on the terms the file itself set: measured 91.69 against a 91.67 tab on a bar overflowing by 61px, where `--active-tab-right` drew 0. **The segmented thumb is the FOURTH bounded exception to "no JS at interaction time"**, and index arithmetic was refuted by measurement before it was refused: segments are equal only while the track sizes itself (425/425/425), and a squeezed track makes `min-width: auto` bind on the longest label (62/62/72 at 200px, where arithmetic answers 65.3). The selection is watched through `data-checked`, because an uncontrolled RadioGroup holds its value inside Base UI and never re-renders the component — the first spelling's effect ran once per lifetime and the thumb sat still while another segment was checked. The chosen segment gave up its fill to the thumb (two boxes painting one grip blink); its ink still switches instantly, which is §8's split. The keyboard is NOT exempted, departing from the bench on purpose: its stated reason is that gliding across intermediate stops is false motion, and an arrow key moves exactly one position. **The channel gained its WALLS 2026-08-25** (Kushagra, from the bench beside the glass preview — reported as glass-only and measured identical in both: the calm spring's ~6.8% overshoot put the grip 14.11px OUTSIDE the track on a full jump; glass only made it visible): the spring rides the two registered `<length>` insets the measurement writes and the painted inset floors at `--segment-inset` through `max()`, so interior flights are byte-identical and a flight into an end seat spends its overshoot as a SQUASH against the wall — the bench's own lean rule ("a lean can never cross a boundary") applied to the flight. Rejected: `overflow: clip` (pins at the box, not the channel, and shaves the cast) and a per-destination non-overshooting curve (breaks "one character, asymmetry only in the durations"). The wall law sweeps a seized flight with a calibration half (the RAW inset must still cross — tamed ≠ clamped), both halves sabotage-proven; the squeezed-seat law's fixture was re-cut on the way (0.55 × natural sat below min-content, putting the SEATS outside the channel — the degenerate geometry, not the law's subject). **Tabs took the wall the same day** ("lets fix tab too") with the half segmented did not need — the floor is ADAPTIVE, `min(target, 0%)` per edge, because on an overflowing bar a tab's resting `right` inset is legitimately negative and a static floor re-commits the 2026-08-19 coordinate regression as a clamp (sabotaged: the static spelling fails the overflow laws). The rule may still stretch past the last LABEL along the hairline of a bar the tabs do not fill — ink on a rail that continues; the wall is the BOX, whose start every flight back to tab 1 used to escape. Tabs still gains no JavaScript. +57 bytes across both. **Two defects the playground found the same day**: the grip painted OVER its own label (white on white — the JSX claimed document order put the thumb under the segments, and painting order says otherwise: positioned descendants paint after every static sibling whatever the DOM says; `elementFromPoint` at the label's centre returned the thumb, while every colour law stayed green and correctly so, because the ink was never wrong — fixed with `position: relative` on the segment, no `z-index`), and **a tab ended on the line it stands on** (Kushagra, naming his own precedent: the segment is 28 in a 32 track, "we need the same for tabs"). The bar keeps the ladder and the TAB shrinks by a new `tabInset` — both halves are the law, since shrinking the bar drops it below the Button beside it; tabs now measure 24/28/36/44, exactly their segment sibling at every index. The padding is BLOCK-only and load-bearing: `--active-tab-left` is measured from the border box while the rule resolves against the padding box, so inline padding would shift every rule by its own width. An existing law asserting "every tab stands at `--control-height-N`" was the defect stated as a guarantee, and is re-keyed rather than deleted. **A third, same day and the same shape as the first two**: the chosen segment kept answering hover, so a click left a static wash exactly where the grip was travelling to (Kushagra: "the hover continues to stay, which doesn't wobble btw, making it look very weird"). Its old fill triple carried TWO facts — the grip's colour AND the rule that a grip does not fill — and moving the colour to the thumb deleted the triple rather than re-pointing it, so the segment fell back to the quiet rung's hover step (measured 6.7% over the gliding thumb). Pinned transparent across all three sources; the laws had moved with the colour too, leaving the invariance with no reader. **The rule this earns: when a declaration moves, ask what else it was saying — a property pinned across three states is usually a value AND an invariance, and moving the value is not moving the invariance.** 51 mounted laws, every one falsified. **Audited 2026-08-19 (ultracode, 31 agents, adversarially verified, the three worst re-measured by hand) and the findings are fixed** — 23 survived, deduping to nine defects and eight laws that could not fail, and the pattern was ONE mistake five times: the components inherited the shared control layer by ASSERTION rather than by measurement, then the laws were written from the same assertion, so each defect shipped with a confident comment stating the false premise and a law reading that comment's own inputs. The worst was invisible text (`--color-thumb` and `--color-text` are the same token in dark by construction, so the chosen segment's label was painted in its own fill at 1.00:1 — closed by minting `--color-thumb-label` beside the fill it answers to, out of values the generator had already solved). Two of the five were defects this repo had already closed and cited BY NAME in the comment re-committing them: the missing border term is audit D3, and a disabled state written in non-tone roles is the slider rail's, now on its fourth appearance. The tab rule went back to `left` + `width` (Kushagra: "whatever everyone else does") after the both-edges spelling measured ZERO width on an overflowing bar — Base UI publishes `right` in scroll coordinates and CSS resolves it against the padding box. Segments sit flush at gap 0 (Kushagra). Left open and recorded: RTL arrow keys move focus, and on the segmented control the VALUE, in the wrong direction — Base UI's composite reads its own React direction context and neither component renders a `DirectionProvider`; pre-existing, systemic (Slider since 2026-08-06) and fixed package-wide. **Two laws were thrown away pre-audit for being unfalsifiable and both were caught by their own sabotage pass**: "drawn by both edges" read computed insets, which resolve to USED values on a positioned element and can therefore never be `auto`, so `translate` + `width` passed it; and "one glass per stack" asserted segments carry no backdrop-filter, which passed with the scope deleted because a segment never asks for a material at all. Base UI bumped 1.6.0 → 1.7.0 in the same change (both its fixes land on these primitives: the Tabs pre-hydration script left client bundles, RadioGroup's form values aligned with native submission). +229 bytes gzipped for both; `segmentInset` and `tabRule` are v0.

**2026-08-19 — the audit's open findings closed, and two of Kushagra's calls landed.** **A solid surface HOSTS glass** (his words: "the whole point of solid surface is to be able to host glass"): the solid pane arm had silently vetoed every explicit `backdrop` statement inside any card — the prop, the region, the public hook alike. GlassScope now resets `BackdropContext` in both arms (a region stops at a pane's edge; unmarked members stay solid and pay nothing) and an explicit statement inside a pane resolves the theme's material; the reset also closed the nested-Theme glass-on-glass escape, Card's `render` path joined the pane scope, and its on-glass lens gate matches the siblings. **`controlLook` is DELETED** ("delete it"): fields and marks read the dress roles directly, no attribute stamps, the HC proximity trap cannot recur for them, and every guarantee the axis's laws carried was re-keyed rather than dropped. **Base UI 1.7 stays and the keyboard keeps the entry flight**: 1.7 stamps `instantType "click"` on `detail === 0` presses, which killed every keyboard open's flight; runner and stylesheet exempt `"click"` together, law driven by a real keyboard. **The veil takes opaque twins**: the alpha-ramp sources multiplied through the material mix (a glass field's 62% veil rendered at 4%); `--*-solid` twins plus one `.kui-control[data-material]` re-point restore the judged ladder on glass. **The dead palette went per-mode** (`disabledSteps`: light a2/dark a3, border a3 — light's dead box had been byte-identical to a live medium's, and the dead edge out-contrasted the live one in dark). **Conformance reached the dark corners**: HC glass panes trade the ring for a tone hairline (was 1.000:1), dark floating veils answer HC through a token, reduced transparency actually seals floating panes, and `depth="flat"` outlined surfaces got the hairline back (an invisible 1.026:1 card otherwise). **Elevation's leftovers**: quiet glass keeps the pool, pressed loud glass tightens through transmitted active rows, disabled glass stops answering hover with its filter, `--kui-control-chrome-medium` deleted. **The menu's ScrollArea viewport lost its tabindex** (`focusable={false}` — ARIA voids `presentation` on focusables, so menus had a generic node between `menu` and `menuitem` and a stray tab stop). Three tally laws became structural (per-thickness symmetry; one resting definition per rung). Every new or changed law falsified; the preview's Materials scene took the composition ladder and lost its second loud button; registry/DECISIONS/LOG amended in the same batch.

**The builder shipped 2026-08-19 at apps/docs `/builder` (LOG carries the full record), and it took the axis lists public.** A constrained composition editor, not a freeform canvas: the canvas is a live render of the real component tree inside a real `<Theme>`, "drag and drop" is tree surgery against a containment grammar, every distance is a token index picked from a closed list, and the export is idiomatic JSX (real imports, only stated props, a Theme wrapper only for axes that differ from `themeDefaults`) that THROWS rather than widening — the system's refusals given a UI, which is why no commercial builder's failure mode (output drifting past the design system) can occur by construction. One data table (`catalog.ts`, ENGINEERING §1.1 on the builder itself) drives palette, inspector, grammar and presets; the inspector generates itself from the schemas and renders the component reference's `refusals` where the missing knobs would be. **`componentAxes` is the package export it forced** — the API decision LOG 2026-08-16 left waiting — one lowercase object, every list derived from its existing single home. Two anchor laws, falsified: coverage (every export placeable or excluded with a written reason; ScrollArea is the one exclusion — it needs a stated raw height, the value class the builder refuses) and round-trip identity (the exported code, compiled, renders byte-identical to the interpreter, useId salts normalized). The sabotage pass caught that shared-home agreement is not fidelity and earned the third law; running `pnpm dev` caught render-time ids diverging under Strict Mode hydration (`withStableIds`). Blocks are frozen subtrees saved locally, storage is persistence never truth, and JSX import is refused whole. **The responsive slice landed the same day**: prop values speak the package's own `Responsive` shape (tier chips in the inspector, tiers emitted in resolution order, `{ initial }` alone collapsing to the plain spelling, unknown tiers and per-tier values on non-responsive props thrown), `tiers`/`tierNames` join the public exports, Box's `container` prop joins the catalog carrying its §2 trap as an inspector note, and the canvas content sits in a real `<Box container>` sized by a width handle — no device frames, because container-keyed tiers mean the canvas only needs a room; measured live flipping `flex-direction` across the md boundary before shipping. **Direct manipulation landed 2026-08-20** (LOG carries each): the selection chrome stopped impersonating focus (purple — the one hue no token uses — a shape outline tracing the element's real corners INCLUDING its squircle, a bounding box, a size chip, re-measured per frame because a hovered control rises 1px under the pointer, and no canvas control takes real focus any more); **resize walks a designed index** — a corner steps `size` (Kushagra: "resize on components increases size"), a side writes the node's SEAT (`flexGrow` in a row, `gridArea="auto / span N"` in a grid — `auto /` because a bare `span n` sets the ROW), and a handle appears ONLY where one writes something, so a Stack shows none; and **the gutters are visible and draggable** — DevTools' idea in this system's vocabulary, since a gap is a token index, with children grouped into visual rows so one measurement serves all four layouts, inset capsules whose PAINT is the true gutter while the target grows to an 11px floor (§16's move for the mark family), and a per-tier gap shown but not dragged. Found and fixed on the way: the builder was **losing the document on every reload** (the write-through effect saved the pre-load starter over the stored doc; measured two roots before, one after). Recorded open in DECISIONS: a control with no content of its own still hugs it, and a flex ROW gives the parent no way to overrule that.

**The builder became an APPLICATION overnight 2026-08-20 (handover `docs/handovers/2026-08-20-builder-app.md`, five LOG entries).** Several named documents each with their own undo history; every action one row in one command table that the keyboard, the ⌘K palette and the right-click menus all render; copy/paste of subtrees across documents and tabs; multi-selection with wrap-these-in-a-row; Layers drag with file-browser thirds; preview; six templates and a starter, every one held by a law to ZERO review findings; named slots; blocks that export their CONTENT as props; a tier-compare view; a per-document appearance. Then, the same night: **the multi-selection inspector** (one knob over several nodes, offered only where the schemas MATCH — `size` is the control ladder on a Button and the type ladder on a Text, so that pair gets no size knob and cannot be given an illegal value); **a canvas magnifier** (CSS `zoom`, so the overlays stay outside it and not one instrument's arithmetic changed — and zooming PINS the canvas width, because a percentage inside a scaled box resolves in the scaled space and the container tiers were silently moving); **the Layers filter** (⌘F, hides rather than dims, keeps the path to every match); **the measured readout** (what a node's indices actually come to, read off the rendered element rather than restated from config — the inspector only a token system can have); **blocks that open, edit and save back**; and **a canvas error boundary**, because the canvas renders REAL components and an orphaned MenuItem, SelectItem or TabsPanel each throw out of Base UI.

**Three adversarial audits ran that night and every finding was re-measured before it was fixed.** The five that mattered were one shape five times — a mechanism written correctly for the case its author had in mind, applied to a case they did not. A node dragged out of a `slot` kept claiming it, and the filter that hides slotted children from the flow is the one BOTH the canvas and the exporter walk: it sat in the tree, in Layers and in storage, drawn and exported nowhere, while the export still IMPORTED it (repaired at the store's edit path — one door every edit passes — identity-preserving so the interpreter's memo holds; its one existing repair path in `sanitizeNode` was a tautology reading the CHILD's catalog membership under a comment describing a check on the PARENT). **The reviewer could mint a value the system refuses** — `gap: index + 2` unbounded wrote `gap="13"` onto a palette that stops at 12, so the fix that promised to open a gap deleted it, the founding premise broken from inside the one feature meant to enforce it. Preview left every destructive chord armed over a screen that draws no selection (Backspace deleting a node silently, measured). Typing was one undo entry per character, and ~200 characters evicted the whole structural history. And a drop between two grid cells was UNREACHABLE, with the indicator drawing a full-width bar somewhere the pointer was not.

**The THIRD audit read the first two's own fixes, and found the same shape in them.** **Preview could delete your document**: the guard lived in the keydown handler, so the keyboard kept the mode's promise and the ⌘K palette — which lists the same table — did not; measured with every editor pane gone, it offered 66 editing commands and Delete removed a node silently, while the ⋯ menu still offered to delete the whole document. `preview` is a field of the command context now and `armed(cmd, ctx)` is the one gate every surface asks; `global` kept its real question (reach past a focused field) and `enabled` took the other (does this mean anything here), which is what stood ⌘F down. **The width handle collapsed the canvas above 100%**: the painted box is clamped by its parent past zoom 1 (measured at 150%: styled 1320px, offsetWidth 880), so dividing it by the zoom answered 587 for a canvas 880 wide and a grab moving ZERO pixels dropped it a third — the first spelling's defect reflected through 1, closed by reading the room the app already states, with the arithmetic moved beside the drop scan's because the drag and the arrow keys had drifted into two spellings. **And the review panel applied the fix its DEFERRED finding held** — an id is `rule:nodeId`, so matching it only catches a finding that vanished, while several repairs bake a computed value in. Plus: two separate Tabs groups were treated as tabs of each other (one loud button in each read as "different panels" and the figure budget fell silent on a screen showing both); the scan was handed boxes of no area the gap bands had always filtered; and its answer, an index among MEASURED children, went straight back into the full child list.

**Its real yield was eight laws that could not fail, all for ONE reason, and the rule that earns.** Not laws asserting the wrong thing — laws whose FIXTURE could not distinguish a right implementation from a wrong one: identical-height boxes (where document order and pixel order cannot disagree — four separate sabotages survived that one), a walk whose earlier step already did the work of the step under test, a fixture built top-down so sequential ids and document positions were the same list, a label law that asked "not the raw chord" where dropping every middle modifier passes. **A law over a general case must be built on an input where the general case and the special case give DIFFERENT answers, or it is a law about the special case wearing the general one's name** — the 2026-08-03 lesson sharpened from "read the computed value" to "the input matters as much as the output". One law needed a new shape: proving `liveFix` picks the live repair says nothing about the button a person presses, since both hold by its construction and the staleness lives at a call site no node test can mount — so it reads the SOURCE, and `finding.fix` anywhere in the app is the defect returning. **`pnpm run ci` was itself one of these**: turbo hands each task a filtered environment, so `PLAYWRIGHT_BROWSERS_PATH` never reached the browser project and the package's 1,414 browser laws could not launch a browser at all, reporting "8 passed (37)" — a launch failure that reads like a pass, and the 2026-08-08 `docs:test` cache-hit finding in a second home. Passed through now; the two-to-three animation-timing laws it newly reveals are pre-existing, rotate between runs, and sit in files this work never touched — **CLOSED later the same day**: the rotation's last four laws each raced a real-time window (a leftover second click in the select replay law's own gesture, a silhouette read at the seed stamp when the seed stamp is only the visibility GATE, two mid-dissolve premises polled for across a ~200ms fade, a hover aimed at a trigger the flying panel still covered), and the fixes remove the races rather than widen bounds — the silhouette reads at the DEPART edge, and `catchDissolve` (test/browser.tsx) holds a mid-dissolve premise open by pausing the exit's own clocks, which also pins the popup mounted because Base UI unmounts on the animations' `finished` promises. Each rewrite falsified against sabotaged code; one sabotage reproduced a CI failure to the pixel (70 ≤ 36). LOG carries the record and the rule's second half: a premise that is a window is seized or edge-anchored, never raced. **And then the core cause was removed rather than chased (2026-08-20, Kushagra: "dont test animations on ci machine")**: four rounds of instrument work each fixed a real law and revealed the next-most-fragile one, 15 of 21 runs red with the components correct every time, so the residue whose subject IS wall time — floating-ui's convergence loop, a release timer, the browser's scroll restoration — no longer runs where the clock is not ours. `watchesFrames` (test/browser.tsx) marks five laws; the criterion is a claim that depends on WHEN it looks, never "the subject animates" (47 laws call `inMotion()` and almost all read declarations, which persist). A measurement settled it: `KUI_STALL=20` throttling over CDP reproduces NONE of the CI failures, so the cause is bursty scheduling rather than slowness, which no bound can defend against. The exclusion is louder than what it excludes — per-law at the call site, counted as skipped on the CI run, pinned in both directions by `src/test/frames.test.ts` (a new opt-out fails until it is recorded WITH the transient it must catch), and still run by the `pnpm run ci` a human owes before pushing — and an excluded law owes CI whatever half of it is static: the seam's defect is a difference between two states, now read on a landed panel with the clocks off, failing at exactly one padding against the original bug.

**The reviewer was rewritten against the brief's own sentences** (§15 + `.claude/skills/composition`), because most rules had drifted from the sentence they cite: the figure budget is the SCREEN and not the nearest pane (a dialog is its own screen; two tab panels never compete); proximity compares a GROUP against the rhythm it sits in, so the house intervals — a section at 6 holding groups at 5 — stopped being flagged as a fault; `mixed-control-sizes` takes the majority rather than whichever control is first; `heading-ladder` matches its own sentence and offers no fix, because which step a heading wants depends on what it is titling; `orphan-part` keys on `partOf`, which every part declares, rather than on the four that carry `requiresAncestor`. Six rules are new, each enforcing a written sentence nothing checked. **Three law clauses were earned by sabotages that SURVIVED**: a fix must be MINIMAL (at most one prop on at most one node — what catches a size repair also rewriting an emphasis, where both the value and the finding look fine); a repaired document must raise no finding the original did not (what catches a row going 2/4/2 → 2/4/4 and falling silent); and every rule must fire on a document the grammar ALLOWS (what catches a dead exemption arm). Plus: every value a fix writes is checked against the axis it writes to, and every `global` command is proven unable to edit, which is what makes preview safe by construction rather than by a list of ids. 344 docs tests.

**`surfaceLook` is DELETED 2026-08-20 (Kushagra: "deprecate surface look also, like we did with control look"), and the look axis is gone whole.** The two halves died for the two different reasons an axis can be dead: controlLook's values had CONVERGED; surfaceLook's second value was never USED — `filled` shipped as a derivation, stayed v0, was judged by no eye and appeared in no screen, while the lab port made the borderless pane the one surface identity. The deletion knowingly forecloses the 2026-08-10 tinted-surfaces future (answered: a tinted surface returns as a Theme value the day a real app wants one — the §19 background-step open question is that direction's home now). Mechanically the surface chrome collapsed one hop: quiet reads the seal, states read `--color-surface-hover/-active`, and the resting pigment edge is `--surface-edge` — a live `transparent` that `contrast="high"` and `depth="flat"` stand down to `initial` so `var(--tone-border)` resolves at the element; the HC look-scoped arms died with the look blocks (the proximity trap needed a surface colour on the Theme element, and nothing declares one). The axis-flipping exclusion laws (Button, slider, switch, segmented, progress) are deleted — structural now; the HC outcome law re-keyed per family (a dressed edge MOVES, the pane's edge APPEARS); the no-trace law covers both dead attributes and the whole `--look-` namespace. Found on the way: radio's look laws were still flipping the day-old-deleted controlLook through a `theme: object` escape hatch, both mounts one cell; the density preview had kept both look selects for the same reason (emitted HTML has no axis law). −267 bytes, budget re-recorded 29763.

**High contrast never reached a nested appearance Theme, and the whole palette was affected (2026-08-20, Kushagra by eye: a dark card identical at both contrasts while the glass beside it gained an outline).** The HC rule lands on the element carrying `data-contrast`, and every name it re-declares (all ten tones' border/text bands, the solid trio, `--control-edge`, `--field-edge`, `--color-track`) is also written by the APPEARANCE scopes — so any `<Theme appearance>` in between re-states the standard palette nearer to the component and wins by proximity. Measured inside `<html data-contrast="high"><Theme appearance="dark">`: every value equals its standard one and a card paints no boundary. That is the SUPPORTED shape (pre-paint script on `<html>` + any dark section) and it is what /preview renders. Glass escaped alone, because its edge arrives through `[data-contrast="high"] [data-material]` — a descendant arm landing on the component — which is the asymmetry that exposed it. Fixed with one more selector on the same rule, `[data-contrast="high"] [data-appearance="<mode>"]:not([data-contrast="normal"])` (+13 bytes, baseline 29776): the ancestor half is deliberately not mode-keyed (a light Theme nests inside a dark document), the descendant keeps its opt-out guard so a nested `contrast="normal"` still escapes, and both halves are falsified. The platform signal was never affected — its arm already matches nested scopes, so `prefers-contrast: more` always worked and only the explicit prop was dead, which is most of why every audit missed it. **The standing lesson gains a clause: a law that reads a computed value through a mounted Theme is the bar, but every law building the SAME DOM shape is one experiment, not many** — `mounted()` co-locates the axes, which is the one arrangement where this cannot appear; the new law drives `document.documentElement` and reproduces the page. `block()` in tokens.test.ts also stopped pinning a whole selector list (three laws died at once when the rule gained an arm — pinning a spelling, not a guarantee).

**High contrast stopped unmaking the glass (2026-08-20, Kushagra: it "looks cheap and incorrect… it's not taste").** Two parts. **The rim stand-down was a defect**: `contrast="high"` emptied `--material-<t>-rim` to stop it "resurrecting the glint it just removed", and that glint (the LIFTED variant) was deleted 2026-08-17 when the ring took the edge — so for three days it deleted the resting rim to defend against a value that no longer existed. It was backwards anyway: the rim is a gradient INSIDE the pane, so it cannot lower any contrast, and emptying it only removes the cue that the pane is a physical thing catching light — an accessibility setting may not delete information, which is the sense in which "not taste" is right; §10's own "leans on the glass, it does not unmake it" had been true in prose and false in code since the port. What still traded then was the EDGE, on the argument that the ring is white and vanishes over a bright backdrop (measured 1.00 against a pale sky at any opacity) — **and that trade is DELETED WHOLE since 2026-08-26**: its premise died 2026-08-24 when the lip became lit-not-painted (the ring takes pigment through its shade arc and flanks, and a mounted law holds it visible against the page in both modes), and the trade written against a dead premise deleted light painting INSIDE the pane, which is verbatim the rim mistake one part over. Under `contrast="high"` a glass pane, button and field now paint ring, glint, rim and hairline byte-identically to standard; **the VEIL is the setting's whole answer** (Kushagra: "We increased veil, why should there any other difference"), the `scopedHigh` emission mechanism went with it, and the control-scale veil gained the HC floor it never had — DERIVED from the control's designed offset, never a second judged number. State arms are untouched: a disabled or invalid glass field still stands its ring down, because a state is a signal and outranks dress at every contrast. −121 bytes. **The alphaHigh row now raises a FLOOR** ("the rule isn't make everything even more high contrast, it's ensure baseline across"), so the rung furthest from it moves furthest: the shipped row did the reverse — thin moved LEAST (+21 light / +8 dark) while regular moved most (+33/+19), the thin→regular gap nearly DOUBLED under the setting (15→27, 15→26) instead of closing, and thick sat at 94-97%, solid in all but name. Now thin travels furthest (+38/+24), thick least (+21/+10), gaps compress to 8 and 6, thick stays glass at 86/90; monotonicity is untouched, so three rungs remain three. v0 for the eye pass. **The law that pinned the wrong behaviour was confident and specific** — it asserted all three `-rim: initial` strings and guarded `rim-lifted` staying dead, written from the same premise as the code, so it locked the defect in rather than catching it; it now asserts the opposite, with a mounted law reading a real pane's `background-image` at both contrasts beside it. −14 bytes, baseline 29762.

**A pane holds what it contains, and a child may reach its edge 2026-08-20 (§3, §10).** Kushagra asked for Card's technical completeness against shadcn, Radix, MUI, Material 3, Chakra, Mantine and Ant — coverage, not visuals. Everything the peers ship and this system refuses is refused ON RECORD (anatomy, variant/elevation, tone); three things nobody refuses were missing, two of them one hole from two sides. **A card could not hold a picture**: padding lives on the surface, `m` took only scale indexes, negative margins do not exist, so nothing could subtract a pane's own inset — the most ordinary card layout on the web was unbuildable, and every peer built a part for it (Mantine `Card.Section`, MUI `CardMedia`, Ant `cover`). And `.kui-surface` set a radius while never setting `overflow`, so a child that could reach the edge would have squared off the corner it sat in — not a refusal, simply never decided. **Both halves shipped together and neither works alone.** The pane clips (`clip`, never `hidden` — the flight rules' own argument: a hidden box is a scroll container, which is how a select's panel came to slide its contents under a growing frame), and a child states it reaches the edge with **`m="bleed"`** — a VALUE on the margin rows, not a prop. That economy is the decision: the margin rows already carry every per-side and per-tier spelling, so a `bleed`/`bleedX`/`bleedTop` set would have been seven prop rows, twenty-eight `@property` registrations and a second mechanism writing `margin` for the existing var chain to arbitrate against; and Flex/Stack/Grid get it free, being Box with a display preset over one shared prop list. It resolves to `calc(-1 * var(--kui-sf-p, 0px))` — the surface padding hook, deliberately never registered `inherits: false`, which IS the delivery mechanism, so the nearest surface wins and a card inside a dialog bleeds to the card; margins only, because padding and gap reject a negative length. **Clipping obliges a floor**: a clipping pane must pad at least the ring's reach, which the surface band clears everywhere (tightest compact size 1, 8px against 4px) and the menu's panel did not — `--kui-menu-pad`'s 2026-08-09 `max()` is now the general rule with one exception rather than a local trick. **Two mistakes worth keeping:** I deleted two `overflow: clip` declarations as redundant and one was load-bearing (a select's panel scrolls itself, so `select.css` beats the base rule on source order — removing the flight's clip put "Select still jumps" straight back); and the menu's popup declared `overflow: hidden` while its own comment said "clips" and a mounted law asserted `hidden` two lines under a sentence reading "the popup's clip is permanent" — prose and code disagreed for three days, invisibly, because the flight rule covered exactly the frames where it would have shown. Rejected: `overflow-clip-margin` for ring clearance (it buys the ring by unbuying the corner), an `overflow` prop on Card, and widening `componentAxes.space` for the builder (it feeds `p` and `gap` too — `marginSpace` states the narrower vocabulary). Left open from the same review, none a defect in what shipped: a card cannot say "chosen" (the playground's plan picker is radios because of it), and with `surfaceLook` gone there is one surface treatment and no inset panel — the §19 background-step question, sharper. −6 bytes, baseline 29756; 1416 package laws.

**The builder became an APPLICATION 2026-08-20 (LOG carries the full record, and `docs/handovers/2026-08-20-builder-app.md` walks it in plain English).** Everything the editor can do is one table — `commands.ts` holds each action as data (id, title, chord, enabled, run) and the keyboard, the ⌘K palette and the right-click menus are renderers over it, so a command's logic has one home and a shortcut cannot drift from what the palette says it does. State is a reducer over MANY named documents with per-document history, a selection LIST whose last member is the primary (so "wrap these two in a row" is expressible), and snapshots that carry the selection so undo restores what you were looking at. Subtrees copy and paste across documents and tabs through the clipboard EVENTS (no permission prompt, JSON envelope, foreign text refused). Slots became SEATS — one optional `slot` field on the node rather than a second map beside `children`, serialized as the prop it is — and what a seat accepts is closed with icons refused in writing (§8 ships no icon set, so a written one would export an import the reader's app cannot resolve). Blocks parameterize their CONTENT (prop names from the role each text plays, captured words as defaults) and freeze their axes. **And the thing only this system can have: REVIEW** — the §15 house style as live checks over the document (one focal action per surface, differentiated rhythm, the retired size-1, accessible names, inert layouts, tone as vocabulary, the heading ladder, orphan parts), each finding selecting its node, citing the system's own sentence, and carrying a pure fix whose LAW is the round trip: the rule's own fix must make the finding disappear. Six templates ship held to ZERO findings — and writing that law corrected the reviewer twice (rhythm now compares same-axis gaps only; a single-child layout is inert only when `gap` is its one word) and caught two real rhythm faults in the templates and one in the starter document. Also: preview mode, an Xcode jump bar, a point-anchored context menu from the real Menu, per-document appearance (defaulting to `inherit`, law-pinned after the first spelling pinned the canvas light), multi-select chrome, drag auto-scroll, and an empty state that opens on the templates.

**Surface shipped 2026-08-20 (§10) — a GROUND, and the pair that completes the family: a Card is an object, a Surface is what an object sits on.** It began as a report that the system cannot say "quiet grey box", so people reach for card-in-card. **The platform check reversed the framing**: Apple ships two background sets (plain and grouped), three levels each, and in Settings the PAGE steps down while the groups sit at surface level — Apple has never put a grey group inside a white card, and where macOS recesses something inside a container it is almost always a CONTROL (text view, list, colour well). Their three answers are step the page, it's a control with its own dress, or separators and type — and we already had the last two. **Then the claim I built that on failed its own test**: the system has no way to make a surface AT ALL, since every surface is a library component with a pinned identity, so an app or a block needing an unshipped region hand-paints a div. I proposed `Surface` with `fill` and `edge`; **Kushagra refuted it in one line — `fill="seal"` + `edge="hairline"` IS the outlined card deleted the day before, reachable from every call site** — so bounding the vocabulary is not the fix, having none is. Walking the cases, only ONE was genuinely uncovered (sidebar beds, toolbars, tiles, empty states are all Card with `render`/`backdrop`): the hole is one ground, not a capability. **His builder canvas settled the shape** — a hand-painted div holding two cards, wearing a SMALLER corner than the cards inside it — and showed the thing is not a well inside a card but a ground holding cards, the page pattern scoped to a region. **One measurement killed the elegant version**: a relative alpha step (one value, adapts anywhere, flips by mode free — an argument I made twice) is wrong in dark, because dark's ramp is built from white, so a ground over the page lands ~#161617 while the card it holds is #141516 — the cards DARKER than the ground holding them. `--color-ground` is an absolute pair, one step under the seal in light and the page's own value in dark; the platforms disagree on direction (Apple nests lighter in dark, GitHub's `canvas.inset` darker) and darker-in-both was taken. The stated consequence: in dark a ground on the page IS the page and only its hairline bounds it — which is why the edge is not a prop. **The hairline is not an emphasis step** (Button's `bordered` ranks; two grounds, one lined and one not, say the same thing — `surfaceLook`'s own failed test, one day old). **It opens no material scope** — glass defends against something passing BEHIND a pane and a ground's backdrop is its own parent — so a card inside a Surface inside a glass region is byte-identical to the card without it. Its size join reads the OVERLAY band deliberately (a pane containing panes must out-round its contents — Dialog's 2026-08-10 relationship); recorded not fixed: with two unrelated consumers that band's name is wrong. **Rejected:** a `background` prop on Box/Flex (one fact of five — colour without corner, padding, clipping or text context, so it makes the thing hand-buildable rather than buildable, and it splits fill from ink); layout props on Surface (measured unnecessary — `<Surface render={<Stack/>}>` is already one element that is both); a Material-style ground LADDER (a ladder needs a bottom you own, and we never own the page, so only a relative step or a stated pair is expressible); and `Well` as a name (Bootstrap deleted theirs in v4). Nine mounted laws, five sabotage passes; two were instrument bugs caught by their own runs (a raw radius token compared against a painted corner — the squircle multiplier sits between them; and a material law whose held card stated `backdrop` explicitly, which resolves through any pane scope, so it passed with a GlassScope transplanted in). **The PAGE took its name in the same change and lost its twin doing it**: the plan carried two page roles on Apple's split (`systemBackground` / `systemGroupedBackground`, alternating in light, identical in dark), and the second collapsed the moment `groundColor` existed — a page for panes IS the ground. So one role, `--color-page` — the palette's end in each mode, which is what makes it a page. It was `--neutral-1` in both when it shipped; **since 2026-08-25 light is the SEAL's own white** (`#ffffff`, derived from `surfaceColor.light.rest`, one home) because `--neutral-1`'s `#fcfcfc` read dull, so a resting card separates from the page by cast or hairline and never by fill. Dark is still `--neutral-1`, and the pair reads as content-on-the-page, cards-on-the-ground. A NAME, not a mechanism, in `--shadow-N`'s class: the library still paints nothing (a Theme that painted a background would repaint the page at every nested appearance scope), a law walks every stylesheet asserting none reads it and holds the ground to exactly one consumer, and apps/docs stopped reaching into the raw palette in the file that judges every other value by eye — resolving to the same pixels, which is the check on whether the name was honest. +70 bytes, baseline 29826; 1425 package laws.

**The builder moved into the Shell 2026-08-20 (§27), and the port found the frame's hiding mechanism dead.** The builder had shipped its own app frame hours before Shell existed — a `100dvh` column, three fixed-width boxes, four Separators — and porting it is the docs' own no-third-party-UI stance applied to the parts that are "just editor chrome". Header/sidebar(272)/content/inspector(304) map onto panes and the Separators and width boxes delete; what it BUYS is the landmarks (`<main>`, two named `<nav>`s, `<aside>`), the jump bar pinning itself because a `ShellScroll` beside it makes the pane a column, and a phone posture the app never had. **The Add/Layers strip became the RAIL** on the Shell's own sentence ("the rail picks, the sidebar shows what was picked"); each square is also a `ShellTrigger action="open"`, because picking a region the sidebar is not showing must show it — the `armed` dead-control problem answered structurally; one list feeds squares and panel, and the panel's last arm narrows to `never` so a third region with no panel fails `tsc`. **Preview closes the panes instead of unmounting them** (it was throwing away panel state with the pixels): the prop is SPREAD, `{...(preview ? { open: false } : {})}`, because `exactOptionalPropertyTypes` refuses an explicit `undefined` — out of preview the nav panes stay uncontrolled and keep the CSS-resolved `auto`. **The inspector is controlled where the sidebar is not**, and the asymmetry is Shell read straight: a nav column's `auto` means "open on a roomy window" (what the app wants), an inspector's means "closed until asked" (what it does not), so the app states its own — `null` for untouched, resolved through `useWindowClass()`, pinned by the first toggle; before it, the builder opened on a 600px window with the inspector overlaying half the screen. Recorded open: an inspector `auto` an app can mean is a library gap, living in the app until a second consumer wants it. **And the defect: a pane holding the recommended anatomy could not be closed at all** — `data-state="closed"` computed `display: flex` at every width, and a sidebar whose ShellScroll is its only child rested OPEN on a phone. Two layout rules at (0,2,0) land later in the cascade than the hides (shell.css's own `:has(> .kui-shell-scroll)`, and surfaces.css's shared `:has(> .kui-scroll-area:only-child)`); against a pane holding a TEXT NODE — what all 46 laws mount — neither matches, so the hides won by having no opponent. Fixed by prefixing the four hides with the `.kui-surface` every pane already wears, settling it by RANK not order (the pane's own `--kui-sf-p` stand-down's shape, same reason — that rule moved to the join layer in the same day's merge, and the argument travelled with it). **This is the degenerate-fixture lesson in the package**: not a law asserting the wrong thing, a law whose INPUT cannot tell a correct implementation from a broken one — the builder audit earned that sentence from eight laws one day earlier, and this is the ninth, found by a consumer rather than a suite. Eight new Shell laws sweep all four hide conditions × both scroller arrangements (five fail without the fix; the three that survive are kept and named, because which ones they are is the finding), four new builder laws over a real server render of the whole app. +7 bytes, budget re-recorded 30852.

**A shell pane is a SURFACE, and it pads 2026-08-21 (§27, Kushagra: "all shell panes must have padding all around… consider it as a safe area").** Every pane said `padding: 0` from the day it shipped — "a pane's content owns its own inset" — and the frame had no air anywhere: the docs header measured 28px tall with its buttons on the top edge, nav rows 4px from the wall, three consumers re-deriving the frame's height by hand in four spellings. **The rule was FORCED once and generalised past its case**: a highlighted nav row must span its column and a `ShellScroll` must reach the pane's edges, so air the ROW cannot escape breaks both — but neither needs the PANE to have none, and both escapes already existed for other components (a ScrollArea that is a direct child of any surface bleeds to its edge and re-pads its viewport; anything else says `m="bleed"`). So the declaration goes, the surface size join is allowed to reach the pane, and the 2026-08-20 bleed defect disappears rather than being fixed — a ShellScroll at x = −16 in a 288px pane was correct against a pane lying about its padding. **`shellNavInset` is DELETED, not renamed**: 4px drawn by each row as its own margin, existing only because the pane had none, and it would have stacked to 20 on Card's rung; the press expander reads `--kui-sf-p` now, so the paint's inset and the reach that undoes it are one number by construction. The rail's formula loses a term with it (the extent is the square; the padding is the air). **A header's height falls out of the same decision** — one control row at the pane's index inside the pane's padding, so a header is exactly as tall as the rail at that index is wide and the frame's corner is square; a FLOOR, so something taller grows it and something shorter centres. Three spellings load-bearing and each falsified: content-box (under border-box a `min-block-size` of 32 is satisfied by 23px of content plus padding and seam, so the row never binds and the header measures its tallest control again — the defect wearing a token), grid rather than flex for the centring (a flex container shrink-wraps the one full-width child every consumer's header holds), and the index REACHING the header at all — `ShellHeader`, `ShellContent` and `ShellBottom` stamped no `data-size`, so all six panes resolve and stamp it now through one helper rather than four copies. **Predicted and deliberately unsolved**: Card's ladder nearly triples across the index while the control ladder does not quite double, so a rail or header comes out 88px at size 3 and 112 at size 4 — the fraction wall's ninth instance, recorded with the cells named because the call was start on Card's rung and change if needed. Sizes 1-2 (every consumer here) checked in a real browser. **Four existing laws failed and each encoded the OLD behaviour**: the bleed law asserted the hook stood down to zero and is inverted (the scroller reaches the wall AND re-pads inside — a bleed with no re-pad is content against the wall); the other three were arithmetic that moved, and all three first rebuilt the padding box from `--border-width` and came out one pixel wrong on the leading edge, because a flush pane draws ONE border and which side carries it is its neighbours' business — they read `clientLeft`/`clientWidth` now. Eight sabotages, each caught by the right law. **The pane's CORNER answers the index in the same change**: it was pinned to the default card's step because `--kui-sf-radius` inherits and a shell inside another surface must not take that ancestor's corner — a reason spent the moment every pane stamped `data-size`, since the join then declares the hook ON the pane. The drawer's two arms restore the shared layer's EXPRESSION rather than a pinned step (a corner must come back as a property there, and the hook cannot be re-pointed instead: a `[data-flush]` arm and the join's `[data-size]` arm are both (0,2,0), settled by file order); the law that pinned the old value is rewritten as an agreement with a mounted Card at the same index, which the squircle multiplier obliges and which makes the vacuity guard possible — a pinned corner agrees with a Card at exactly ONE index. **A net DELETION of 18 gzipped bytes**, which is the evidence it was made at the right layer; budget 31,295 → 31,277.

**The consumer docs site shipped 2026-08-21 (LOG, handover `docs/handovers/2026-08-21-docs-site.md`) — a GUIDELINE document in the HIG's register, not a props catalogue** (Kushagra: *"very close to HIG by Apple… thats what Kookie is, its not just a UI library"*). Twenty-one chapters, ~17k words, across four sections (Getting started, Philosophy, Foundations, Patterns), plus the 31 rebuilt component pages. Almost none of it is new: it re-states THESIS and DECISIONS in normative voice, which is THESIS §3's own undischarged half (*"teach relentlessly; enforce lightly"* — the published system had been doing the reverse). **The one new chapter is the ontology** (`/philosophy/component-families`): grounds, surfaces, controls, marks, rows, instruments, type, layout and the frame, with the near-twin pairs (Tabs/segmented, Dialog/alert, Menu/Select, Card/Surface) separated by what each one DOES rather than by appearance. **Infrastructure by the Base UI test — take HARD headless things**: the MDX compiler yes (as a loader, every element mapped onto a Kookie component), Shiki yes with a theme GENERATED FROM THE TEN TONES (each token colour reads `--{tone}-ink`, so code samples use the solved contrast values; bold dropped because the package refuses it); `fumadocs-core` refused (a nav tree and a TOC are small code we understand), `content-collections` refused (chapter order must be authored, so a registry exists either way and frontmatter is a second home), Pagefind and twoslash refused with reasons (search is 80 lines over our own data and indexes the REFUSALS — "why can I not set a margin" is a real search; twoslash adds hovers, not correctness, since `tsc` checks every example file). **Examples became real files** (`examples/<slug>.tsx`, imported and rendered AND read and shown), which left the registry pure data and let its coverage law import values instead of parsing source; builder-documents-as-the-only-source was refused because a builder document cannot express a handler, and a docs system that cannot show a submit makes a refusal this system has no argument for. **The API tables generate from the types** with a `tokens.css`-shaped drift law, showing DECLARED props while the agreement law reads the CHECKER's full answer — the first spelling checked the displayed list and failed on eight correct sentences (`gap` from Box's shared table, `rows` from the platform), a law wrong about the general case while right on the case its author had in mind. **The builder is woven in where it matters**: the composition chapter states the house style and then renders the reviewer's ACTUAL `RULES` array, so a rule added to the linter reaches the page the same day. The site is the Shell's SECOND real consumer; the nav tree is one disclosure block standing in for the tree the package will ship. **And the writing audited the system** (/preview forced the composition rules, the builder forced `componentAxes`): 47 exported props had NO JSDoc and they were the most central ones — `size`, `tone`, `emphasis`, `weight`, the controlled-state trios — undocumented because everyone assumes they are obvious, while `bordered`/`loading`/`leading` beside them carried careful comments; the law that caught them then turned out to walk registry ROOTS only, hiding 64 more on the compound parts, which is a law narrower than the rule it enforces and therefore a law that cannot fail. Doc–code drift was fixed in DECISIONS §3, §4, §6, §7, §8, §9, §10 and §11 — the largest being §7's worked example for a `Theme accentColor` prop that has never existed. Three prose primitives are recorded as unshipped gaps (a list, an inline `Link`, a static table); `DialogOpenChangeDetails` is named by a public signature and not exported; TextField stamps `data-bordered` where its field sibling SelectTrigger does not. **The chapters are written in ASD-STE100 Simplified Technical English** (Kushagra rejected the first draft's metaphor and filler): active voice, short sentences, literal vocabulary. `apps/docs/content/AUTHORING.md` states those rules, so later chapters inherit them.

**The message family was reconciled before any of it was built, and Field shipped out of it 2026-08-21 (§28, §29).** Asked what to build next I recommended Callout, on the argument that four semantic tone families ship and nothing consumes them — true, and an argument for spending the tones rather than for that component. Kushagra: *"before we even discuss callout, I want to know what would apple do."* Apple's full HIG component set (eight categories, ~45 components, read from their own data) has **no callout, no inline alert, no banner and no toast**; every message component they ship interrupts, the Status category holds four instruments, and their instruction for the non-interrupting case is *"prefer finding an alternative way to communicate it within the relevant context… a nonintrusive label that describes the problem"*, with colour carrying rank through the four label colours. But the pattern is real — Claude Code's own usage banner is the specimen Kushagra pointed at — so what dies is the NAME, not the component. **Two questions generate the whole family**: what is the message about, and what made it appear; answer both and placement, lifetime, layout space, dismissibility and the ARIA role are all forced. **Notice** is the one condition-message component (placement is the caller's, which is why it is not Banner — Atlassian needs Banner AND SectionMessage because their split is *where it sits*, and §3 forbids a component owning its position); it takes space and never floats, carries at most one RESOLVING action plus a dismissal that only ACKNOWLEDGES, keeps dismissal in the app's state (`onDismiss`, because a component cannot outlive the page it promised to remember), takes an empty-safe icon slot, and rests NEUTRAL because tone is the category and not the volume. **Toast is REFUSED** (Kushagra: *"if an action deserves attention, it must be done so before the action"*) — a toast is important enough to say and too late to act on, a category that does not exist; both cases argued for it collapse (undo belongs in an undo stack — Apple owns the gesture at OS level and can promise it, the web cannot, so build undo rather than rent its silhouette; a copy confirmation belongs on the button that copied). The refusal owes a Button done state, recorded open, plus one case it does not yet reach, found in our own builder: **the system refusing an act, with a reason and no field to hang it on**. **The background job is not a message** (Kushagra, from Womp, naming the hammer before I could reach for it): a message is an event, a job is an object whose artefact outlives it; "people miss it" is five misses and only one is a notification; every OS solved it with a LOCATION rather than an announcement, which inverts the failing property (a toast leaves whether you looked, a badge waits), and the real repair is usually the data model — a durable result makes the moment free to miss. **This is the first forcing case Badge has had.** `Callout` and `Banner` die as names; §11's three status rows became one plus a refusal.

**Field shipped the same day — repetition's twelfth entry, +28 bytes gzipped, the additivity curve's strongest point.** Four exports over Base UI's Field; it passes §10's anatomy criterion by the widest margin in the library (label association by id, `aria-describedby`, the announced error — three non-visual forcers, so no part of the arrangement is a preference). The stylesheet is a column and a gap: the parts are `Text` at roles §15 already designed, so it names no colour, no index and no family. **Order is label, control, description, error since 2026-08-21** (Kushagra; it shipped with the description ABOVE for one day) — the label sits on the control it names and everything about that control pools beneath it. The reversal is a RETRACTED ANALOGY, not a change of taste: §29's before/after rule orders things met at different moments, and a field is met all at once, so position inside one unit is PROXIMITY rather than sequence. Rejected: the error directly under the control (an arriving error then displaces the description). GOV.UK's magnification cost GREW with the change and is recorded in §28 rather than buried — bounded by `aria-describedby` carrying the description to AT from any position, and by every peer but GOV.UK putting supporting text below. Stacked always, `orientation` refused on Slider's precedent (a horizontal field is a designed grid, not a direction flip). `FieldControl` is not exported — every Kookie control already IS a Base UI input reading Field context by itself, so it would be a second spelling of the control already standing there (`Progress.Track`'s call). **The one genuinely new mechanism is the size supply and its bounding is the work**: `size` reaches the control by CONTEXT (`system/control-size.ts`), read by eight controls and deliberately not by Button, Card or the type family, bounded three ways because silent inheritance is this project's most-repeated defect — ONE provider (no Theme, no Box, no surface), an explicit prop ALWAYS wins, and `null` rather than `"2"` as the context default so "nobody asked" can be told from "a Field chose 2". The label's step is an IDENTITY, not a table (the control size join is itself the identity map), law-read against a mounted control. 18 mounted laws, eight sabotage passes; **two of my own laws were wrong before they were right, both in the shapes the audits keep naming** — the no-leak law compared a hostile-axes subject against a default-theme twin (measuring compact-versus-default, not supplied-versus-unsupplied: a law's two sides must differ in the ONE thing it is about), and the destructive-ink guard compared `var(--color-text)` resolved INSIDE a toned element against itself, since a stamped tone re-scopes the tone-less role names onto the family's ink trio. `Form` is deferred, not refused. **`FieldItem` joined 2026-08-21 (+46 bytes gzipped), and it closes a hole the mark family has carried since Checkbox**: a Field names ONE control, so a group's options had nowhere to carry their own name — every named checkbox in this repo was a hand-written `id` + `htmlFor` at the call site, the exact wiring Field exists to remove. Base UI's `Field.Item` opens a fresh naming scope (measured: a label inside names the mark beside it, and descriptions CHAIN — an option is described by the field's line then its own). Two-column grid, mark then name-over-explanation, so the explanation aligns with the WORDS; the mark needs no alignment rule because `--mark-N` IS `--line-height-N`. The field's column flipped to `align-items: stretch` with the LABEL as the one exception — the old `flex-start` + `.kui-control` opt-in reached nothing through a group primitive, so items shrink-wrapped. **Recorded open, measured**: with every control inside items the field's own label renders `<label for>` pointing at nothing (Base UI mints a control id up front; items capture the registrations). Nothing is announced wrong — the group is named by `aria-labelledby` — so the cost is an HTML validity error and an inert click; unfixed because the outer label renders above the items and the only automatic fix swaps the element after mount, which is a hydration mismatch. **Two of my own laws could not fail and both sabotage passes caught them**, the degenerate-fixture rule twice in one component: "an item spans the field" mounted its field inside a Flex, so both sides collapsed together under the broken spelling; and the item-label click-target law compared against the ITEM, which the mark's own column already makes it narrower than. The builder's grammar law also caught FieldItem's first preset placing a `Radio` with no `RadioGroup` ancestor — an export that would have thrown.

**Notice shipped 2026-08-21 (§29) — repetition's thirteenth entry, +81 bytes gzipped, and what it is law-tested on is an ABSENCE.** The root is a `.kui-surface` at the tone-forward rung, so fill, corner, padding, edge and the foreground re-scoping arrive from the surface layer (a destructive notice tints its box AND its words with the component naming no family); `notice.css` is a flex row. No `position`, no `z-index`, no `float`, read as computed values — taking layout space is the whole structural difference between this and the toast refused beside it, since a condition that is true NOW may not hover over the content it is telling you about; the displacement law measures the same claim from the other side. At most one RESOLVING action and one ACKNOWLEDGING dismissal; `onDismiss` is a callback with no internal state, because a notice that dismissed itself would forget on reload. **The system-drawn ✕ contradicts §24 deliberately and the reason was checked first**: Dialog refuses one because it has two other ways out and a corner glyph competes with its one action zone, and neither holds here — the ✕ is the only route out, its position is conventional, and making every call site compose an icon-only Button means every call site re-derives the accessible name (`dismissLabel` is where a non-English app states its word). Rests neutral: tone is the CATEGORY, not the volume. `--kui-sf-icon` joined the surface size join, publishing the same ladder `--kui-ct-icon` reads so a symbol on a surface and one in a button at one index are one box. Parts refused (nothing non-visual forces a title — a notice needing paragraphs is a Card), more than one action refused, and a `transient` prop that would make this and a toast one component refused as structural rather than cosmetic. **16 mounted laws, eight sabotage passes — and one sabotage SURVIVED the first run**: the long-word law's fixture was hyphenated, browsers break after hyphens, so removing the flex floor changed nothing; with a genuinely unbreakable word the same sabotage overflows by 277px. That is the degenerate-fixture rule caught in a component built the day after it was written down. **Three things were then wrong on the day it shipped, found by MEASURING a mounted one rather than re-reading its source, and fixed the same day (Kushagra): it cast a Card's shadow byte-for-byte** (§5's plane criterion, already the answer twice — a notice is a marker ON a plane; the pool survives because on glass it is what the material HAS, not what the app SAYS), **the material never reached it** (default correct by selectivity, but nothing could say otherwise — it takes `backdrop` now, Card's wiring verbatim with GlassScope), and **the words did not scale** while the dismiss button the component places DID (54→114px of strip around 16px of text) — a notice is an alert that does not interrupt, so the index reaches the message through the ALERT's own step map, promoted on its third consumer from `system/floating.tsx` to `system/type-steps.ts` as `OWNED_*_STEP`, because the rule is ownership and a notice floats over nothing. The corner was measured (38.7px at size 1 against a 54px strip, clamped to a lozenge) and deliberately left. **And the fixture mistake happened a SECOND time in the same component on the same day** — "a glass strip scopes its subtree" passed with the scope deleted, because a button with no `backdrop` and no region around it resolves solid either way. The rule it earns, stated as a question so it becomes routine: **before trusting a law, ask what its fixture would look like if the mechanism were absent; if the answer is "the same", the fixture is the defect.**

**A dead interactive surface got its appearance 2026-08-22 (§10, Kushagra)** — closing the `:disabled` gap the 2026-08-20 Card review had left open (that sentence is amended above). Measured first: a `<Card render={<button disabled/>}>` computed BYTE-IDENTICAL to a live one, `cursor: pointer` included, because the shared remap is written against `.kui-control` and a card is not one. Four changes, the same four a disabled Button shows — the fill recedes to the dead step, the ink dims across all three rungs (a card's content reads the tone-LESS foreground roles, which the control remap cannot reach, so a dead card had a receded fill under full-strength words), the cursor stops promising, and the cast goes (`0 0 0 0 transparent`, never `none`). The motion stands down on the same selector and the ordering is stated, because the rule is (0,2,0) against (0,2,0) hover and press rules and wins only on source order.

**The floating family was audited 2026-08-22 (ultracode, 16 lenses, one adversarial verifier each; 102 findings, 96 surviving) and the ten that reach a person are fixed — with all four suites GREEN throughout at 199/200, which is the finding behind the findings.** The headline is one mistake three times: **the flight does not own the element's style.** Base UI writes five properties imperatively on an item-aligned Select; the runner borrowed and restored exactly ONE, with a paragraph beside it explaining why an inline declaration beats every rule, and twelve lines later DELETED another — so a 30-row select settled 910px tall inside an 800px window with `clientHeight === scrollHeight` (not scrollable, a third of the list unreachable, chosen row 163px off its trigger), and the flight's `overflow: clip` never reached a select at all, so the placement's own scroll offset was taken and clamped away frame by frame. Snapshot-and-restore now, on all three. **The width step returned and had TWO causes**: the press is a SPRING, so the trigger's scale at measurement time depends on how the panel was opened (`scale: 1` on a pointer press, 0.975 on `defaultOpen`) — which is why the division stays and the 2026-08-17 reversal that removed it was argued from a false premise (`getScale` appears in Base UI's positioning file zero times) — and floating-ui keeps re-measuring the anchor as the press travels, so `--kui-anchor-w` is now the one flight var the release KEEPS. **Reduce Motion was producing the largest movement in the family**: `margin: 0` in the guard stood down nothing (the only auto margins in the poses are on the BODY elements) and beat the `margin: auto` that CENTRES a dialog, four specificity steps to one, while the guard's `transition: none` never reached the exits at all — measured, a closing dialog at `24,24 560x752` for ~130ms. **Escape deleted a Menu's exit** (`dismiss` means a KEYBOARD dismissal in Menu's store; the rule's comment described Popover's and had the two backwards), which also cost a CONTROLLED menu its entry after the first Escape. **A dismissed alert kept the page unclickable while invisible** — the exit's keep-alives gate Base UI's unmount, and nothing anywhere stood `pointer-events` down. **A mirrored submenu grew backwards into its parent** (`inline-start` absent from the flight pin, `data-align` read on the wrong axis for side placements — the transform-origin block 230 lines above got both right). **The lens rebuilt 27 maps per glass menu open** and leaked one filter per pane. Plus menu's uncapped width floor, the alert's false clip warning, and a dangling `--kui-floating-gap` the dangling-var law's own allowlist vouched for. **Two fixes were built, measured as no-ops and REVERTED** rather than left in looking plausible (shortening the overlay exit's clocks; any z-index for the un-rooted portal's missing stacking frame, which is real and gets a dev warning instead, because every repair from inside the portal is the ladder §20 rejected). **The reason 200 laws were green is one line of test data**: both select fixtures used eight options, the one shape where the item-aligned mechanism is a no-op, blinding three laws at once. Widened to thirty, a law failed on CORRECT code — it asserted `scrollTop <= 1` on every frame, which an item-aligned select is supposed to violate, so it had codified the defect as a requirement. Every fix falsified against the pre-fix code; the new laws were wrong before they were right in this repo's own recorded shapes (stale-popup selection three times, a panel dismissed while still POSED, a wait that resolved before the entry began, a 0.5px tolerance against a 10px defect, two unfalsifiable lens laws caught by their own sabotage). Budget 31,407 → 31,628 gzipped.

**Composer spec'd 2026-08-23 (§30), after the v1 Chatbar was audited and the whole composer family was reconciled** (`docs/handovers/` carries both). It is a SURFACE, decided by what it holds: a TextField holds shrunken slot controls, a composer holds full-size Buttons, which is a box containing controls. So its inset is surface padding (the attachment strip needs `m="bleed"`, which only reads `--kui-sf-p`), its corner is the surface band (it grows like a TextArea and is not on the height ladder — the control band would repeat the checkbox-corner fraction), and the squircle arrives with it. Three exports: `Composer` (a real `<form>` — both references are, v1 is a div with `onSubmit` stripped, which is why v1 needs `submitOnEnter` and they do not), `ComposerInput` (a BARE textarea, not our TextArea, which would show two boxes; it exists as a part only because the ring keys on it) and `ComposerSend` (ONE button, four meanings by `status`: ready sends, submitted spins, **streaming STOPS**, error retries). `sendMode` dies with it — v1's prop decides whether the button is visible and never what it means, so a person cannot stop a generation, the largest gap in v1 and one the audit missed. **The disabled arm (2026-08-22) applies unchanged; the RING does not** — all six interactive-surface rules sit on one selector meaning *pressable* (cursor, 1px rise, press sink), and a composer must have none of those. That selector is doing two jobs and the composer is the first thing wanting focusable-without-pressable; it SELF-KEYS rather than splitting the layer, on TextArea's own second-member rule. The ring is `:focus-within` narrowed to the input (TextField's 2026-08-05 repair — a hosted button must not light the whole box), not `:focus-visible`, because a composer's focus is a mode. **Growth is `field-sizing: content`** (Baseline 2026-06-16), which retires v1's per-keystroke `getComputedStyle`/`scrollHeight`/rAF/`ResizeObserver` against the no-JS-at-interaction-time rule — the mechanism did not exist when v1 was written; Safari takes a `rows` + `max-height` fallback and both paths get a law. Refused: `ComposerRow` (a Flex — five of v1's eleven parts were layout wearing a part's name), the attachment tile (it ships SEPARATELY as `Attachment`, because a file about to be sent and one already sent are the same tile, and it takes `state` as a prop the caller sets — the system draws the state, the app owns the file, which is what v1 inverts by minting object URLs and revoking them one commit after handing them to `onSubmit`), and **compact** — deferred not judged, since assistant-ui ships it and the axis is real, but it only means something when there is something to collapse and this ships none of it; when it lands it is content-driven with no blur handler emitting one attribute, never v1's focus-driven React branch. Takes `backdrop` with Card's wiring and scopes its subtree solid. Five laws named as load-bearing before any code, each a shape this repo has already been bitten by. **SHIPPED the same day — repetition's fourteenth entry, +121 bytes gzipped**, 15 mounted laws and nine sabotage passes. **One sabotage SURVIVED**: deleting `overflow-y: auto` from the text changed nothing, because the HTML rendering spec already gives a `<textarea>` `overflow: auto` — the declaration restated a default and the law pinning it could not fail, so both went; the CEILING is what makes the overflow reachable and breaking it fails two laws. Measured, not assumed (a bare textarea computes `overflow-y: auto` and `resize: both`, which is what proved `resize: none` load-bearing where its neighbour was not). **A docs law caught what neither file could see**: the specimen frame decides by two implementations that must agree — one reads source, one renders the module — and a composer IS a `.kui-surface`, so the frame would have put a pane inside a pane with the nesting written in neither file. **The INDEX was wrong the same day, in two directions, and Kushagra found it by asking why a size-3 composer left its buttons at 2.** Measured at 1 against 3: the pane's padding moved, a `TextField` in the row moved, a `Button` did not, and the composer's OWN TEXT did not (`composer.css` hardcoded step 3 while the spec and the registry both claimed size set the type). Four elements, three behaviours, the third chosen by nobody — `ControlSizeContext` reached the row's field controls as a side effect of being Field's mechanism. **Resolved by a rule that already existed twice: Dialog prices the box alone because it does not own its content (§24), AlertDialog prices everything because it does (§25) — ownership is the difference.** A composer owns its pane and its text and nothing in its row: the provider is DELETED (a Button and a Select under the text now behave identically), and the text takes `OWNED_BODY_STEP`, so a composer, a notice and an alert at one index are one typography. The input wears `kui-type` and stamps the step (the map keeps its single TS home rather than gaining a CSS copy), and the box is bounded in `lh` — one line and eight — so it is the same number of LINES at every index where the pinned length made a size-4 composer shallower than a size-1 one. **None of the original fifteen laws read anything at two indexes**, which is why nothing caught it; three do now, each falsified by re-committing the fault. −22 bytes, because the fix deletes three declarations. 1,709 of 1,710 package laws green (the one red is `motion.browser.test.tsx`'s whole-pixel `outline-offset`, verified identical on clean main — the 2026-08-22 environmental note, Chromium 141 aliased in for the pinned Playwright build; a menu law rotates in and out across full runs, the documented flake set).

**One stroke literal was two weights, because it was written on two grids (2026-08-23, §4, §8, Kushagra: the icons "are too thin... Usually 1.75 stroke width works").** A stroke is stated in VIEWBOX units, so the painted weight is `stroke x box / viewBox` — and the package draws its seven glyphs on a 16 viewBox while every icon set the docs could install draws on 24. Both said `strokeWidth="1.5"` in ten hand-written places, which in a 16px box is 1.50px against 1.00px: **a select's chevron had been painting 50% heavier than the Hugeicon in the button beside it since the day the docs installed a set**, and the thin half is what he was looking at. Raising the shared literal — the first answer, written and thrown away — would have WIDENED the gap to 1.75 against 1.17. What is constant is the PAINTED stroke, so `iconStroke` (1.75) states the number for the ecosystem's grid, `iconGrid` (24) names that grid, and `glyphStroke` derives the same weight for 16 — a derivation, never a second judged number. **`iconStroke` and `iconGrid` are PUBLIC**: §8 ships no icon set, so a chevron that does not match the app's glyphs is the one mismatch a consumer cannot fix from the outside. 1.75 is v1 KookieUI's own settled number and the ecosystem brackets it (Heroicons and Hugeicons 1.5, Lucide and Feather 2); no arithmetic is offered, because a font stem is rasterized with stem darkening and an SVG stroke is not, so matching text by calculation gives the wrong answer. It is a taste number, judged by eye like every other number in `config.ts`. **The mounted law reads PIXELS and its fixture is the load-bearing half**: two package glyphs cannot disagree, so the subject is a real package glyph against a 24-grid glyph carrying the public value, in the same box, at every index; sabotaged with the shipped literal it reproduces the defect exactly (`expected 1.5 to be close to 1.1666`). A source law walks every shipped `.tsx` and fails on any hand-written stroke, because the next chevron is the one the browser law does not mount. Doc-code drift closed in DECISIONS §4, whose worked painted-weight table was computed at 1.5-on-24 — under a sentence reading "the stroke needed no answer of its own... it scales with the box", which is true of the LADDER and was read for three days as true of the VALUE. **The composer's send button defaults LOUD the same day** (Kushagra: "why is send button not loud?") — it shipped taking Button's `medium`, an omission rather than a decision, and §25's AlertDialogAction argument transfers verbatim because the rule is held by ANATOMY: a composer has exactly one send, the component places it, everything else in the row is the caller's. A DEFAULT, not the alert's pinned identity — the row is the caller's, so the control the system puts in it stays theirs to re-rank. Zero CSS either side; budget unchanged at 31,750. **A disabled composer kept your words at full black, and the spec said it could not (2026-08-23, Kushagra: "we fixed this bug in card too, card can read disabled, why did we use text field rules?").** Building the component's preview page left its States section with nothing to draw, and my first measurement compared PANES and reported that the panel does not dim. His question sent me to the right element: a disabled TextArea greys its words (0.2697 → 0.7625) and a disabled composer greyed NOTHING (0.1202 both) — text you typed staying at full contrast in a box that had stopped taking keystrokes, `readOnly`'s own 2026-08-05 shape and worse than what I had reported. The cause is the 2026-08-22 Card finding verbatim (the shared remap rewrites TONE roles; this input reads the tone-LESS foreground roles) and the price of §30's own decision that the input is a bare textarea rather than a `.kui-control`. **Refuted by measurement: splitting the pressable selector** — my first proposal, framed correctly (that selector answers *can you press this* and *can this be dead* at once) and aimed at the wrong thing, because a disabled TextArea's fill is byte-identical to a live one, so grey the words and leave the box IS the library's answer and the pane is right as it stands. `composer.css` states it on `:disabled`, placeholder standing down beside it; three laws, three sabotage passes, the value law's fixture a mounted TextArea rather than a colour literal so an invented dead grey fails as loudly as no grey. §30 had read "applies unchanged… its selector widens by one entry" — a widening that never happened, written from the arm's intent rather than the elements it names. **Composer also got a dedicated preview page the same day** (the sixth through the per-component structure), and looking at the rendered pixels caught four demos that could not show what their captions claimed — a repeat of the ladder above it, two cells identical because the overflow scrollbar hides at rest, a grid stretching the small pane into fake padding, and quiet helper text under a quiet placeholder. Budget 31,750 → 31,760. **And the size index was reversed a SECOND time the same day** (Kushagra, reading the new preview page's Sizes section: "why is size index not increasing text and buttons and send button etc" → "everything scales, explicit sizes still win"). Measured across four indexes: padding 12/16/24/32, text 14/14/16/16, every control 32 — the padding moved four times, the text ONCE, no control at all, so three of four steps did nothing but pad the box. Two causes, neither accidental: the text rode `OWNED_BODY_STEP` (`{1:2,2:2,3:3,4:3}`), which prices a message the SYSTEM writes to you and compresses on purpose — a composer holds a message YOU write and is a text input, so it takes the identity ladder every other input takes (12/14/16/18); and the row did not move because the composer deliberately withheld `ControlSizeContext`, its own comment giving the reason — that context "would resize a Select while the Button beside it kept its own index". **That was a fact about Button, so the change is Button's**: `control-size.ts` excluded it arguing "a button beside that control is a sibling in the form's layout", which is right about a button OUTSIDE the Field and is exactly the button React context cannot reach — inside a unit the rule inverts, and the file already required a call site for widening. Hosted slot buttons are unaffected by construction (`--kui-ct-hosted-height` is declared on the slot, so CSS sizes them regardless of index), Field's laws pass untouched, and exactly ONE law failed: the one written that morning to hold the previous answer. **Both spellings shipped green, and both were caught by a person reading the ladder as a ladder** — 1 against 4 differs under every spelling tried here, so a two-index law cannot tell 12/14/16/18 from 14/14/16/16 and no law read more than two; the text law now walks all four and its sabotage fails printing `14/14/16/16`. Four laws, three sabotage passes; zero CSS, budget unchanged.

**The resting gray lightened at its edge 2026-08-24 (Kushagra: "the text field and hairline feel bit darker than they should be… I see no reason for [segmented] to be any other color than text fields or areas").** Three moves, each one config line or one re-point, LOG carries the full record. The light field edge went a4 → a3 — it now EQUALS the fill and still reads because the border area composites the same alpha twice; the FILL held, pinned from both sides (a2 was "a bit too light" 2026-08-17, and dark's a2 is literally transparent, so the ramp has no half step below — a lighter field means moving a3 itself). The dead border followed (`disabledSteps.light.border` 3 → 2): three mounted laws failed on the edge change because the dead border sat exactly ON the live edge, and the 2026-08-19 rule is recession. And **the segmented track left `--color-track` for `--dress-field-fill`** — the well's one-step-deeper argument overruled; ONLY segmented moves (his call), the slider rail, switch off-track and progress well keep the deeper channel, and what still separates the track from a field is the EDGE (a field wears the dress hairline, a well has none). The WELL law re-keyed to the AGREEMENT with a mounted TextField in both appearances rather than the token name, falsified against the pre-fix CSS. Stale HC prose in config's dress comment closed with it (the edges are reached by the `initial` stand-down, not band membership — stale since 2026-08-17).

**The grips went flat on glass the same day (§5, §10, §26, Kushagra's glass model: "When glass, segmented control's thumb should render as neutral gray like hover in menu… switch's thumb goes flat also, and so does slider").** Two rules, both in surfaces.css, both falsified three ways. A grip in a glass pane loses its cast — "casts always" is about the WORLD, and glass is a MATERIAL, the axis the sentence was never about; the grips left `--control-chrome` for `--grip-cast` on 2026-08-17 precisely so no world switch could strip them, which also walked them out of the one-lift-per-pane stand-down that already governs button casts (slider.css stated the escape as a feature: "why one-lift-per-pane cannot strip it" — reversed, and Apple's flat liquid-glass handle retires its "every platform" clause). The stand-down is a `--kui-grip-cast` hook consulted before the role in all three grip sheets — the keycap's own mechanism, so no specificity fight exists. And when the segmented TRACK is the glass, its thumb trades `--color-thumb` for `--material-row-wash` (the exact token a glass menu lights a hovered row with — agreement by construction) under the full ink, since `--color-thumb-label` was minted against a near-white fill the thumb no longer has. The in-pane and is-pane cases are told apart by the FILL (inside someone else's pane a grip keeps its pigment and loses only its lift — his alpha rule: glass never renders on glass, it renders the solid appearance at the pane's alpha), and the laws assert both fills so a rule washing both would fail. The wash rule's placement was forced by two laws: a component sheet may not name a material, and recipes.css's material region may not touch a colour or a label — the second FAILED the first placement and was right to. Six new laws; three sabotages each caught by only the laws written for it.

**Six components took their own preview pages 2026-08-24 (Kushagra: "I need to see text field and text area and tabs etc indepdently, in their own preview pages like we added for card etc to really judje it").** text-field, text-area, tabs, segmented-control, switch and slider ported into the per-component structure (registry 6 → 12); the hand-written sections in specimens.tsx are DELETED rather than left beside their replacements. The seven-section contract forced each one to declare what it does not have — four `tones` absences for four different reasons — and the demos are written to make the day's two changes judgeable rather than merely present: the field's resting gray beside the medium Button that shares it in both appearances at once, the segmented track beside a TextField and a Slider (one shared gray, one deliberately deeper instrument channel), and the glass grip's two cases side by side. Two findings fell out of the writing: `BedSurface minHeight="none"` is invalid CSS, so three composer beds had been falling back to the 240px default instead of hugging (fixed to `auto`), and **a disabled `Field` dims neither its LABEL nor its DESCRIPTION** — measured byte-identical to a live field's while the input inside greys correctly (0.2697 → 0.7625), the Card/Composer finding's fourth appearance (the shared remap rewrites TONE roles; these parts are `Text` reading the tone-LESS roles). Recorded, not fixed — a package change out of a docs change.

**The glass contract LOCKED 2026-08-24 (Kushagra: "We need to decide and lock what glass means, every element cant act differently when its glass, this is a major red flag for me"), and the field family's edge became the RING.** Measured first: a glass Card and Button wore the conic ring while a glass TextField/TextArea wore the pre-lab flat hairline (--material-*-edge, white 60% — "the border is weird") and a glass segmented track wore nothing — three renderings of one material. §10 now states the contract: five parts (veil, filter+lens, ring, rim, matter+one-lift), every pane resolves all five identically, a component may vary WHERE a part is painted (platform constraint) never WHAT it is (a value). The ring could not move to the fields as the ::after (a form control renders no generated content — measured, the pseudo computes and never paints), so the field family paints the SAME ring token as a background layer under `background-clip: border-area` (supported in the pinned Chromium, probed before building) with the border transparent through the panes' own HC hop; outside the @supports guard the flat hairline stays as the stated fallback. The segmented track joins through the button's ::after (its border is stood down by width, D2 — border-area has no band there). ONE generated HC arm hands the pigment edge back and stands the ring down together; the state arms kill the ring beside the glass edge. The old field law asserted the flat edge — the defect stated as a guarantee — and is re-keyed to the AGREEMENT: the Button's resolved conic must appear verbatim in the field's and textarea's background stack and byte-identical in the track's ::after. Four sabotages, each caught by exactly its own law. 1,892 laws.

**On glass, hover was shouting over the selection — and painting OPAQUE (2026-08-24, Kushagra from the preview: "Week is selected but its barely visible on thin, month is hover and its extremely dark, so much more contrast than the selected thumb").** Two defects, one screenshot. A hovered segment on a glass track measured FULLY OPAQUE (neutral-3 in light, near-black in dark, no alpha at all): the 2026-08-19 opaque-twin re-point is declared on the element carrying `[data-material]` and those roles INHERIT, so a glass track handed its segments an opaque source and a segment — having no material of its own — had no veil to mix it back down. The silent-inheritance trap the `--kui-h` rename and four `@property` guards were each written for, arriving where nobody guarded: parent control to child control. Fixed in the pane's rules, never by registering the tone roles non-inheriting (a stamped tone MUST reach a descendant). And the thumb was painting the HOVER value — it shipped that morning as `--material-row-wash`, the glass menu's own hover, so persistent and transient were one colour by construction and no tuning could rank them; `--material-grip-fill` joins the glass ink ladder (white in both modes where the wash is a darkening — the relationship the solid control already has). §10's contract gains its fifth clause: **a control paints its states in the pane's currency, and the persistent state outranks the transient one**. The law reads a RANKING (alpha compared — the two are opposite directions in light, so a token name would not catch them being equal) plus `alpha < 1`, which is the half that catches the leak. That morning's own law asserted thumb ≡ row wash — the defect stated as a guarantee, one commit old, re-keyed. The fill-triple law caught the fix's first spelling re-sourcing two states of three.

**The glass lip was white on white, and no law asked whether it was VISIBLE (2026-08-24, Kushagra with six screenshots: "Segmented control isnt visible on card, in dark mode it is fine, text field has the same problem, and card also… See iOSs edit button, segmented control, if they didnt have that hairline, they would blend with background too").** Every stop of light's ring was white — 0.95/0.34/0.1/0.26 against a 0.9869 page — a lit lip photographed against a dark room: over a photograph it read, over the page nothing. The MODEL is corrected rather than the value nudged: a lip is bright where light reaches it and dark where it does not, so light keeps its catch and takes pigment through the shade arc and flanks (anchored on `glassInk.light.border`), compositing over the pane's own veil so the dark arc reads whatever is behind it — one value fixing the segmented track, both field controls and the card at once, which is the lock paying for itself. **The gap is the finding**: 1,895 laws green over an invisible edge, because every law that could have caught it asserted AGREEMENT (a field's ring equals a button's, the ladder is monotone, HC stands it down) — all true, of a ring nobody could see. `material.browser.test.tsx` now reads the ring's stops against the page and asks whether any arc is far enough to be a line, with its threshold set from measurement in BOTH directions (defect 0.0125 thin/regular and 0.0773 thick — the faint coloured hairline Kushagra had already spotted as the only visible edge — against 0.1480 after; floor at 0.10). **The instrument was wrong twice before it was right**: `colorOn` resolves a COLOUR and a conic-gradient is an image (came back transparent), and then a bare `[\d.]+` sweep took the *3 in display-p3* as the red channel — the 2026-08-08 calibration lesson reproduced by its own author, the tell being 0.3939 scored identically before AND after the fix.

**The grip repeated the ring's mistake, and the glass rules had never met an on-glass member (2026-08-24, Kushagra: "the selected thumb is still very white, see iOS's. and on a card theres no visible difference still").** The grip went WHITE that morning on the solid control's relationship (track recedes, grip catches light) — the same error as the white lip three hours earlier, because a light pane has no headroom above it; iOS's chosen segment is a subtle grey DARKER than its capsule, so light draws with shade (black 0.14) and only dark keeps the catch (white 0.26). And every glass rule was keyed to the three THICKNESSES — the list meaning *this element IS a pane* — so a segmented control inside a glass Card (`on-glass`) matched none of them: thumb fell back to the solid `--color-thumb` and rendered PURE WHITE on a white pane, track had no boundary at all. They key on `[data-material]` now (every glass state, never a solid control), and the track — whose border is stood down by width, so neither the edge chain nor `border-area` has a band — takes the ::after annulus filled with the pane's pigment hairline rather than a conic lip, having no thickness of its own to claim. **The value law could not fail and its own sabotage caught it**: reverting light's grip to white broke nothing, because the ring laws only read the ring; the grip now rides the same law, stated as the principle (each mode's states move away from its own ground, in its hover's direction, and further) so values stay taste and direction stays law. §10's fifth clause gains both halves.

**The glass polish pass ran 2026-08-24 (Kushagra: "I look at iOS liquid glass and it is MILES ahead, and i see no reason why you and I can't reach there"), and the specular became a BAND.** The diagnosis, from Apple's own material description (WWDC: previous materials *scattered* light, Liquid Glass *bends and concentrates* it) plus the serious recreations: our glass still bought legibility with scatter — and three things were already judged and lost. The lab's LOCKED refraction (2026-08-14: concave, bezel 120, IOR 2.4, "the material is DIAMOND") was never judgeable in the package because the bench only moved `boost`/`fringe`; the lab's judged dark-control ring ("not enough" → roughly double) never left the lab; and kube's rim re-emits the displaced backdrop saturated 4-9× where ours was only white or pigment. Shipped: the **GLINT** — a per-box alpha mask from the lens's own SDF, masked over the mode's ring conic on a `::before` (geometry JS at the lens's seam; colour, stand-downs and mode flips all tokens through `--material-ring-opacity`), which also gives **Safari and Firefox the specular** for the first time; the **rim-saturate stage** inside the lens (the edge catches the backdrop's own colour; invisible over a neutral page by construction, the pigment arc's finding from the other side — built at 4, judged OFF the same day, ships at 0 with the bench dial as the road back); **`--material-*-ring-control`** (dark = the lab's doubled row, light = the pane's verbatim from one emission source); and the bench widened into the **material bench** — `__retuneLens` (dunder-fenced package seam) regenerates every mounted map live, so bezel, depth, IOR, profile, concave, pre-blur, band, feather, rim-saturate, veil, blur, saturation, contrast, sheen and edge are judgeable at /preview, every dial one config line, 1.0× restoring the package. The spectral fold stays OFF the band (corrected same day: thick's lip split read as pink haze at band width); a bare `<textarea>` takes the band FLAT (same day, "text area still looks old": no pseudo paints there — re-measured — so `--material-glint-flat` gradients sample the same curve in its own stack, width from the hook's `--kui-glint-band`, stood down through `--kui-ct-glass-glint` with the ring; the corner wrap is what the platform withholds). Rim-saturate was judged OFF and the band re-priced wider and tighter (1.5×, feather 4) within the hour. Refused with reasons: backdrop sampling (CSS cannot read one), the pointer-tracked rim (still), materialize-by-lens-ramp (per-frame filters, the 2026-08-22 finding). Laws: four sabotages each caught by exactly its law; the first fixture mounted a bare Card under a glass theme — SOLID by selectivity, the degenerate-fixture rule caught by its own falsification run. Working lesson recorded: a sabotage on uncommitted work is reverted by inverse edit, never `git checkout`.

**Then:** repetition across the component set. **The eye pass was never a discrete event, and saying it was pending is the claim that went stale (2026-08-23, Kushagra: *"Dude how else have I been giving feedback. Ive judged everything we have so far"* — and he is right).** The 2026-08-04 framing was that taste is the LAST layer, added once the infrastructure is done; what actually happened is that every value was judged in `/preview` and `/matrix` the week it shipped, which is what most dated entries in this file record — the icon box that "doesn't seem to scale", the glass that "looks cheap and incorrect", the segment label that vanished, the lens at twice the bend. So `v0` is deleted as a label: it meant "designed, not yet looked at", it was carried at ~60 sites in `config.ts` and the stylesheets, and at three of those it sat in the same sentence as Kushagra's own name and the date he decided the number. The fact those labels were gesturing at is true and now has ONE home, in `config.ts`'s header: every number in that file is taste, one config line each, and moves whenever the eye says so. **What is genuinely unjudged is two things, and they are named where they live rather than in a list that rots:** the narrow window boundary (48rem, inherited from the type band — its wide sibling was judged by eye 2026-08-05 and this one never was; a shell exists now, so it is judgeable), and a chroma family's loud ink in dark (~66 against medium's 60, so a coloured ladder's top two rungs read as one — the fix is a per-family loud, the exception the 2026-08-10 solve removed). The §14 slice's two standing debts are both closed: dark SSR 2026-08-05 at apps/docs, and the motion system 2026-08-09/10.

**Four components shipped 2026-08-23 — Badge, Row, Popover, Tooltip — and three of the four fired a PROMOTION or a split rather than only adding a component.** **Badge** (+17 bytes, the cheapest entry the curve has recorded) is the inert-atom family's THIRD member, so the family rule fired: the fill, the corner and the inherited-size arm moved out of `code.css`/`kbd.css` into `.kui-atom`, and the one-line box Kbd designed into `.kui-atom-box`; Kbd's own laws needed not one edit. Tone is the category, not the volume (§29's sentence one family over) — no fill rungs, no `variant` — and it is FLAT where a cap is raised, law-asserted against a mounted Kbd. Its sabotage pass caught a defect in a law that had shipped six days earlier: the floor law's fixture used `1` (1.634em, CONTENT-driven) against a 1.6em floor, so deleting `min-inline-size` moved nothing it could see, and Kbd's own floor law had the same blind spot with `K` — **copying a law copies its blind spot, so a copied law owes its own sabotage.** **Row** (net **−17 bytes**, the first NEGATIVE entry) is §21's fourth named row, the one that lives outside Menu and Shell; two screens in this repo were hand-rolling it, one painting `var(--neutral-a3)` inline for its highlighted row. What lights a row is now a per-INSTANCE claim, not a per-component one — the same Row is roving in a palette and static in a settings list, so no component boundary can answer it: `Row` stamps `data-hover-lit` when nobody else is driving it and stops the moment a caller states `highlighted`. The marker is OPT-IN because a menu LABEL is a `.kui-row` that is not a control, and `shell.css`'s private `:hover` rule is deleted — the promotion its own comment scheduled. **Popover** (+14 bytes) is the floating family's first member whose CONTENT the system does not own, and it split `kui-floating`: that class was carrying *where a pane sits* and *what a pane holds* at once, and measured through the concentric arm a popover came out 42/54.25/71.75/89.25px against a Card's 38.7/51.6/64.5/77.4 — rounder than a card at every index, matching neither. The criterion was already written one join below ("nothing about it hugs rows, so there is no concentric arithmetic to do"), so the arm and the squircle bump both key on `kui-floating-rows` now. Its modality law took three attempts: scroll lock is unobservable (Base UI only locks a scrolling document), inertness is unmarked, and what is always true is that a dialog renders a scrim and a popover renders none. **Tooltip** (+117 bytes) closes §20's enumeration and builds §11's one described-but-unbuilt identity: the inversion is `--color-text` as the fill and `--color-surface` as the ink — the mode's own two ends swapped, correct in both appearances with `contrast="high"` free. A `<Theme appearance>` flip cannot work, because under `appearance="inherit"` no React code knows the mode. `children` is a `string`: an inverted pane cannot invert a subtree, since a component that stamps a tone re-declares the ink roles on its own element (measured — a `Kbd` in a tooltip vanished). Every refusal is one rule restated — a tooltip may only say what its control already says — so it is `aria-hidden`, needs no touch story (§17/§18's hover-reveal question answered rather than deferred), and takes no size, tone, emphasis, material or per-call-site delay; the delay lives on `TooltipProvider`, which also groups. **Two fixture defects fell out of the Popover split**: the menu and select agreement laws hand-copied the popup's class list, and repairing that broke the RTL law's anatomy selector by leaving a spare portal in the document — both twins now take their identity from the panel they compare against. Budget 31,647 → 31,778 gzipped; 1,771 package laws, 525 docs laws.

**Then:** repetition across the component set — taste is the LAST layer (Kushagra, 2026-08-04: if the infrastructure is right, taste can be added later; every v0 value is a config line by design). The eye pass comes when the structure is done, and its accumulated list is: surface padding, layout-space picks incl. the gutter-band hold, surface radius band, material recipes incl. the new edge/rim and `alphaHigh` triples, elevated chrome, the audit's dark focus ring, the type/ink ladders, `pxPill` and the checkbox's full-radius cap, the switch's width ladder, inset and well pairing, the window classes' boundaries once a shell exists, and both type bands' picks + thresholds. Plus the §10 deferrals still open (Theme material policy clamp, brightness-floor branch — the material border edge closed above). The §14 slice's two standing debts are both closed: dark SSR 2026-08-05 at apps/docs, and the motion system 2026-08-09/10 (paragraph above), whose own v0 numbers join this list.

**TextArea wears TextField's wrapper since 2026-08-25 (§4, §10, §11), and the flat-band approximation is deleted whole.** The 2026-08-24 glass pass ended with the bare textarea approximating the material's band in gradients, and one day killed the approximation three times: the gradient token baked a ZERO width (substitution-at-declaration — a theme-scope custom property resolves its var()s where it is declared, so the element's own `--kui-glint-band` never reached the paint, and the presence law was green over an invisible band because it read the gradient's existence and never its width), the fixed full-width catch painted a bar of milk over any dark backdrop (the field's white is confined twice, by the conic's arc AND the mask's feather, and a one-dimensional gradient reproduces neither), and the top-centre ellipse that replaced it still could not spell the mask's corner wrap ("Still doesnt have full parity"). The wall is the platform — a form control renders no generated content and a mask on the element takes the text — so full parity within one element does not exist, and the 2026-08-05 one-element decision, priced before the material existed, is reversed: the wrapper wears `kui-control kui-textarea` (every shared field-family arm — states, dress, glass, the `:has()` disabled/invalid — applies unchanged, and it joins the field's ::before band rules), the inner `.kui-textarea-input` is TextField's bare input minus the slots. Parity is held by AGREEMENT laws (a glass TextArea's stack and ::before must equal a glass TextField's, both appearances) rather than tuning — an approximation law can only measure distance; an agreement law makes it zero or red. `className`/`style` dress the wrapper, `ref` reaches the textarea, no slots (an adornment over a scrolling paragraph is still not a designed position), `render` refused on TextField's two-elements sentence. The resize handle was the priced fiddly bit and cost one reversal: the wrapper cannot host it (resize needs an overflow other than visible; a scrollable wrapper is the select flight's own hazard), so the wrapper STATES the axis — keeping the documented `style={{resize:"none"}}` escape — and the inner takes it by `resize: inherit`, verified by a real drag (66 → 139px), not computed values alone. The deletion took the `--material-glint-flat-*` tokens, the generator sampler, the hook's band write and the dead `--kui-ct-glass-glint-bg` slot; −113 gzipped bytes, baseline 32883. Same day, earlier: the spectral fold left the RING too (`ringSpectral` deleted — thick's blue arc read as a coloured hairline on plain grounds, the band had refused the same stops as pink haze a day before), `glint.band` 1.5 → 0.5 (light at the lip), and boost 4 was tried and reverted with a pre-blur experiment (both "go back to what I had" — the wash-out clamp's own warning, and a blur that turns the backdrop's clip boundary into colour the channel split then separates). 1,925 package laws, 533 docs.

**Blocks infrastructure shipped 2026-08-26 at apps/docs `/blocks` (LOG carries the reconciliation).** A block is COPIED SOURCE — shadcn's distribution without its architecture, because the copied file makes no design decisions: the center travels in the package dependency, the copy carries arrangement and behaviour (which a builder document cannot express — hence two block kinds, builder-assembled and source). Blocks live in THIS repo (`apps/docs/blocks/` + registry + route in the docs chrome; a separate repo is how blocks rot), and the old kookie-blocks repo retires. The code block was the forcing case and splits three ways: a block-level code ELEMENT for the package (mono well, pure display — joins the prose-primitive gaps), the tone-derived syntax THEME as the package's colours, and the CHROME (label, copy) as the first source block. The docs' own fence renderer IS that block now (`blocks/code-sample.tsx`, renamed from `code-block` so the element's name stays free), composing a STUB of the future element (`blocks/code-block.tsx`, token-only, held by a purity law) — and **the swap law** asserts the package does NOT export `CodeBlock` yet, so the day it ships, the suite fails and names the one-import swap. **The block was spec'd against the field and built the same day (LOG): five jobs — show, name, take, point, bound** — with the author's annotations (`[!code]` comments + fence meta via a local remark plugin, absolute-path-string in next.config for Turbopack), copy handing over the STRIPPED source derived from the rendered tokens, line numbers as CSS counters, and `maxLines` bounding by SCROLL never clip (v1's collapse defect made inexpressible; expand is a convenience, decided by line count on the server). Refused with reasons: code groups (a separate block composing Tabs and this one), wrap toggle, window frames, language icons, twoslash, preview (the Example frame), streaming. Docs laws 533 → 552, every new law sabotaged.

**Tree and NavTree shipped 2026-08-26 (§33) — one machine, two announcement vocabularies, +158 gzipped bytes.** Spec'd and built the same day, reconciling two sentences that pointed opposite ways (§27: a sidebar tree is a block; the docs site: the package will ship a tree): **the MACHINE is the package's, the INSTRUMENT is a block** — the disclosure state, the ARIA keyboard, the announcement and the indent are a spec rather than a taste, and a spec copied into app source is what rots (Field's argument). The depth-1 "collapsible nav group" is NOT a separate component: to a user it is a tree with subcontents, and carving by names is the industry's inversion (THESIS §2). Data-driven (`items: TreeNode[]`), because the visible nodes render as a FLAT list carrying `aria-level` — the ARIA-sanctioned spelling of nesting, which keeps rows ordinary row-family members, keeps `m="bleed"` trivial and leaves virtualization a door; nested `<ul><li><ul>` was rejected for putting ancestors between the row and the pane. **The indent is `--kui-ct-icon` itself** — one level is one icon box, zero new numbers. `Tree` announces `role="tree"`/`treeitem` with single AND multi selection (`aria-selected` + `data-selected` + the family's medium rung, the same vocabulary a chosen Card reads); **`NavTree` announces as a nav landmark** with `aria-expanded` sections and `aria-current="page"` links, selection refused outright — location is not selection — because the APG separates disclosure navigation from tree views and `role="tree"` on a nav over-claims. The docs sidebar became one NavTree (`currentId={pathname}`, a leaf's id IS its href) and `NavDisclosure` was deleted on its own instruction. **The row family's posture flipped with it (2026-08-26):** a standing row rides the height ladder like the Button beside it, and only FLOATING panels keep the notch — the menu's sparse-panel reason does not reach a permanent column. Refused with reasons: drag (its own pattern, contested accessible answer, the app's drop semantics), cascade selection, async children, virtualization, rename-in-place. Dependencies surveyed rather than remembered: Base UI has no tree, TanStack none, React Aria and Headless Tree both refused as a second runtime dependency. 13 + 6 mounted laws, sabotage passes each caught by exactly its law.

Read order: `docs/THESIS.md` (why it exists, the governance tiers) → `docs/DECISIONS.md` (the spec) → `docs/REVIEW.md` (audit + resolution log) → `docs/ENGINEERING.md` (how we build) → `docs/LOG.md` (dated decision log: why it became this, what was rejected).

`docs/handovers/` is a separate genus and NOT part of that order: one plain-English file per unattended session, written for the human reviewing that branch. It is the only place allowed to repeat what the four docs already say, because the reader has not been in the room. It is never a source of truth — where a handover and a governance doc disagree, the governance doc is right and the handover is stale. Nothing is read out of one and into code.

`DECISIONS.md` is what the system is now; `LOG.md` is how it got there. A choice that was genuinely open and got closed — a reversal, a measurement that moved a decision, a rejected alternative worth staying rejected — earns a LOG entry in the same commit. Tuning does not.

## Non-negotiables

- Components never own outer spacing. No margin prop on any control. The escape is `<Box m>`.
- Tokens only. No raw px in component CSS; every value resolves through a `--*` token.
- No utility classes shipped, ever.
- `size` is an index, not a measurement. Closed unions: `"1" | "2" | "3" | "4"`.
- Appearance is resolved output. Components expose tone/emphasis/material, never raw fills. Elevation does not exist; no component exposes a shadow API. --shadow-1..5 is a fenced resource reached only through the world chrome roles (--kui-surface-chrome, --kui-control-chrome), which Theme depth=elevated declares — a law walks every stylesheet and asserts no rule names --shadow-N directly.
- Semantic references, not numeric coincidence (`--radius-control-2`, never `--radius-2` in a component).
- State styling lives in CSS via data-attributes. No JS at interaction time.
- Never edit generated files (they carry a header naming their source config).
- CSS budget is a CI gate, not a goal. Regression fails the build.

## Working rules

- Every implementation task cites the DECISIONS.md section it implements.
- Refute a proposal before presenting it (Kushagra, 2026-08-07). Attack your own plan the way the audits attack shipped code — scope reach, inheritance/registration, precedence, which existing law it breaks — and present the plan WITH its strongest objection either killed or answered. The rule earned itself the day it was written: a proposed `--kui-ct-chrome` world token sat in the control layer's private registered namespace, the `--kui-h` silent-inheritance trap re-committed by the author who documented it.
- Doc–code drift is a bug. If code forces a decision change, the doc amendment ships in the same commit.
- A component ships with its playground section (Kushagra, 2026-08-08). Every component the package exports is rendered in apps/docs `/preview` — law-checked (`apps/docs/app/preview/playground.test.ts` walks the exports against the rendered tags), so forgetting fails CI rather than memory.
- Follow the build order in DECISIONS.md §14 exactly. Each step has a measurement gate; do not run ahead of a gate.
- Tests assert system laws (token identity, +1-step rule, APCA, budget), never snapshots.
- Commits: conventional style (`docs:`, `feat:`, `fix:`, `chore:`), granular, one concern each.

## Commands

From repo root (pnpm 10 / Node 22; turbo fans out to packages):

- `pnpm run ci` — build + test + lint + budget gate, the full check; run before claiming any task done
- `pnpm run build` — tsdown (JS + d.ts) then Lightning CSS bundle to `dist/styles.css`, then `pack:check` — publint + are-the-types-wrong against the PACKED artifact, so the exports map is verified rather than trusted. attw runs the `esm-only` profile: node10 resolution and `require()` are formally unsupported, not silently untested (LOG 2026-08-05).
- `pnpm run test` — Vitest, two projects: node (generator/config laws) and browser mode (computed values through a mounted `<Theme>`, the standing rule after the audit)
- `pnpm run lint` — ESLint + `tsc --noEmit`
- `pnpm run measure` — CSS budget gate (regression ratchet); numbers live in `packages/ui/budget.json` only. Intentional growth means re-recording `baselineGzipBytes` in the same commit.
- `pnpm --filter @kookie-ui/react run tokens` — regenerate `src/tokens/tokens.css` from `config.ts` (also runs as part of build)
- `pnpm --filter @kookie-ui/react run preview` — emit `packages/ui/preview/density.html`, the size × density matrix for judging the numbers by eye
