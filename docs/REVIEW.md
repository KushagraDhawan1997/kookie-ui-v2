# Review of DECISIONS.md

2026-07-31. Audit of the v2 decisions against measurements of v1 (`~/Code/kookie-ui`) and the site rebuild's decision log (`~/Code/kushagradhawan-v2`). Verdict: build it. Architecture is sound; three defects to fix in the doc before code, none architectural.

**Resolution log (2026-07-31, later same day):** blockers 1-3, typography, density coupling, accent brand fidelity, `contrast="high"`, the DESIGN.md reconciliation, and both build-order amendments are resolved in DECISIONS.md (§2, §3, §7, §9, §12, §14, §15). Still open from this review: focus-visible, disabled/loading, motion, dark-mode SSR, icon sizing, Shell, migration — focus-visible, disabled/loading, motion and icon sizing gate Button (§14 step 5), joined 2026-08-02 by the pointer axis's coarse numbers (§16, §14 step 4b). Dark-mode SSR was passed by Theme and comes due at apps/docs (ENGINEERING.md).

**Second resolution round (2026-08-02, evening):** focus-visible, disabled, loading and the icon box are decided in §8/§4; motion is deferred without gating Button, which ships on §8's single canonical transition; the coarse v0 numbers are accepted as refinable. Button's sole remaining gate is the Theme-as-query-container question (§2). Still open: the full motion system, dark-mode SSR (due at apps/docs), the icon *system* (set contract, stroke, optical centering — only the box is pinned), Shell, migration.

**Third resolution round (2026-08-05):** dark-mode SSR is resolved where it was due — apps/docs exists (bare Next.js; every visible pixel is @kookie-ui/react, no docs framework — LOG). A pre-paint inline script stamps `data-appearance` *and* `data-contrast` on `<html>` — both, because the generated high-contrast selectors assume the two attributes co-locate on one element (§7) — and the root Theme runs `appearance="inherit"` so it stamps neither: one source of truth, no flash, hydration-safe (the store's server snapshots render the toggles neutral and `useSyncExternalStore` corrects them without a mismatch). Still open from this review: the full motion system, the icon system, Shell, migration.

---

## 2026-08-08 audit — Progress, Code, Kbd, Blockquote, and a sweep over Switch/Separator/the look axis

Single-pass, self-adversarial: every finding reproduced by measurement in a mounted browser, and every fix falsified against the pre-fix code before it was accepted. Smaller than the multi-agent rounds and scoped to the four components shipped the same night plus the two that had shipped without an audit. Five findings, two of them repairs, three recorded rather than fixed.

**The one real defect was mine and it was in the newest file.** `Progress` declared `flex: none`, borrowed from Separator on the reasoning that a squeezed Stack must not thin the one element whose thickness carries the information. It does hold the block axis. It also freezes the INLINE one — and the bar declares `inline-size: 100%`, so as a flex ROW item it took a 100%-of-container basis it could no longer shrink: **measured 381px of bar inside a 300px row, overflowing by 81px**, beside an ordinary Button. Separator survives the identical spelling only because its inline size is a hairline. Fixed as `min-block-size`, which states the actual intent — a floor on the dimension that carries the information — and leaves the row free to squeeze a bar that has no intrinsic width.

**The law that let it through was one-sided**, which is the finding under the finding. It asserted "a squeezed Stack never thins it" and stopped there, so it tested the axis that was right and never the axis that was wrong. It is now two laws, and the sabotage run shows them catching opposite mistakes: restoring `flex: none` fails the row law alone; deleting the floor fails the column law alone. **A law about one axis of a two-axis mechanism is half a law.**

**A superseded law survived its own replacement.** `checkbox.browser.test.tsx` carried two focus-ring laws: the D9 fix from the 2026-08-06 round, which names the ring (solid, the system width, the system colour, absent at rest), and the weak spelling D9 was written to replace — `expect(outline-style).not.toBe("auto")`, which `none` satisfies, so the shared ring could be deleted outright and it passed. Both were on the same element (`markOf` resolves to the root). Its comment also documented a limitation that does not exist: it claimed a programmatic focus "may or may not" qualify for `:focus-visible`, and the probe says it does — `matches(":focus-visible")` is true and the ring computes solid at 2px. Deleted; the D9 law states the claim and can fail.

**The loud-extraction lesson had never reached the browser project.** Four law files carried `const markOf = (el) => el.querySelector(".kui-checkbox") ?? el`, which reads as "the mark inside, or the root if the root is the mark" and is neither: the root carries the family class and `querySelector` searches descendants only, so **the query arm has never once matched** — every call takes the fallback, and the helper documents an anatomy the component does not have. This is the node laws' own history (`test/stylesheets.ts` exists because ~20 `slice(indexOf(...))` sites went green on a renamed selector) and the harness's own idiom (`mounted({ select })` throws) arriving in the one place neither had reached. Promoted as `within()`, which throws. Reported honestly as **hygiene, not a hidden bug**: the first sabotage tried was a class rename, and it fails anyway because the stylesheet keys on the same name — 54 laws went red. The narrow hazard the loud form actually removes is a consistent rename *plus* a structural change, where the silent form hands back a wrapper and says nothing.

**Recorded, not fixed — each an observation rather than a repair:**

- **`contrast="high"` reaches nothing on a progress bar, and by policy that is correct.** Measured: the well and the fill are byte-identical between normal and high in both appearances, while the quote rule, the separator, the kbd edge and the code ink all move. The 2026-08-07 standing rule says why — a resting fill is dress, held to no floor, and high contrast is the conformance surface for *edges and signals*. A bar is the one component whose entire information is fill-against-fill, so the policy has no surface to act on here. Severity is low and measured rather than assumed: **3.06:1 in light and 4.13:1 in dark, identical to the shipped Slider's rail-versus-fill** (the same two roles) and above WCAG 1.4.11's 3:1 non-text floor at rest. Written down so the next audit does not rediscover it as a defect.
- **A Separator in a wrong-axis container is invisible but still in the a11y tree.** `<Stack><Separator orientation="vertical"/></Stack>` computes 1×0 — `align-self: stretch` cannot stretch an axis whose size is stated, and the block axis has no content to give it height. The horizontal case in a row Flex is the mirror. Not fixed, and the reason is §16's own argument for refusing pseudo-element hit expansion the first time: CSS cannot read the container's axis, so any fix would be a guess. The escape is to state the orientation the container actually has.
- **The switch's width ladder holds its claim.** Measured across all cells: 1.667–1.714 of the track's height, against config's stated 1.67–1.71, with the documented coarse size-4 collapse present exactly where §4 records it. No finding; recorded because the claim was prose with no law reading the rendered box, and now the number is on the record.

**Two of my own measurements were wrong before they were right, and both failed silently.** The first contrast probe used canvas `fillStyle` to normalise colours, which returns black for `color(display-p3 …)` — every pair read 1.00, a perfectly plausible-looking table of nonsense. The second parsed channels with `/[\d.]+/`, which takes the **3 in `display-p3`** as the red channel and produced 1.22 for everything. Neither would have been caught by anything except noticing that a measurement answering the same number for every input is not a measurement. Same class as the transmission law that rebuilt its expected value with the generator's own regex: **an instrument has to be calibrated against a case whose answer is known before its output is evidence.**

---

## 2026-08-06 audit — the apps/docs slice (multi-agent, adversarially verified)

Same method as 2026-08-03: independent lenses over the diff, every finding then handed to a verifier prompted to REFUTE it, surviving only what was personally reproduced. Six lenses (hydration/SSR, claimed-versus-actual, system law, a11y, build integration, runtime behaviour) plus a completeness critic. 27 findings confirmed, 5 refuted, deduped to eight significant defects.

**Two criticals, and both were about a half-guarded mechanism rather than a missing one.**

- **`pnpm dev` was broken on every route**, and the defect was in the package, not the docs. An element created in a Server Component and passed as `render` arrives across the RSC boundary as a **lazy node** — `props` undefined, `isValidElement` false — so `composeRender` threw at hydration and Next replaced the document. It is dev-only (facebook/react#32392): the production Flight build sends a real element, so `next build` was clean and `pnpm dev` was never once run. Base UI's own escape carries the workaround, via the public `React.Children.toArray`. Fixed, with a law that builds the Flight shape by hand — the scenario has no other route into a browser test.
- **A storage-denied browser got a blank site.** The store's two readers are `useSyncExternalStore`'s snapshot, so they run during render on every route; unguarded, Safari's "Block all cookies" and its equivalents took the whole app to Next's error page. The pre-paint script had always guarded the identical reads — so the half that fails *safely* was guarded and the half that kills the page was not. The script had the mirror defect (its single try/catch wrapped its own stamp, so a storage throw dropped a dark-OS visitor to the light fallback), and the fix was still incomplete after both guards landed: `apply()` re-derives from storage, so a write that could not persist was invisible to the next read and the toggle stayed dead. The session's truth is now memory; storage is persistence.

**Fixed with them:** the missing `not-found.tsx` (Next's fallback injects a `prefers-color-scheme` body style that re-decides appearance and is third-party UI on a route we serve — the stance and the bug had one fix); the type ramp sitting outside every pointer Theme, so the picker could not move the one section where type is shown *as* type; `next dev` writing an unauthored `apps/docs/CLAUDE.md` into a repo where that filename is governance — observed doing it mid-audit; and `ENGINEERING.md` still declaring the dark-SSR decision open, four lines from where `CLAUDE.md` called it closed.

**The lesson, and it is the 2026-08-03 lesson at one remove.** That audit's rule was *an axis is proven by a law that reads a computed value*. Every defect here sat where **no law existed at all**: `apps/docs` was not in the test graph, so the mechanism the dark-SSR debt was paid with had zero assertions and both criticals shipped through a green `pnpm run ci`; and the underline invariant had quietly become two sites with one law, so deleting the second left everything green. The rule grows accordingly: **a mechanism with two implementations owes a law that they agree**, and the first thing a new app in this repo owes is a place for its laws to live. Writing that law is what exposed the incomplete storage fix.

**Refuted, and worth keeping refuted:** the "every visible pixel is `@kookie-ui/react`" stance survived challenge over the hand-drawn icons — §8 says the icon set is "installed by the app (and the docs), never by the library", which names this exact case; the 404 was its one real breach. And the containment entry's diagnosis in the open-questions list held up under independent re-measurement (plain div 131px, containment disabled 131px, Box 0px), with only unstated preconditions on two of its three escapes.

**Confirmed and NOT fixed — carried to the open-questions list**, because each is a system decision rather than a repair: Button stamping `role="button"` on an anchor with `href` (the whole docs site exposes one link to a rotor, and a law currently pins the behaviour); TextField unable to shrink below its input's intrinsic width, so `/matrix` scrolls sideways at phone width; `.kui-surface:where(button, a)` resetting seven UA properties but not `display`, so a card-as-link is inline; `.kui-surface` never zeroing margin, which lets `<Card render={<figure/>}>` own outer spacing against a stated non-negotiable; and the a11y spelling of selection state on the pickers (`aria-pressed` versus a role, plus group labelling), which wants deciding once for the system rather than patched per page.

## Sound, no changes

Base UI behind a Kookie-owned API boundary. Token-only styling, no utility classes. Margin only on layout primitives. Size-as-index. Semantic radius references (`--radius-control-N`). Variants as shared role-token bundles. Tone x emphasis x material with appearance as resolved output (elevation was deleted as an axis on 2026-08-03; see LOG). Generated OKLCH color with a role layer. Build order (tokens, Button, Card, measured at each step).

## Blockers (fix in DECISIONS.md before code)

### 1. Responsive props have no compile story, and the CSS-size diagnosis is wrong

Measured v1 gzipped: ~91.5KB total, of which component CSS is ~55KB+ (responsive prop utilities per breakpoint; `sidebar.css` alone 48KB raw), all tokens ~13KB, ~~color scales ~2.8KB~~. The doc's diagnosis (colors are the bulk, component CSS is lean) does not hold for the shipped codebase. Section 3 promises `Responsive<SpaceToken | string>` object syntax; section 2 never says how it compiles. This is the single biggest size lever.

**Correction (2026-08-02): the color figure was wrong by an order of magnitude, and it was the number carrying the "colors are not the problem" claim.** Re-measured: `tokens/colors/` is 31 scales, 248KB raw, **29.3KB gzipped** — ~1.1KB per scale, which is what 2.8KB was probably a sample of. Full re-measure, each file gzipped separately: `styles.css` 127KB, `components.css` 67KB, `tokens.css` 36KB, `utilities.css` 25KB, `layout.css` 24KB (`sidebar.css` at 47,426 bytes raw confirmed). So color is a **major** term, not a rounding error, and shipping only the configured tones is doing real work — v2 currently spends 6,369 bytes gzipped on three tones in two modes with alpha ramps, a P3 block and a high-contrast block. What survives is the narrower claim: component + layout + utility CSS is still much larger than color, responsive prop utilities are still the biggest single lever, and section 2 still had no compile story for them. What does not survive is "the doc's colour diagnosis is wrong" — it is substantially right.

Options: (a) pregenerated token x breakpoint utility classes, the v1 mass scoped to layout primitives; (b) variable-per-breakpoint remap: props set `--gap`, `--gap-sm`, `--gap-md` inline, O(breakpoints) global rules decide which var wins per tier. (b) is the answer; it also absorbs the raw-string escape (`gap="13px"`) for free, which (a) structurally cannot. Decide it, write it into section 2, and prove it on Box before Flex/Grid/Stack ship.

Breakpoints stay the Radix scale (xs 520 / sm 768 / md 1024 / lg 1280 / xl 1640): the site rebuild is already written against it "to stay aligned with KookieUI." ~~Reversed the same day~~ — tiers are container-keyed and there are three of them (`sm` 30rem, `md` 48rem, `lg` 64rem), because a component should adapt to its slot rather than the window (DECISIONS §2; LOG 2026-07-31).

### 2. The fixed L ladder breaks on bright hues

Step 9 at L ~.62 is a mud olive for yellow, amber, lime, mint, sky. Radix hand-places those solid bands at L ~.85+ with dark contrast text. Because accent is an arbitrary user hue, someone will pass a brand yellow. APCA contrast text handles the label flip, not the ugly fill.

Already proven in-house: the site's color log rejected gold/yellow accents ("step 9 must hold 4.5:1, which a light hue cannot at any saturation") and hand-darkened the teal ramp's deep steps "where the hue runs bright." That hand adjustment is the missing term.

Fix without breaking one-law: make the solid band's L (steps 9/10/active) a bounded, continuous function of hue. Backgrounds, borders, and text bands keep the fixed ladder. Still generated, still not hand-tuning. Amend section 7 and remove "do not reopen" from the ladder (the chroma-vs-lightness tradeoff paragraph stands).

### 3. emphasis vs variant is unresolved, and the mapping is lossy

Section 8 defines five recipes (soft/solid/surface/outline/ghost); section 9 defines four emphasis rungs with `low -> surface/outline` naming two recipes for one rung, so one is unreachable through the semantic API. Decide before Button hard-codes it: is `variant` a public prop beside `emphasis`, or internal only? Which recipe does `low` resolve to, and how is the other reached (fifth rung, `variant` escape, or it dies)?

## Missing sections (silent in the doc, each needs a decision)

- **Focus-visible.** Absent from the state machine entirely. One system ring recipe (offset, color role, follows radius), defined once like hover/press.
- **Disabled and loading.** States are rest/hover/press only. One global law (which tokens survive, alpha or desaturation) or every component reinvents it.
- **Typography.** `--font-size-N` is referenced throughout with no type section: scale, line-height law, weight tokens, heading/body mapping.
- **Motion.** One interaction transition exists; overlay enter/exit (a stated reason for Base UI) has no duration/easing tokens or reduced-motion mapping.
- **Accent brand fidelity.** Does step 9 reproduce the supplied hex exactly (ladder bends locally) or normalize it to the ladder's L (output differs visibly from the brand color)? Pick one; pinning step 9 to the input in light mode is the defensible default.
- **`contrast="high"` mechanism.** A Theme prop with no generator story; it is a second ladder or an L-delta and should be costed in section 7.
- **Dark-mode flash / SSR.** `appearance` hydration (inline script vs class on html), RSC / `"use client"` boundaries.
- **Icon sizing.** `--icon-size-N` pulled by the same size index; one paragraph closes it.
- **Shell.** v1's flagship, one table row in section 11. Decide: v2 core, separate package on v2 primitives, or deferred. Its 48KB of v1 CSS makes it the stress test for blocker 1.
- **Migration.** v1 promises Radix API compatibility; v2 breaks it. Even "clean break, no codemods, v1 maintained until X" is a decision worth a line.
- **Density coupling.** Section 12 has density multiply all spacing, so compact mode shrinks page gutters, the same coarseness the doc rejects in Radix's `scaling`. Either scope density to control-adjacent tokens (height, control-px, control gaps) or accept the coupling in writing.

## One argument the doc must make

The site's DESIGN.md rejects the Box/Flex/Stack suite by name ("a layout DSL moves visual decisions into markup, where they cannot be reviewed as design"). v2 ships that suite. The reconciliation: in Astro, scoped CSS modules are the enforceable layer; in a React library, the component API is the only enforceable surface, so the primitives are the enforcement, not the leak. Write this into section 3 so the two documents do not argue.

## Build-order amendments (section 14)

- Insert between steps 2 and 3: prove the responsive-prop compile strategy on Box alone and measure it (blocker 1).
- Step 2's measurement should include a two-accent config (alpha ramps x modes x tones) so the budget covers section 7's full output.

---

# Audit 2026-08-06 — Radio and Slider (ultracode; findings OPEN, nothing fixed)

Scope: `771b95b..cb8a8f2` (the value-control tokens, the mark-family promotion, Radio + Slider). Eight parallel finders on one model, every finding then put to two adversarial verifiers on a *different* model — one refuting by reading, one by measurement — so a shared blind spot had to survive two models to land. 31 raw → 25 after dedup → **8 confirmed, 10 plausible, 7 refuted**. The three worst were re-measured by hand afterwards and all three reproduce.

**Verdict.** The promotion is clean and the additivity claim holds (+177 gzipped for two controls, a minted role, a designed ladder and the move itself; checkbox's mounted laws needed no edit, verified as a 0-line diff). What did not survive contact is the mark family's founding identity and the shared state layer's reach: **the thumb is the first mark that is not a `.kui-control`**, and every consequence of that — its box model, the invalid and disabled remaps, the box that accepts a press — shipped unmeasured. The pattern is 2026-08-03's lesson one level up: these laws do read the browser, they just read the wrong element or the wrong box, and in each case that thing was wrong.

## Confirmed

**R1 — the thumb paints 2px larger than the checkbox at every size, and is not a circle (major).** `box-sizing: border-box` is on `.kui-control`, not `.kui-mark`; the thumb wears `kui-mark kui-slider-thumb` with a real border, so it resolves content-box. Measured (`getBoundingClientRect`): checkbox 16/20/24/26 against thumb **18/22/26/28**. Falsifies §4's `checkbox = radio = slider thumb = mark(n)` and the §6 circle (radius 8px on an 18px box leaves a ~2px flat run per edge). Both governing laws are blind the same way — they compare `getComputedStyle().height`, which is the *content* box in both modes: 16 === 16 while the paint is 16 vs 18. *Fix:* `box-sizing: border-box` on `.kui-mark` (a no-op for checkbox and radio); re-write both laws against `getBoundingClientRect`. Switch inherits this — a track that is not a `.kui-control` is the next content-box mark.

**R2 — the invalid and disabled remaps never reach the thumb's edge (major).** `.kui-mark` declares `--tone-border: var(--mark-edge)` *on* the mark. For checkbox and radio the mark IS the control, so the shared remaps are same-element and win; the thumb is the first **descendant** mark and its own declaration shadows the inherited remap. Measured both appearances: rest, invalid and disabled thumb borders are byte-identical `--mark-edge`, while the root's `--tone-border` does become `--invalid-edge`. Nothing else on a slider reads the role, so **an invalid slider at rest is pixel-identical to a valid one** — its only expression is the focus ring, which arrives after the user has already focused. The contradiction is the promoted comment itself (recipes.css: re-pointing the role is load-bearing "because a component that reaches past them makes itself unreachable by every state law"): true for same-element topology, carried by the promotion to a topology where it is false. *Fix (a real choice):* a state arm reaching a descendant mark (`.kui-control:is([data-invalid],[aria-invalid="true"]) .kui-mark`, plus the disabled equivalent), **or** the family stops re-declaring `--tone-border` on the mark. Plus a law reading the thumb's `border-top-color` at rest/invalid/disabled in both appearances.

**R3 — under forced colors a checked Radio is indistinguishable from unchecked (major; predates these commits).** Screenshot diff under emulated `forced-colors: active`, light palette: radio checked→unchecked **0 changed px** (checkbox 0 too), against a negative control of 395/524 px with forcing off. Chromium's UA sheet sets `forced-color-adjust: preserve-parent-color` on SVG content, resolving `currentColor` to `--accent-contrast` white against a Canvas-white backplate; dark palettes survive by accident. Shipped with Checkbox 2026-08-05 and survived the 40-agent audit; 957a0ba generalised it to the family, and it is Radio's only state signal. AT is unaffected (the hidden input carries `checked`); the loss is for sighted HCM users. `grep -rn "forced-color"` over `src` returns zero — the package has no forced-colors handling at all. *Fix: scope is Kushagra's call — this would be the first forced-colors rule in the package.*

**R4 — the pressable strip is one border short of the height ladder's promise, and one cell breaks the locked floor (major).** Base UI's press handlers are on `.kui-slider-control`, which `align-self: stretch` fills to the root's *content* box (root − 2×`--border-width`). Row scan by `elementFromPoint`, null treated as an error: coarse/default/size 2 → root 44px, **43 live**; fine/compact/size 1 → root 24px, **23 live**. That last cell sits exactly on the tier-1 24px floor, so its real target is 23 — under WCAG 2.5.8 and under this repo's own non-negotiable. Trusted clicks agree (a click on the bottom row does not move the value). Both target laws read `getComputedStyle(root).height` — 44 and 24, both true — one indirection from the element Base UI hit-tests, so they cannot fail while the pressable box is 43/23. *Fix:* one declaration on `.kui-slider-control` (stretch over the border box, or move the border to the control); rewrite the two laws to press the box.

**R5 — standalone `aria-invalid` never reaches the node AT exposes as the slider (moderate).** It spreads onto Base UI's root, which is `role="group"`; the thumb's hidden `<input type=range>` carries `aria-label | aria-orientation | aria-valuenow` and no `aria-invalid`. Inside `Field.Root` Base UI wires it correctly, so the gap is exactly the standalone platform spelling §8 calls required — and `render`/`children` are refused, so a call site cannot fix it. *Fix:* forward `aria-invalid` to the thumb as `aria-label` already is, plus a law reading it off the range input.

**R6 — the mark-promotion law has two demonstrated escapes (minor).** Mutation-tested against a scratch copy: `height: calc(var(--kui-ct-mark) * 0.8)` and `height: var(--mark-2)` both pass the whole node suite (each is caught loudly by the member's own mounted laws, so this is defence-in-depth). `--kui-ct-mark: 40px` is *not* an escape — the raw-px law catches it. *Fix:* widen to `/(inline-size|block-size|width|height)\s*:[^;]*--kui-ct-mark/` plus `/--kui-ct-mark\s*:/`; both leave the legitimate `border-radius: calc(var(--kui-ct-mark) / 2)` alone.

**R7 — "law-tested at every level in both worlds" is false for one of the two breaching corners (minor).** Radio's circle law loops level × pointer × size; the thumb's loops levels only, in the fine world. Mutation: `[data-pointer="coarse"] .kui-slider-thumb { border-radius: 0 }` is caught in 20 cells on `.kui-radio` and **zero** by the shipped slider law. §6 and LOG both claim both worlds; only the radio half is true.

**R8 — "the only two corners the radius axis never reaches" is false: the rail is a third (minor).** The track's `calc(var(--kui-ct-track) / 2)` resolves through raw designed px with no radius-palette token in the chain, and the fill inherits it. Measured under `radius="none"`, both worlds, all sizes: track corners 2/2.5/3/3.5px (a full capsule) while root, Button, Checkbox and TextField all read 0. The claim appears in §6 twice, LOG and CLAUDE.md; LOG's slider entry documents the track exhaustively and never mentions its corner. *Fix is doc-shaped* (a capsule rail under `none` is what every platform ships): argue the end-cap as role the way the two circles are argued, correct the count in four places, add the missing law.

**R9 — the track-well law's recorded rationale names an axis that cannot reach it (minor).** The comment says a raw hex "would go deaf to `contrast=high`", but `trackWellStep` is 4 and `contrastHighBands` covers 5/6/7 and 10/11, so the high-contrast pass never emits index 4. Measured: `--color-track` is byte-identical between normal and high in both appearances while positive controls move. The `var()` indirection is still load-bearing — it buys the *appearance* scopes and the *P3* rewrite. *Fix:* reword to name appearance and P3. (Instructive: `--mark-edge` would have been half-true — dark's step 11 is in the text band and does move; light's step 9 does not.)

## Plausible (one line each; what would settle it)

- The track exemption's predicate "a well is a region the APCA-passing fill moves through" is not law-held at the boundary it implies — fill vs track measures Lc 44.97 light / 40.9 dark, both under the nonText floor of 45. *Decide which edge is the 1.4.11-bearing one, then write that law.*
- Both laws pinning `--slider-track-N`'s density/pointer invariance have blind spots (the mounted one resolves the token inside the scope it measures). *A whole-sheet occurrence assertion closes it in one line.*
- The invalid-slider law survives sabotage (`outline-color: rgb(1,2,3)` leaves both assertions true) — downstream of R2 either way.
- Slider collapses to its 2px borders in any indefinite-width containing block, and in a TextField slot the thumb stays `visibility: hidden` permanently. *The generic half IS the recorded Box shrink-wrap open item; does the slot case earn a line?*
- 3+ thumbs share one accessible name — Base UI's default `aria-valuetext` special-cases exactly two. *Is 3+ supported? If yes it needs a law.*
- `min === max` renders an invisible, unfocusable slider with a live `aria-valuenow`; Base UI logs a dev warning. *Is an upstream warning accepted as the coverage?*
- A bare Radio outside a group is silently inert, and `value=""` paints permanently checked — while radio.tsx's own docstring example shows a Radio with no group wrapper. *Fix the example, or decide the requirement is stated elsewhere.*
- Dark's `--color-surface-active` is the same token `--color-track` reads, so a slider on a pressed card-as-button has a track indistinguishable from its bed. *Eye pass, with numbers: light track vs page Lc 8.7; dark Lc 0.00.*
- RadioGroup renders an unnamed `role="radiogroup"` on its own documented path; two naming routes work and neither is documented. *One JSDoc sentence.*
- Both circle laws read only `border-top-left-radius`; a mutation squaring three corners passes both.

## Refuted — recorded so they are not re-raised

- **RTL is not a defect.** The physics reproduce, but `DirectionProvider` (a dependency's public entry, and an *ancestor*, so the closed `render` cannot block it) makes every measurement correct. RTL is a recorded deferral whose "architectural room left" verifies — slider.css is written entirely in logical properties.
- **No light-mode SC 1.4.11 failure.** The finding enumerated only boundaries internal to the rail; the thumb's edge against the page is 3.09–3.17:1 across ~85% of its perimeter.
- **The slider is not unreadable under forced colors.** `.kui-control`'s normally-transparent border is forced to CanvasText, so the root paints a bounded frame whose edges are the track's endpoints; position stays readable. What is lost is the fill/track distinction. (The real forced-colors defect is R3.)
- **The mark expander in a Card-as-button is not the D4 shape** — the halo behaves identically in a plain Card, and the finder's negative control was itself the error (the reach is general to any neighbour, which is what the 12px rule exists for).
- **`aria-invalid` on `role="radio"` is conforming** — "deprecated as global" flags a future restriction, not present non-conformance; Chromium maps it identically to `role=checkbox` and to a native radio. The suggested alternative is worse.
- **`--slider-track-N` is not specially frozen under a subtree `--scale`** — 29–47 tokens declared once at `:root` behave identically, `--icon-size-N` among them; the thumb and track freeze together.
- **"The promotion proving it changed nothing" is not an overclaim** — it is scoped in both LOG and CLAUDE.md to the mounted checkbox laws needing no edit, which is verifiably true. Residue: a stale comment at checkbox.browser.test.tsx (":not([data-slot] > *)" language outliving the respelling).
- **Recorded clean, do not re-raise** (all mounted, with negative controls): mixed Radio+Checkbox stacks hold the 12px rule in all 24 cells in both orders; the size-4 comfortable-coarse interior is slider-owned; the fill never paints outside the track at 0 or 100; `name` reaches every range input; SSR emits Base UI's prehydration script; Base UI's label association works across five shapes.

---

# Audit 2026-08-06 — the look axis (§19) and its companion fixes (ultracode; findings OPEN, nothing fixed)

Scope: `468344c` (merged as `3368e29`) — the `look` axis, its ten roles, five emission scopes and the matrix picker's fifth control — plus the five companion fixes carried in the same slice: `9c3031c` (the UA underline on Button), `9680524` (the hosted mark's equidistant inset), `9753fcc` (fields leave the elevated world), `371f5b4` (the RSC lazy-node `render` fix) and `6376fd4` (the underline's second site). The Radio/Slider slice (`771b95b..cb8a8f2`) and the apps/docs slice are excluded as already recorded; nothing below duplicates R1–R9, their plausibles or their refuted list. Method as 2026-08-06: eight parallel finders on one model, every survivor then put to two adversarial verifiers on a *different* model — one refuting by reading, one by measurement — so a shared blind spot had to survive two models to land. 33 raw → **31 after dedup → 14 taken to verification: 10 confirmed, 3 plausible, 1 refuted.** Seventeen were dropped unverified and are named below. A completeness critic added four more, personally reproduced but not adversarially verified.

**Verdict. The mechanism is sound; every value `filled` ships is not, and the pattern is that no look law compares anything to anything.** The machinery survived everything thrown at it: ten roles emitted in all five scopes and nowhere else (50 declarations, no scope short a role), `[data-look]` after both appearance blocks at equal (0,1,0) specificity so the co-located Theme element resolves, `initial` yielding the guaranteed-invalid value at every consumption site, the P3 rewrite reaching the roles by substitution with no per-gamut duplication, nested Themes escaping in both directions, and `outlined` byte-identical to the bare render for Card, TextField, TextArea, Checkbox, Radio and the slider thumb in both appearances. The artifact regenerates to a 0-line diff, and the +229 / +3 byte attribution is honest with no dead roles. What did not survive contact is the value layer and the law surface, and **one sentence covers all five defects: not one look law compares the two ends of the axis, and not one compares a dressed part to the thing it sits on.** These laws satisfy 2026-08-03's rule (mounted `<Theme>`, computed value) and R1–R9's correction (right element, right box) — and then measure a part against the token name the author had just written, or against itself across looks, or against a sibling that is wrong in the same direction. `card.browser.test.tsx` asserts a filled card is `var(--neutral-2)`, which in dark is satisfied by the outlined render it exists to distinguish itself from. `checkbox.browser.test.tsx` asserts the mark's border is `rgba(0, 0, 0, 0)` — it asserts the *removal* and never asks what is left. `slider.browser.test.tsx` pins the track unmoved across looks and never reads the thumb riding on it. The rule this earns: **a law about a boundary measures the boundary against what is on the other side of it, and a law about an axis compares its values to each other.**

**On severity.** Five findings were raised critical and all five came back major, on a ground both verifier models reached independently: `outlined` is the default and the identity, so nothing on the default path regresses, and §19 fences filled's numbers as "v0 config lines, eye pass pending." That defence is real, and it is also exactly what the missing laws would have made unnecessary — three of the five are *structural* (two config files that picked the same palette step, an axis value that resolves to its own opposite, a state with no arm), so an eye pass on one family cannot see them.

## Confirmed

**L1 — in dark, `filled` and `outlined` resolve the surface family to the same three fills, so the look only subtracts the border (major; found four times, by four lenses).** `surfaceColor.dark` is `{rest: var(--neutral-2), hover: var(--neutral-3), active: var(--neutral-4)}` (config.ts:638) and `look.filled.surface` is the same triple (config.ts:684-687). Measured through mounted Themes: dark outlined card `color(display-p3 0.08 0.083 0.0869)`, dark filled card the same string; hover `0.1098 0.1136 0.1184` and active `0.1416 0.1463 0.1521` identical in both looks; a real pointer via `userEvent.hover` on `<Card render={<button/>}>` agrees. The only delta the axis produces on a dark Card is `border-top-color` `color(display-p3 0.2737 0.2818 0.292)` → `rgba(0, 0, 0, 0)` — it costs the card its only separation (outlined edge vs page APCA |Lc| 10.77, WCAG 2.08:1) and returns nothing. *Negative controls, all in the same runs:* light differs on all three rungs (`rgb(255, 255, 255)` vs `0.9664 0.9673 0.9684`); in the same dark appearance the field family moves `0.08` → `0.1098` and the mark family `0.08` → `0.1416`. So the collapse is specific to the surface family in dark. *Law vacuity, measured not asserted:* card.browser.test.tsx:30 asserts `background-color === colorOn(el, "var(--neutral-2)")`, and in dark the *outlined* render satisfies that same target; tokens.test.ts:1028 regexes the neutral **step numbers** out of the `[data-look="filled"]` block and asserts 2 < 3 < 4, appearance-blind by construction. One verifier forced the same collision into light (`surfaceColor.light` → neutral-2/3/4) and got 757/757 green with `filled ≡ outlined` in **both** appearances; another deleted the flip outright and only the `light:` twin failed while the `dark:` twin stayed green. Two corrections that survive verification and must travel with the fix: "one neutral step apart per family" plausibly means the three families are one step apart *from each other*, which holds — the sentence that actually falsifies is §19's "trades the hairline for a darkened well"; and dark's seal sits *above* the page by design, so the remedy is a **lightened** well, not a darkened one. Also, filled-vs-page is |Lc| 0.00 in light too (#f6f7f7 on #fcfcfc), so weak fill-vs-page is a v0 matter in both modes — the dark-specific fact is the *zero delta between the two values of an axis*. *Fix:* express the filled fills relative to each mode's own seal, the way `surfaceColor`, `markEdge` and `focusRing` are already per-mode; plus the law the family has never had — for each family and each appearance, the painted `filled` value must differ from the painted `outlined` value.

**L2 — `filled` deletes the mark family's only boundary, voiding the guarantee audit D2 minted `--mark-edge` to provide (major; found twice, confirmed by all four verifiers).** `look.filled.mark.border` is the literal `"transparent"` (config.ts:695) against outlined's `"initial"`; recipes.css:275 is `--tone-border: var(--look-mark-border, var(--mark-edge))`, and `transparent` is a *valid* value, so the fallback never fires and D2's role is not consumed — not overridden, structurally unreferenced. Measured on an unchecked Checkbox inside a Card, all four appearance × contrast cells: `border-top-color` `rgba(0, 0, 0, 0)`; light mark `#e9e9ea` on card `#f6f7f7` → **|Lc| 0.00, WCAG 1.13:1**; dark `#242527` on `#141516` → **|Lc| 0.00, 1.19:1**; against the bare page, light 8.70 and dark 0.00. The floor is `apcaFloors.nonText = 45` (color-config.ts:226). Nothing else paints a boundary: `box-shadow: none`, `outline-style: none`, `background-image: none`, `::before` background transparent and 0px border, glyph `visibility: hidden` — the border box is there at 1px and paints nothing. `contrast="high"` cannot rescue it: `contrastHighBands` is border [5,6,7] / text [10,11], emitting neutral-6/7/8 and 11/12, while filled's hierarchy is 2/3/4; measured byte-identical at both settings. Radio and the slider thumb measure identically (one `.kui-mark` rule), so this is the family, not the checkbox. *Negative controls:* outlined edge |Lc| **63.90** light / **66.49** dark (WCAG 3.63:1 / 9.81:1), rising to 89.63 under dark + high — the metric is not stuck at zero and it tracks the contrast axis; a *checked* filled mark keeps |Lc| 58.04. *Law surface:* `look` appears **zero times** in color.test.ts, which is the only file in the package importing `apcaLc` — the D2 floor law takes `markEdgeStep` and `buildScale` as inputs and no value of `look` can fail it. checkbox.browser.test.tsx:347-352 asserts `border-top-color === "rgba(0, 0, 0, 0)"`, i.e. it pins the defect. A verifier applied the proposed fix (`border: "initial"`) and **three shipped laws went red** — tokens.test.ts's "filled darkens by the hierarchy" (whose last arm requires `transparent` for every family) plus the checkbox and radio "no hairline" laws; a second verifier shifted the whole filled hierarchy one step down instead and passed all 312 node laws, failing only six literal token pins. So the accessibility-restoring change is currently rejected by CI, and no law anywhere relates a look value to `apcaFloors.nonText`. *The counter-argument, recorded because it is the real question:* the trade is documented (§19, recipes.css:270, config.ts:653), and `--color-track` — shipping on the **default** path — is the identical `var(--neutral-4)` measuring 1.13:1 / 1.19:1 against the same bed, with a written exemption from the non-text floor (§4, LOG 2026-08-06) whose soundness REVIEW already carries as a plausible. *So this is a decision:* is a well's fill the 1.4.11-bearing boundary or is a hairline? Answer it once and it settles the slider track and the filled mark together.

**L3 — the slider thumb is byte-identical to the track it rides under `filled` (major).** `look.filled.mark.fill` is `var(--neutral-4)` (config.ts:692) and `trackWellStep` is `{light: 4, dark: 4}` (color-config.ts:216, emitting `--color-track: var(--neutral-4)`) — two families that picked the same palette step independently, in two different config files, with `filled`'s `transparent` border removing the only thing that separated them. Measured, every colour pushed through a 1×1 sRGB canvas because the tokens serve `color(display-p3 …)`: filled/light thumb `#e9e9ea` on track `#e9e9ea`, **1.000:1, identical**; filled/dark `#242527` on `#242527`, identical; all 8 filled cells (4 sizes × 2 appearances) identical. *Negative controls:* outlined/light thumb `#ffffff` + edge `#82878c` against track `#e9e9ea`, outlined/dark `#141516` + edge `#bcbec0` against `#242527` — different in every cell; and a **disabled** filled thumb reads `color(display-p3 0.933 0.9347 0.937)` against the same 0.913 track, so under `filled` a disabled slider handle is more visible than a live one. *Correction that must be kept:* "100% invisible" does **not** reproduce. Measured geometry is thumb 22×22 against a 5px rail — only 22.7% of the handle overlaps it — and a decoded-screenshot column scan shows the filled thumb painting 24 of 35 rows as one unbroken run against the Card, where the *outlined* thumb paints only 5 (its two hairline arcs, because outlined's thumb fill is `--color-surface`, the Card's own colour). What is 1.000:1 is the handle against the rail; what remains is the handle against the card at L2's numbers, because the thumb is a mark. *Law surface:* slider.browser.test.tsx:148 compares `trackOf(filled)` to `trackOf(outlined)` and to `colorOn(root, "var(--color-track)")` — neither assertion reads the thumb; :135 compares the thumb to a Checkbox, which is also neutral-4 under filled, so it *agrees* with the collision. Mutation: `look.filled.mark.fill` → `var(--neutral-8)`, tokens regenerated, **757/757 still passing** — the value is unconstrained in both directions. *Fix constraint:* moving only the thumb breaks slider.browser.test.tsx:142's `thumb ≡ checkbox` law, so the fix has to move the whole mark family off `trackWellStep`'s step or give the thumb back an edge. Then a law that measures the thumb against the **track**, and each mark against its **bed**, in both looks and both appearances.

**L4 — under `filled`, no resting boundary of any dressed family strengthens under `contrast="high"` (major), and the OS-level `@media (prefers-contrast: more)` is equally deaf.** filled, normal → high, both appearances: card, field and mark `border-top-color` `rgba(0, 0, 0, 0)` → `rgba(0, 0, 0, 0)`, fills byte-identical (light 0.9664 / 0.933 / 0.913; dark 0.08 / 0.1098 / 0.1416). *Negative control, same probe under outlined:* card and field border light `color(display-p3 0.8273 0.8322 0.8383)` → `color(display-p3 0.5863 0.5982 0.6134)` (WCAG vs bed 1.440 → 2.797), dark `0.2737 0.2818 0.292` → `0.4568 0.4693 0.4853` (2.078 → 4.288), dark mark edge `0.7381` → `0.8936`. APCA on the card's edge against the page: light 20.99 → 53.14 and dark −10.77 → −30.45 under outlined, against 0.00 → 0.00 in every filled cell. The critic reproduced the same result over CDP media emulation for `prefers-contrast: more` — the path a low-vision user reaches with **no app opt-in at all** — with outlined moving 0.8273 → 0.5863 under the identical emulation as the control. **Three corrections the record must carry, because the headline as raised is false.** (a) The setting is not a no-op in a filled app: text moves in both appearances (card text light `0.1202` → `0.0126`, dark `0.9204` → `0.98`; field value light `0.2697` → `0.1326`, dark `0.8195` → `0.9324`), a bordered Button's border moves, `--tone-border` and (in dark) `--mark-edge` move, the dark focus ring moves `0.6076 0.7577 0.9497` → `0.844 0.9036 0.9793`, the dark invalid edge moves, and the filled mark's **active** fill moves because neutral-6 is inside the border band (light `0.8667` → `0.6235`). (b) DECISIONS:1247's sentence is grammatically scoped to *outlined* and holds; the claim genuinely in tension is §10's material precedent at DECISIONS:838 — "the edge and rim empty, sending the border back to the tone system, which is the one place high contrast can reach it" — and §19's own invalid and disabled `initial` arms, which make contrast the third case with no arm. (c) DECISIONS:303's "contrast=high reaches the mark automatically" is already false in **light** under the default outlined look (`--mark-edge` is neutral-9, in neither band; measured identical at both settings, with dark as a working control) — which R9's closing parenthetical already records, so that half is not the look axis's doing. *Law surface:* theme.browser.test.tsx:168 is the only contrast law; it reads `--neutral-11` / `--accent-11` / `--neutral-label` off a bare probe and a verbatim copy with `look="filled"` forced passes 2/2. No law in the package crosses look × contrast on a painted family. *Fix, and it is constrained:* an `initial` stand-down inside a contrast scope is barred by the setting's own written contract (color-config.ts, `contrastHigh`: "it shifts values, it does not remap which step a role reads… A band that stays put is the setting working, not failing. The only real failure is a pairing that gets *worse*"), because it would silently convert a filled app into an outlined one. The shape that fits the contract is config-side — a filled-specific high-contrast band, or widening `contrastHighBands` to the well steps — asserted under **both** emitted blocks, since `[data-contrast="high"]` and `@media (prefers-contrast: more)` are separate and only one of them is what the user has switched on.

**L5 — a read-only field under `filled` paints nothing at all, and the governing law's own predicate is satisfied while it does (major; found twice).** recipes.css:506 routes the field edge through `var(--look-field-border, var(--tone-border))`; the disabled arm (recipes.css:570) and the invalid arm (:606) each stand the role down with `initial` under a "State outranks dress (§19)" comment; the read-only rules (text-field.css:130, text-area.css:90) zero only the three fill sources. `readOnly` is §8's third state and has no arm. Measured on TextField and TextArea, both appearances: `background-color` `rgba(0, 0, 0, 0)` **and** `border-top-color` `rgba(0, 0, 0, 0)`, `border-top-width` 1px, `box-shadow: none`. An **empty** read-only filled TextField screenshotted and decoded paints **0 non-white pixels** (against outlined/live 568, outlined/read-only 718, filled/live 4740; counter controls: a black div >1000, an unstyled div 0). Falsifies text-field.css:122 verbatim — "The border stays (it still bounds the value)" — and DECISIONS §8:695, written the day before the look axis existed. *Negative controls:* outlined + read-only keeps its edge (light `0.8273 0.8322 0.8383`, dark `0.2737 0.2818 0.292`); filled + live keeps its fill (light `0.933 0.9347 0.937`, dark `0.1098 0.1136 0.1184`); and under the same filled Theme a **disabled** field reads border `color(display-p3 0.8667 0.8704 0.875)` and an **invalid** one `color(display-p3 0.8626 0.2843 0.2931)` — direct proof the `initial` arm works and that read-only's missing arm is the whole difference. *Law surface:* text-field.browser.test.tsx:756 and text-area.browser.test.tsx:233 both call bare `render()` with no `<Theme>`, so they only ever see `outlined`, where their titles are true. Replaying their exact five predicates under a filled Theme returns all five true — `{ro bg transparent, plain bg not transparent, ro border === plain border, ro color === plain color, cursor text}` — while the control paints zero pixels, because the border assertion is *relative* and under filled both sides are `rgba(0, 0, 0, 0)`. Mutation in both directions: applying the fix keeps 109/109 and 757/757 green (the cell is pinned in neither direction), while a harness control injecting `--look-field-border: transparent` into the read-only rule fails the law at line 763, so the rig works. *Fix constraints, both from verification:* the arm cannot live in a component sheet — recipes.test.ts:124 fails any `--look-*` declaration under `components/`, so it belongs in recipes.css beside the other two; and looping the existing laws over `look` is not enough, because their parity assertion passes vacuously. The law must assert the absolute fact: **a field always paints either a fill or an edge**, across look × appearance × readOnly.

## Plausible (one line each; what would settle it)

All three are the same defects as L2, L3 and L4 seen from a second lens, and their verifier pairs **split** — which is the information. Each refuting argument is substantive and is what needs deciding.

- *L2 restated as an accessibility failure* — refuted on the ground that the trade is documented three times in the words the finder treated as a discovery, and that `--color-track` measures the identical 1.13:1 / 1.19:1 on the **default** path with a written exemption from the same floor. **Decide which edge is the 1.4.11-bearing one on a well, then write that one law for both wells.**
- *L3 restated as "the thumb disappears"* — refuted by pixel scan: under `filled` the thumb paints 24 of 35 rows of its silhouette against the Card, under `outlined` only 5, so the claimed direction inverts. **The residue is L2 at family scope; keep the collision, drop the "disappears" framing.**
- *L4 restated as "contrast reaches nothing"* — refuted because text, solids, the dark ring and the dark invalid edge all move, and three of the four quoted claims are scoped to outlined and hold. **Keep the narrow claim (no resting boundary strengthens) and decide the config-side remedy that `contrastHigh`'s own contract allows.**

## Refuted — recorded so it is not re-raised

- **"`contrast="high"` is a total no-op under `filled`" is false as written.** The measurement reproduces; the finding does not. Foregrounds move in every dressed family in both appearances (card text `0.1202` → `0.0126` light, `0.9204` → `0.98` dark; the field's value likewise), and `--look-mark-fill-active` moves because neutral-6 is inside `contrastHighBands.border`. The §19 sentence quoted takes *outlined* as its subject and explains why late binding keeps outlined's edge reachable; `filled` having no edge is the axis's definition and is law-pinned (`tokens.test.ts`: "every border withdraws — filled's boundary is the well itself"). The proposed `initial` arm is barred by `contrastHigh`'s own contract. What survives is the narrower L4 above, plus the observation that a filled card, field and mark separate from their beds by a single neutral step that no contrast setting can widen — a v0-values question, already fenced.

## From the completeness critic (reproduced, not adversarially verified)

- **A glass Card in a filled app gains a hairline no other card in the app has, under `prefers-reduced-transparency: reduce`.** surfaces.css:234 re-declares `--kui-border-color: var(--tone-border)` unconditionally in that block, whose own comment says the fallback "takes the ordinary surface dress back" — under `filled` the ordinary surface dress is no border. Measured over CDP media emulation: light glass card `color(display-p3 0.8273 0.8322 0.8383)` beside a plain card at `rgba(0, 0, 0, 0)`; dark `0.2737 0.2818 0.292` beside `rgba(0, 0, 0, 0)`. *Controls:* under `outlined` + reduce the two are byte-identical; a glass TextField and TextArea under filled + reduce stay transparent (the control layer's reduce block declares no border), so the bug is surfaces.css's alone. *Law gap, mutation-tested:* deleting the declaration entirely leaves 22 files / 757 tests passing; `prefers-reduced-transparency` appears in the suite only as text assertions on the sheet, and **no browser law in the repo has ever executed a media feature**. Three lenses reported this route unavailable; `cdp()` from vitest's browser context makes it a five-line helper, and the same helper closes L4's `prefers-contrast` gap.
- **`surfaces="elevated"` silently acquired a second composition.** surfaces.css:156-159 says "the border stays the same `--tone-border` every surface wears", restated in CLAUDE.md; under `filled` the elevated card is shadow + **no** border, in both appearances. The `box-shadow` half is genuinely untouched (byte-identical across looks in all four cells), so only the border sentence is false. Either amend both statements to name the two compositions, or decide that depth re-asserts an edge.
- **Both governing docs define the field family as "fill only" (DECISIONS.md:1242, CLAUDE.md:61); it consumes a border role, and that border is precisely what `filled` deletes.** Verified by forcing `--look-field-border: rgb(1, 2, 3)` on the mounted element — both `.kui-field` and `.kui-textarea` paint it. Doc-shaped, but load-bearing: it is the misconception under which the whole L5 / L4 cluster reads as fine on paper.
- The `prefers-contrast: more` measurement, folded into L4 above.

## Dropped unverified (17)

Named so they can be picked up rather than re-found. Four are restatements of confirmed findings (`untheme-2`, `contrast-2` → L4; `untheme-3` → L2). The rest, by severity: **major** — `lawq-2` (four of the ten roles are read by no law, and the law written for this defect class points at a token that no longer paints), `lawq-3` (Button's "belongs to no dressed family" negative law reads rest only), `contrast-3` (the filled field and interactive surface lose their boundary too, worsening a recorded-accepted state), **`companion-1`** (see below). **Moderate** — `claims-5` (the filled field's well is 1.08:1 against its bed with no substitute cue; Material ships an activation line for exactly this), `gen-4` (contrast reaches exactly one of the ten roles, so a filled mark's press jumps while rest and hover do not), `cascade-4` (a glass field under filled has no edge — "the material outranks the look on glass" is true only for the surface family), `lawq-4` (the D6 disabled law loops appearances but not looks), `untheme-4` (a disabled mark is lighter than a live one under filled and gains a border it lacks), `contrast-4` (the filled mark's ramp straddles the contrast-high band: invisible hover plus violent press). **Minor** — `lookflip-4`, `lawq-5`, `lawq-6`, `untheme-5`.

**`companion-1` is the one that is neither a duplicate nor about the look axis, and it should be verified first:** Button's `render` escape never goes through `composeRender`, so `371f5b4`'s RSC lazy-node fix reaches five components and misses the sixth — across the Flight boundary a `<Button render={…}>` would drop the consumer's `className`, `ref` and `onClick` and mis-derive `nativeButton` (`button.tsx:109`; `unwrapLazy` is module-private in `system/render.ts:55`). That is the same shape as the apps/docs critical, one component over.

## Verified clean, do not re-raise

All mounted, all with negative controls, all reproduced by the verifier pair where a finding was raised against them.

- **Emission and cascade.** `pnpm run tokens` regenerates to a 0-line diff. All ten roles in exactly five blocks (`:root`, both appearance scopes, both look scopes) and nowhere else; both `[data-look]` blocks after both appearance blocks at (0,1,0); the high-contrast and P3 blocks declare no look role, so nothing higher-specificity can silently re-dress. The P3 rewrite reaches the roles by substitution — every look value is a `var()` reference — so no per-gamut duplication is needed and none is emitted. `--color-surface` is declared in exactly three places and nothing below a Theme redeclares it, so `--look-*-fill: var(--color-surface)` cannot bake stale.
- **`initial` behaves at every reader.** The disabled and invalid arms resolve correctly on TextField, TextArea and both marks (marks win on specificity from (0,2,0) as claimed); a hosted mark in a field slot is byte-identical to a solo one. `outlined` is the identity end-to-end for six components in both appearances, and Button is look-invariant across 48 cells (2 appearances × 3 rungs × bordered × 4 materials, comparing background, border, colour and box-shadow: 0 diffs).
- **The recorded `[data-appearance]` reset is exactly as documented and no wider.** Sweeping the other six raw attribute scopes (`contrast`, `density`, `tone`, `radius`, `pointer`, `surfaces`) inside a filled app, every one keeps the filled dress; only `[data-appearance]` resets. And no colour role is `:root`-only — of 129 `:root`-only properties, all are mode-invariant.
- **`material` outranks `look` on glass** in both appearances and all three thicknesses (filled edges identical to outlined; only the veil's *source* moves). `surfaces="elevated"` × look: `box-shadow` byte-identical across looks in all four cells (the border half is the critic's item above). A filled field's `<input>` computes `rgba(0, 0, 0, 0)` — no double fill.
- **Contrast where it does work under `filled`.** Focus ring passes everywhere: worst cell light field ring vs its own fill |Lc| **52.89**, mark ring vs filled card 58.04, dark 66.92 / 66.04 (outlined control 62.94 / 66.92), floor 45. Invalid returns the error edge at |Lc| 55.30 light / 63.86 dark. Value text on a filled field 81.90 / 77.50 against the 60 body floor. **Forced colors rescues `filled` rather than breaking it** — card/field/mark converge exactly with outlined (`rgb(255, 255, 255)` on `rgb(0, 0, 0)`), Chromium forcing the transparent border to CanvasText.
- **Budget.** 18,547 gzipped, matching `budget.json`; +229 for the axis and +3 for the underline fix, attribution traceable. No dead bytes — every one of the ten roles has a consumer in a shipped sheet.
- **The five companion fixes.** The underline invariant is lawed at both sites and each law fails on mutation; `grep` finds exactly two `text-decoration` declarations in the whole `src`, and every component accepting `render` as an `<a href>` reads `none` (Box/Text/Theme read `underline`, correctly, being non-interactive). The hosted mark is equidistant in all 24 cells **and both slot sides** — 48 measurements, outer === top === bottom to 0 — and hosted buttons render byte-identically in all 24. Fields leave the elevated world: one `box-shadow` declaration in the entire package (surfaces.css:170), and the semicolon-less evasion attempt fails on two independent laws. `React.Children.toArray` really does resolve a lazy node through the public API, and the hand-built Flight law fails against pre-fix code with the recorded `TypeError`.
- **Theme plumbing.** The `resolved` memo depends on `props.look` *and* `parent.look`, so the react-best-practices invariant is intact; `contrastSet` untouched; `look` is a build-time constant with no platform signal, so there is no flash and no hydration branch. The "no stylesheet outside tokens.css declares a look role" law survives a deliberate semicolon-less escape (for an incidental reason — the greedy `[^;]+` overruns — but it holds).
- **Measured and deliberately not raised, for the eye pass:** the placeholder on a filled field drops |Lc| 69.50 → 59.45 light (no floor exists for the faint role by design, but `filled` spends ~10 Lc of its budget); disabled arms under filled are effectively invisible, but outlined is barely better and the signal is carried by the label going flat; and surfaces.css:129/:135 route the interactive steps through the look for **every** emphasis, not just quiet, against the sheet's own "dress never touches rank" — unreachable today because Card hard-codes `data-emphasis="quiet"`, and free to pin with a negative law before Callout or Panel ships.

## Method notes worth keeping

- **The Bash tool in this session runs sandboxed, and file writes made from `python3`/`sed` inside it appear to succeed and are then discarded.** One agent's mutation test reported a false green ("42 passed") on a stylesheet that was never touched; `git diff --stat` showed it. Every mutation in this audit was redone with the Edit tool and confirmed on disk **after** the run. Any mutation test in this round done via a Bash heredoc is a false green.
- **Parallel agents sharing one working tree contaminated real measurements.** Two verifiers caught negative controls failing impossibly (an outlined light card reading neutral-2), traced them to another agent's in-flight `config.ts` edit, discarded those runs and re-measured against `git show HEAD` or in a detached worktree. Every number above comes from a run bracketed by a clean-tree check.
- **APCA's implementation clamps |Lc| < 10 to exactly 0.00**, so a reported 0.00 means "under 10", not "identical pixels". Hexes and WCAG ratios are quoted alongside throughout so the real separation is visible.

---

## Resolution (2026-08-06, same day) — the value layer is fixed, the law surface is rewritten

Kushagra judged the axis in the preview against the audit's numbers and reversed two of its founding premises. Both reversals are recorded in LOG and in §19; what follows is only which findings they close.

**`filled` is no longer a trade.** Its borders were `transparent`; they are now the family's own softer edge, picked from inside `contrastHighBands.border` so `contrast="high"` still reaches them. This closes the mark-boundary cluster (`lookflip-2`, `contrast-1`, `claims-2`, `untheme-3`), the contrast-escape cluster (`lookflip-1`, `cascade-3`, `contrast-2`, `untheme-2`, `gen-4`), the read-only invisible field (`cascade-2`, `lawq-1`), and `contrast-3`. The read-only case is the one worth remembering: a state and a dress, each individually defensible, multiplied to a control that painted nothing at all.

**The filled steps are per appearance.** They were one mode-blind set naming raw `--neutral-2/3/4`, which in dark ARE `--color-surface` and its two states — so the surface family resolved byte-identically to `outlined`. Pigment now arrives through `--dress-<family>-<slot>` roles declared in each appearance scope. Closes `untheme-1`, `gen-3`, `claims-4`, `lookflip-3`.

**The track joined the axis**, reversing the exclusion recorded hours earlier, and the well moves rather than the mark family. Closes `claims-1` and `cascade-1`.

**Every look law was rewritten to compare something.** The audit's single sentence — *not one look law compares the two ends of the axis, and not one compares a dressed part to the thing it sits on* — was the real finding, and it is now false in both halves: per family and per appearance, `filled` must differ from `outlined`, and a dressed fill must differ from its bed. Four mutations were run against the shipped-and-broken config and each kills its law: dark's surface back on the seal, the borders back to transparent, the thumb back on its rail, an edge moved outside the contrast band. Budget 18,547 → 18,675 (+128, the dress roles across three scopes and the track's own role).

**Still open, carried forward.** The seventeen findings dropped unverified at the severity cap were not addressed and are not closed by the above — `companion-1` in particular (Button never goes through `composeRender`, so `371f5b4`'s RSC lazy-node fix skipped it and `button.tsx:109` reads `render.type` off a possibly-lazy node) was independently confirmed by reading and is the same bug shape that broke the docs site. The critic's four findings are likewise unverified. The eye pass on the new `dress` ladders has not happened: every step is a v0 config line.

