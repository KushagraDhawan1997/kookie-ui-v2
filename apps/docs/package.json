{
  "name": "docs",
  "private": true,
  "type": "module",
  "description": "KookieUI docs. Bare Next.js as the rendering substrate; every visible pixel is @kookie-ui/react. No docs framework and no third-party UI — a design system whose docs are built on someone else's system argues against itself (decided 2026-08-05, Kushagra; LOG). Icons are the exception the package itself names: §8 ships no icon dependency because icons are ReactNode slots the APP fills, so the app fills them with Hugeicons (2026-08-10).",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "eslint app blocks examples mdx-components.tsx mdx-plugins next.config.ts scripts && tsc -p tsconfig.json",
    "test": "vitest run",
    "api": "node --disable-warning=ExperimentalWarning --experimental-strip-types scripts/generate-api.ts",
    "api:check": "node --disable-warning=ExperimentalWarning --experimental-strip-types scripts/generate-api.ts --check"
  },
  "dependencies": {
    "@hugeicons/core-free-icons": "^4.2.3",
    "@hugeicons/react": "^1.1.9",
    "@kookie-ui/react": "workspace:*",
    "@shikijs/transformers": "^4.4.3",
    "next": "^16.3.0",
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  },
  "devDependencies": {
    "@mdx-js/loader": "^3.1.1",
    "@mdx-js/react": "^3.1.1",
    "@mdx-js/rollup": "^3.1.1",
    "@next/mdx": "^16.3.1",
    "@types/mdx": "^2.0.14",
    "@types/node": "^26.1.2",
    "@types/react": "^19.1.0",
    "@types/react-dom": "^19.1.0",
    "esbuild": "^0.28.1",
    "eslint": "^9.30.0",
    "remark-gfm": "^4.0.1",
    "shiki": "^4.4.3",
    "typescript": "^5.8.0",
    "vitest": "^4.1.0"
  }
}
